# v3.08 Feature Formula / Raw Source Specification Review

## Checkpoint

checkpoint = v3.08 Feature Formula / Raw Source Specification Review
record_type = documentation-only review record
specification_commit = 09ea505
specification_ci = Tests #381 green
specification_record = docs/runs/v3.08_exact_feature_formula_and_raw_source_specification.md

## Review Decision

decision = PASS_SPECIFICATION_REVIEW_FOR_RECIPE_PLANNING_ONLY

This review permits only a future documentation-only implementation planning checkpoint.

It does not authorize:
- data fetching
- data downloading
- dataset generation
- sealed dataset creation
- sealed dataset validation
- validation-only preflight rerun
- PPO v2 training
- sealed training command execution
- model artifact creation
- paper/live orders
- tagging

## Review Questions

1. Does the specification preserve the recovered v3.07 contract?
   - review_result = PASS

2. Does it clearly distinguish proposed standards from proven original recipe facts?
   - review_result = PASS

3. Does it define a complete enough baseline for future implementation planning?
   - review_result = PASS_FOR_PLANNING_ONLY

4. Does it avoid claiming byte-identical regeneration?
   - review_result = PASS

5. Does it preserve all no-generation, no-validation, no-training, no-order boundaries?
   - review_result = PASS

6. Does it keep unresolved items explicit?
   - review_result = PASS

7. Does it identify the next safe checkpoint before generation authorization?
   - review_result = PASS

## Contract Preservation Review

The reviewed specification preserves the recovered v3.07 contract:

- provider = Alpaca historical bars
- bar_interval = 1h
- hours_policy = regular session only
- timezone = UTC
- symbols = AAPL, AMD, MRK, PFE, UNH, XOM
- expected_output_path = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet

review_result = PASS

## Proposed-versus-Proven Distinction Review

The specification clearly marks the formula/source choices as proposed standards requiring review before generation authorization.

It does not claim that these choices prove the original byte-identical v3.07 dataset recipe.

It preserves that exact raw retrieval start remains unresolved:

- exact_raw_retrieval_start = TO_BE_FINALIZED_AFTER_WARMUP_POLICY_REVIEW

review_result = PASS

## Raw Source Boundary Review

The specification includes the required raw source boundary:

- raw_provider = Alpaca
- bar_interval = 1h
- timezone = UTC
- session_policy = regular_session_only
- symbols = AAPL, AMD, MRK, PFE, UNH, XOM
- contract_start = 2023-01-03T14:30:00Z
- contract_end = 2025-06-30T20:00:00Z
- warmup_start = at least 50 trading periods before 2023-01-03T14:30:00Z
- exact_raw_retrieval_start = TO_BE_FINALIZED_AFTER_WARMUP_POLICY_REVIEW

The specification does not authorize data fetch or download.

review_result = PASS

## Feature Formula Coverage Review

The specification defines or proposes formulas for:

- OHLCV pass-through
- per-symbol return_1h
- per-symbol log_return_1h
- SMA definitions
- EMA definitions
- RSI method
- MACD definitions
- ATR method
- realized_volatility_20
- volume_zscore_20
- close_to_sma20
- close_to_sma50
- hour_sin
- hour_cos
- day_of_week_sin
- day_of_week_cos

review_result = PASS

## Formula Method Review

The following formula choices are explicitly proposed and require review before generation authorization:

- RSI method = Wilder smoothing
- ATR method = Wilder smoothing
- EMA adjust = False
- rolling std ddof = 0
- rolling min_periods = window size
- NaN warm-up rows = dropped after feature generation but before split assignment
- infinity values = replaced with NaN, then handled by warm-up/drop policy
- zero std in volume_zscore_20 = output NaN, then handled by drop policy

These choices are complete enough for future implementation planning, but they are not generation authorization.

review_result = PASS_FOR_PLANNING_ONLY

## Deterministic Defaults Review

The specification includes deterministic defaults:

- all feature calculations grouped by Symbol
- sort by Symbol, Datetime before feature generation
- rolling min_periods = window size
- EMA adjust = False
- RSI = Wilder smoothing
- ATR = Wilder smoothing
- rolling std ddof = 0
- warm-up/NaN drop policy
- infinity handling
- duplicate Symbol/Datetime rejection
- OHLCV nonnegative validation
- observed Alpaca bars for early closes/holidays
- output column order = Symbol, Datetime, then configured feature columns in manifest order

review_result = PASS

## Byte-Identical Regeneration Review

The specification does not claim byte-identical regeneration.

The record preserves that the formula/source choices are proposed standards and that future reconstruction should be treated as a governed reconstruction plan unless a trusted artifact is later found.

review_result = PASS

## Unresolved Items Review

The specification keeps unresolved items explicit:

- exact_raw_retrieval_start requires warm-up policy review
- regular-session filtering exact rule requires review against Alpaca 1h bar timestamp semantics
- early-close and holiday handling requires review against observed Alpaca bars
- row-count methodology remains to be defined
- schema/order of output columns requires independent review before generation authorization
- source-controlled formula implementation binding remains required
- no data fetch/download/generation command is authorized

review_result = PASS

## Boundary Review

The specification preserves all required boundaries:

- no data fetching
- no data downloading
- no dataset generation
- no sealed dataset creation
- no sealed dataset validation
- no validation-only preflight rerun
- no PPO v2 training
- no sealed training command execution
- no model artifact creation
- no paper/live orders
- no tagging

review_result = PASS

## Review Conclusion

decision = PASS_SPECIFICATION_REVIEW_FOR_RECIPE_PLANNING_ONLY

The specification is accepted as a documentation-only planning baseline for a future implementation planning checkpoint.

This review does not authorize execution, data retrieval, dataset construction, validation, training, model artifact creation, or orders.

## Preserved Authorization State

preflight_readiness = NOT_PASSED
sealed_dataset_validation = NOT_PROVEN
sealed_dataset_recovery = NOT_AUTHORIZED
sealed_dataset_restore = NOT_AUTHORIZED
sealed_dataset_copy = NOT_AUTHORIZED
sealed_dataset_repair = NOT_AUTHORIZED
sealed_dataset_regeneration = NOT_AUTHORIZED
sealed_dataset_mutation_or_rewrite = NOT_AUTHORIZED
sealed_dataset_validation_execution = NOT_AUTHORIZED
validation_only_preflight_rerun = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED

## Command-Contract Guardrail

Verify that any command/run_id/path/config/output/evidence path in the checkpoint matches the source code, config, command manifest, artifact manifest, or source-of-truth record.

Do not accept manually invented values.

If run_id is involved, confirm:

authorized_command_run_id == source_required_run_id

No run_id execution is authorized in this checkpoint.

## Execution-Head Freshness Guardrail

Before any authorized execution or commit workflow:

- git fetch origin
- git status --short
- git rev-parse HEAD
- git rev-parse origin/main
- git merge-base --is-ancestor 09ea505 HEAD

Proceed only if:

execution_head_includes_required_review_commit = TRUE

## Boundary Confirmation

external_artifact_inspection = NOT_RUN
data_fetching = NOT_RUN
data_downloading = NOT_RUN
dataset_creation = NOT_RUN
dataset_copy = NOT_RUN
dataset_restore = NOT_RUN
dataset_repair = NOT_RUN
dataset_regeneration = NOT_RUN
dataset_mutation_or_rewrite = NOT_RUN
sealed_dataset_validation_execution = NOT_RUN
validation_only_preflight_rerun = NOT_RUN
ppo_v2_training_execution = NOT_RUN
sealed_training_command_execution = NOT_RUN
model_learn = NOT_CALLED
model_fitting = NOT_RUN
model_artifacts = NOT_CREATED
quarantine_outputs = NOT_CREATED
paper_orders = NOT_SUBMITTED
live_orders = NOT_SUBMITTED
tagging = NOT_RUN

## Recommended Next Checkpoint

next_checkpoint = v3.08 Dataset Reconstruction Implementation Plan

Purpose:

Plan the future code/command/evidence design needed to reconstruct the dataset, without executing generation yet.
