# v1.37 Paper-Trading Run Summary Includes Decision State

Date: 2026-06-10  
Status: Completed  
Mode: Engineering / no-submit reporting integration  

## Purpose

Add a consolidated paper-trading run summary artifact that includes the decision-state classification report.

This checkpoint integrates the v1.35/v1.36 decision-state reporting workflow into a higher-level run summary artifact.

No broker connection is required.

No orders were submitted.

## Starting State

```text
git_tag = v1.36.1-paper-trading-classification-hook-run-note
tests = 220 passed, 2 warnings
```

## Files Added

```text
src/paper_trading/build_run_summary_with_decision_state.py
tests/test_build_run_summary_with_decision_state.py
```

## New Run Summary Artifact

The utility writes:

```text
paper_trading_run_summary.json
```

inside the reviewed paper-trading run directory.

The summary includes:

```text
dry_run
execution_plan
paper_order_run
checklist
decision_state
safe_default
artifact presence flags
```

## CLI Usage

First write the decision-state report:

```bash
python -m src.paper_trading.pipeline_decision_state_hook \
  --run-dir reports/paper_trading_dry_runs/latest \
  --prior-symbol AMD \
  --prior-side buy
```

Then write the consolidated run summary:

```bash
python -m src.paper_trading.build_run_summary_with_decision_state \
  --run-dir reports/paper_trading_dry_runs/latest
```

## Latest Run Summary Result

The latest run summary included:

```text
state = NO_CANDIDATE_HOLD
decision = NO_SUBMIT
orders_required = 0
submit_allowed = False
```

## Safety Design

The run-summary utility:

```text
does not connect to Alpaca
does not submit orders
does not alter broker state
does not change execution plans
does not approve trades
```

It only reads local run artifacts and writes a local JSON summary.

## Test Coverage

New tests validate that:

```text
decision_state_report.json is included in the consolidated summary
safe_default.submit_allowed remains false
paper_trading_run_summary.json is written correctly
```

Validation commands:

```bash
python -m pytest tests/test_build_run_summary_with_decision_state.py
python -m pytest tests/test_pipeline_decision_state_hook.py
python -m pytest tests/test_classify_decision_state.py
python -m pytest
```

## Interpretation

v1.37 makes the decision state visible at the run-summary level.
This allows future dashboards, run notes, and portfolio reviews to read one consolidated artifact instead of manually checking several files.

## Guardrails

The summary artifact does not authorize trading.

A future controlled submit still requires:

```text
fresh dry run
evaluation pass
risk controls pass
checklist pass
single-order reviewed directory
manual approval
--max-plan-age-minutes 90
--confirm-run-dir <exact reviewed run dir>
broker verification after submit
documentation
```

## Next Step

Recommended next checkpoint:

```text
v1.38 Paper-Trading Dashboard Reads Decision State
```

Purpose: update dashboard/reporting documentation or dashboard generator so decision state appears in the decision dashboard.
