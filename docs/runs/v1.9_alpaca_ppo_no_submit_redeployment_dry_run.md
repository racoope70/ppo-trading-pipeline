# v1.9 Alpaca PPO No-Submit Paper-Trading Redeployment Dry Run

Date: 2026-06-01  
Status: Completed  
Mode: No-submit Alpaca paper-trading dry run  
Environment: Local VS Code virtual environment  
Broker: Alpaca Paper Trading  

## Purpose

Validate that the v1.9 Alpaca PPO candidate manifest can run through the full paper-trading safety chain without submitting orders.

This checkpoint follows:

```text
v1.8.5 Final Holdout Validation / Untouched Test Period
v1.9.1 Alpaca PPO Candidate Selection + Manifest Update
```

## Updated Manifest

```text
manifest = config/paper_trading_six_ticker_manifest.json
artifacts_dir = models/alpaca_ppo_models_master
```

Selected models:

```json
{
  "AAPL": "ppo_AAPL_window19",
  "AMD": "ppo_AMD_window14",
  "MRK": "ppo_MRK_window20",
  "PFE": "ppo_PFE_window11",
  "UNH": "ppo_UNH_window20",
  "XOM": "ppo_XOM_window15"
}
```

## Dry-Run Result

```text
predict_ok_count = 6
error_count = 0
orders_submitted = 0
Evaluation result = PASS
```

All six tickers produced valid predictions.

## Execution Plan Result

```text
rows = 6
orders_required = 1
gross_intended_notional = 61.763487
buy_count = 0
sell_count = 1
```

The only reviewed action was a small AMD rebalance:

```text
symbol = AMD
side = sell
qty = 0.120571
delta_notional = -61.763487
```

## Risk Controls

```text
Risk result = PASS
risk_passed = True
```

## Guarded Paper-Order Runner

```text
submit_orders = False
orders_required = 1
orders_submitted = 0
```

No orders were submitted.

## Pre-Trade Checklist

```text
Checklist result = PASS
broker_open_orders_zero = PASS
dry_run_error_count_zero = PASS
paper_order_summary_orders_submitted_zero = PASS
audit_log_orders_submitted_zero = PASS
```

## Interpretation

The v1.9 no-submit redeployment dry run confirms that the newly selected Alpaca-trained PPO candidates can run through the broker-connected paper-trading safety chain.

This validates:

- manifest loading
- artifact loading
- recent Alpaca bar retrieval
- model.predict() for all six symbols
- execution-plan creation
- risk-control checks
- guarded no-submit paper-order runner
- audit logging
- broker-state pre-trade checklist

This checkpoint does not submit orders and does not start unattended trading.

## Guardrails

Do not move to unattended trading.

Do not submit orders without manual review.

Future paper-order submission should remain controlled and limited to explicitly reviewed execution plans.

## Next Step

Recommended next checkpoint:

```text
v1.10 Controlled Alpaca Paper-Order Redeployment Test
```

Only consider this after reviewing the AMD rebalance behavior and deciding whether a small controlled submit test is appropriate.
