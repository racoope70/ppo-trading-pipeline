# v1.14 Controlled Single-Order Submit Test

Date: 2026-06-03
Status: Completed
Mode: Controlled single-order Alpaca paper submit test
Environment: Local VS Code virtual environment
Broker: Alpaca Paper Trading

## Purpose

Validate that the v1.13 single-order submit guard can be used to submit exactly one reviewed Alpaca paper order from a fresh execution plan.

This checkpoint follows:

```text
v1.13 Single-Order Submit Guard / Execution Filter
```

## Starting State

The system started from:

```text
git_tag = v1.13-single-order-submit-guard
tests = 200 passed, 2 warnings
```

## Fresh Dry Run

The v1.14 dry run used:

```text
manifest = config/paper_trading_six_ticker_manifest.json
artifacts_dir = models/alpaca_ppo_models_master
```

Dry-run result:

```text
datetime_utc = 2026-06-03T14:44:17.737627+00:00
predict_ok_count = 6
error_count = 0
orders_submitted = 0
Evaluation result = PASS
```

## Fresh Execution Plan

The fresh execution plan produced exactly one eligible order:

```text
orders_required = 1
gross_intended_notional = 271.006785
buy_count = 0
sell_count = 1
```

Reviewed order:

```text
symbol = AMD
side = sell
qty = 0.504002
price = 537.71
target_weight = 0.0
actual_weight = 0.002700165263911132
target_notional = 0.0
actual_notional = 271.006785
delta_notional = -271.006785
reason = rebalance_required
```

## Single-Order Filter

The v1.13 single-order guard was applied:

```text
output_dir = reports/paper_trading_dry_runs/v1_14_single_order_AMD_sell
selected_symbol = AMD
selected_side = sell
selected_qty = 0.504002
selected_delta_notional = -271.006785
original_orders_required = 1
disabled_order_count = 0
orders_required = 1
orders_submitted = 0
```

## Filtered Risk Controls

Filtered risk controls passed:

```text
Risk result = PASS
order_rows = 1
total_order_notional = 271.006785
single_order_notional_pct = 0.002700165263911132
summary_orders_submitted = 0
```

## Filtered No-Submit Checklist

The filtered no-submit checklist passed before submit:

```text
Checklist result = PASS
orders_required = 1
orders_submitted = 0
submit_orders = False
broker_open_orders_zero = PASS
broker_snapshot_errors_empty = PASS
```

## Controlled Submit

The guarded paper-order runner was run against the filtered v1.14 directory:

```text
run_dir = reports/paper_trading_dry_runs/v1_14_single_order_AMD_sell
submit_orders = True
```

Submitted order:

```text
symbol = AMD
side = sell
qty = 0.504002
order_submitted = True
order_id = a1db6804-1cb6-4159-81e4-1109191bf4e0
risk_passed = True
execution_note = order_submitted
```

Paper-order summary:

```text
rows = 6
orders_required = 1
orders_submitted = 1
submit_orders = True
```

## Broker Verification After Submit

Broker account remained active:

```text
status = AccountStatus.ACTIVE
equity = 100363.88
cash = 100363.39
buying_power = 200727.27
```

Position after submit:

```text
AMD qty = 0.00092
AMD market_value = 0.489733
AMD avg_entry = 502.160031
AMD unrealized_pl = 0.027746
AMD unrealized_plpc = 0.06006
```

Open orders:

```text
none
```

## Post-Submit Checklist Interpretation

The post-submit checklist reported FAIL because it still includes no-submit safety checks.

Expected post-submit failures:

```text
paper_order_summary_orders_submitted_zero = FAIL
paper_order_summary_no_order_mode = FAIL
audit_log_orders_submitted_zero = FAIL
audit_log_no_order_mode = FAIL
```

These failures are expected because v1.14 intentionally submitted one paper order.

Important checks passed:

```text
dry_run_predict_ok = PASS
dry_run_error_count_zero = PASS
paper_order_risk_passed = PASS
audit_log_risk_passed = PASS
broker_equity_near_expected = PASS
broker_cash_available = PASS
broker_open_orders_zero = PASS
broker_snapshot_errors_empty = PASS
```

## Interpretation

v1.14 confirms that the single-order submit guard works in the controlled paper-trading workflow.

Validated:

- fresh dry-run prediction cycle
- fresh execution-plan generation
- single-order filter usage
- filtered execution-plan validation
- risk controls on filtered plan
- pre-submit checklist on filtered plan
- controlled submit of exactly one selected paper order
- broker verification after submit
- no unexpected open orders

The remaining AMD position is a tiny fractional residual and should not be manually cleaned up without a fresh dry run and separate review.

## Guardrails

Do not run unattended trading.
Do not submit from reports/paper_trading_dry_runs/latest unless the plan has already been filtered or has exactly one reviewed order.
Do not submit another cleanup order immediately.

Future submit tests should continue to require:

- fresh dry run
- Evaluation result = PASS
- Risk result = PASS
- Checklist result = PASS before submit
- single selected order
- manual approval
- broker verification after submit

## Next Step

Recommended next checkpoint:

```text
v1.15 Post-Single-Order Monitoring / Residual Position Verification
```

Purpose: run a fresh no-submit monitoring cycle after the v1.14 AMD sell and verify that the tiny AMD residual position is handled safely without unnecessary overtrading.
