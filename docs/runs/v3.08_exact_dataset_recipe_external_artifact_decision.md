# v3.08 Exact Dataset Recipe / External Artifact Decision Checkpoint

## Checkpoint

```text
checkpoint = v3.08 Exact Dataset Recipe / External Artifact Decision Checkpoint
record_type = documentation-only decision record
new_execution_authorized = FALSE
dataset_operation_authorized = FALSE
validation_only_preflight_authorized = FALSE
training_authorized = FALSE
tagging_authorized = FALSE
```

## Source Evidence

```text
v3.08_project_context_alignment_commit = 9d0fe34
v3.08_project_context_alignment_ci = Tests #378 green

separate_read_only_repository_lineage_inspection = READ_ONLY_COMPLETED
codex_vs_code_repository_lineage_inspection = READ_ONLY_COMPLETED
```

The separate read-only lineage inspection was performed through the project's separate AI-assisted review context; the Codex / VS Code inspection was a distinct local-repository evidence path. Both remained read-only and are retained as separate provenance inputs.

## Shared Findings From Both Read-Only Inspections

```text
missing_dataset_path = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet

PATH_MISMATCH = NOT_SUPPORTED
DATASET_INTENTIONALLY_GITIGNORED = SUPPORTED
PACKAGE_PREPARATION_INCOMPLETE = SUPPORTED_AT_EXECUTION_READINESS_BOUNDARY
EXTERNAL_ARTIFACT_REQUIRED = STRONGLY_IMPLIED_OR_PLAUSIBLE
REGENERATE_FROM_TRACKED_RECIPE = NOT_YET_SUPPORTED
DATASET_CREATED_BUT_UNTRACKED = PLAUSIBLE_BUT_NOT_PROVEN
DATASET_NEVER_CREATED = UNDETERMINED
EXACT_DATASET_ORIGIN_AND_CREATION_STATUS = UNDETERMINED
```

## Codex / VS Code Repository-Lineage Findings

```text
local_worktree_clean = TRUE
local_HEAD_equals_origin_main = TRUE
dataset_exists_locally = FALSE
dataset_tracked_by_git = FALSE
dataset_ignored_by_gitignore = TRUE
git_history_shows_dataset_ever_committed_exact_path = FALSE
configs_scripts_dirs_at_root = NOT_PRESENT
src_prepare_data_py_exists_but_not_exact_recipe = TRUE
```

## Decision Interpretation

The exact dataset path is consistent across config, manifests, source, evidence, and project context.

```text
expected_dataset_path = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet
path_mismatch = NOT_SUPPORTED
```

The v3.07 package expected a pre-existing local input artifact:

```text
data/processed/ppo_v2/v3_07_no_submit_training_input.parquet
```

The package did not fetch, generate, or create that parquet.

The available repository files recover the dataset contract, but they do not recover the exact artifact identity or byte-identical generation recipe.

```text
available_repo_files = recover contract but not exact artifact identity or exact generation recipe
legacy research code = useful reference only
src/prepare_data.py = not proven exact v3.07 sealed dataset recipe
```

## Recovered Dataset Contract

```text
provider = Alpaca historical bars
bar_interval = 1h
hours_policy = regular session only
timezone = UTC
```

## Ticker Universe

```text
ticker_universe =
- AAPL
- AMD
- MRK
- PFE
- UNH
- XOM
```

## Minimum Expected Schema Metadata

```text
minimum_expected_schema_metadata =
- Symbol
- Datetime
```

## Configured Feature Columns

```text
configured_feature_columns =
- Open
- High
- Low
- Close
- Volume
- return_1h
- log_return_1h
- sma_10
- sma_20
- sma_50
- ema_12
- ema_26
- rsi_14
- macd
- macd_signal
- macd_hist
- atr_14
- realized_volatility_20
- volume_zscore_20
- close_to_sma20
- close_to_sma50
- hour_sin
- hour_cos
- day_of_week_sin
- day_of_week_cos
```

## Temporal Contract

```text
temporal_contract =
train: 2023-01-03T14:30:00Z to 2024-06-14T20:00:00Z
train_eval_embargo: 2024-06-17T13:30:00Z to 2024-06-21T20:00:00Z
evaluation: 2024-06-24T13:30:00Z to 2024-12-13T21:00:00Z
eval_holdout_embargo: 2024-12-16T14:30:00Z to 2024-12-20T21:00:00Z
holdout: 2024-12-23T14:30:00Z to 2025-06-30T20:00:00Z
```

## Source-of-Truth Files Found

```text
PROJECT_CONTEXT.md
artifacts/ppo_v2/package_preparation/v3_07_no_submit_training_execution_package/config/v3_07_no_submit_training_config.yaml
artifacts/ppo_v2/package_preparation/v3_07_no_submit_training_execution_package/manifests/data_source_manifest.md
artifacts/ppo_v2/package_preparation/v3_07_no_submit_training_execution_package/manifests/ticker_universe_manifest.md
artifacts/ppo_v2/package_preparation/v3_07_no_submit_training_execution_package/manifests/feature_set_manifest.md
artifacts/ppo_v2/package_preparation/v3_07_no_submit_training_execution_package/manifests/temporal_split_manifest.md
artifacts/ppo_v2/package_preparation/v3_07_no_submit_training_execution_package/manifests/preflight_validation_manifest.md
src/ppo_v2_data_contract.py
src/ppo_v2_validation_only_preflight_execution.py
src/ppo_v2_retraining_config.py
docs/runs/v3.08_diagnostics_only_sealed_dataset_source_of_truth_inspection.md
docs/runs/v3.08_sealed_dataset_recovery_strategy_authorization_plan.md
docs/runs/v3.08_trusted_external_artifact_source_inspection_authorization.md
```

## Historical Upstream Context

```text
quant-trading-model-validation/ppo_research_pipeline/ppo_multi_stock_training_pipeline.py
quant-trading-model-validation/ppo_research_pipeline/README.md
quant-trading-model-validation/config/paper_trading_six_ticker_manifest.json
quant-trading-model-validation/ppo_rf_research_pipeline/README.md
src/prepare_data.py in the local ppo repo exists but uses a materially different source boundary and is not proven to generate the v3.07 sealed contract
```

Historical upstream code is useful lineage context, but it is not an exact v3.07 sealed dataset recipe.

The old research code can help reconstruct design intent, feature conventions, and model lineage, but it should not be treated as proof of the byte-identical sealed dataset.

## Branch Evaluation

### BRANCH_A_EXACT_TRUSTED_ARTIFACT_INSPECTION

```text
branch = BRANCH_A_EXACT_TRUSTED_ARTIFACT_INSPECTION
status = CONDITIONALLY_VIABLE_ONLY_IF_EXACT_TRUSTED_ARTIFACT_LOCATION_IS_PROVIDED
authorized_now = FALSE
```

This branch is viable only if the user provides an explicitly named trusted artifact location, repository-controlled artifact bundle, release artifact, or mounted archive with enough evidence to assess:

```text
filename/path
checksum/hash
schema
ticker universe
date range
feature list
temporal split
mutation risk
```

Current evidence:

```text
exact_artifact_location_known = FALSE
trusted_external_artifact_identity = NOT_PROVEN
restore_authorization_ready = FALSE
```

Branch A may be reopened before regeneration if a trusted artifact location is later identified.

### BRANCH_B_EXACT_RECIPE_RECONSTRUCTION_PLAN

```text
branch = BRANCH_B_EXACT_RECIPE_RECONSTRUCTION_PLAN
status = RECOMMENDED_NEXT_BRANCH
authorized_now = DOCUMENTATION_OR_DESIGN_ONLY
dataset_regeneration_authorized_now = FALSE
```

This branch is viable as the next documentation/design checkpoint.

It must define:

```text
exact feature formulas
raw source boundaries
deterministic preprocessing
row-count methodology
manifest requirements
checksum requirements
no-training boundary
no-order boundary
command-contract guardrail
execution-head guardrail
```

It must not immediately regenerate the dataset.

The purpose of Branch B is to convert the recovered dataset contract into an exact, reviewable, reproducible recipe before any data operation is authorized.

### BRANCH_C_DECLARE_UNRECOVERABLE_AND_AUTHORIZE_NEW_SEALED_DATASET_PACKAGE

```text
branch = BRANCH_C_DECLARE_UNRECOVERABLE_AND_AUTHORIZE_NEW_SEALED_DATASET_PACKAGE
status = FALLBACK_BRANCH
authorized_now = FALSE
```

This branch is needed if:

```text
no trusted artifact exists
no exact recipe can be proven
```

If used later, this branch should create a new governed sealed dataset package under a new version/checkpoint, not pretend to restore v3.07.

## Recommended Decision

```text
recommended_next_branch = BRANCH_B_EXACT_RECIPE_RECONSTRUCTION_PLAN_UNLESS_USER_PROVIDES_EXACT_TRUSTED_ARTIFACT_LOCATION
```

## Rationale

```text
No exact artifact location is currently known.
No exact source-controlled generation recipe exists.
Existing repo files recover the contract but not a byte-identical recipe.
Legacy research code is reference only.
src/prepare_data.py is not proven exact v3.07 sealed dataset recipe.
Regeneration must be designed and reviewed before execution.
If the user later identifies a trusted artifact location, Branch A may be reopened before regeneration.
```

## Explicit Non-Authorization

This checkpoint does not authorize creating, copying, restoring, repairing, regenerating, mutating, rewriting, or validating the sealed dataset.

This checkpoint does not authorize validation-only preflight rerun, PPO v2 training, sealed training command execution, model artifact creation, paper orders, live orders, model promotion, deployment-readiness claims, trading-edge claims, profitability claims, or tagging.

## Preserved Authorization State

```text
preflight_readiness = NOT_PASSED
sealed_dataset_validation = NOT_PROVEN

sealed_dataset_recovery = NOT_AUTHORIZED
sealed_dataset_restore = NOT_AUTHORIZED
sealed_dataset_copy = NOT_AUTHORIZED
sealed_dataset_repair = NOT_AUTHORIZED
sealed_dataset_regeneration = NOT_AUTHORIZED
sealed_dataset_mutation_or_rewrite = NOT_AUTHORIZED
sealed_dataset_validation_execution = NOT_AUTHORIZED

validation_only_preflight_rerun = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED

model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED
```

## Command-Contract Guardrail

Verify that any command/run_id/path/config/output/evidence path in the checkpoint matches source code, config, command manifest, artifact manifest, or source-of-truth record.

Do not accept manually invented values.

If run_id is involved, confirm:

```text
authorized_command_run_id == source_required_run_id
```

No run_id execution is authorized in this checkpoint.

## Execution-Head Freshness Guardrail

Before any authorized execution or commit workflow:

```bash
git fetch origin
git status --short
git rev-parse HEAD
git rev-parse origin/main
git merge-base --is-ancestor <latest_required_authorization_or_review_commit> HEAD
```

Execution or commit workflow may proceed only if:

```text
execution_head_includes_required_review_commit = TRUE
```

For this checkpoint, the required alignment commit is:

```text
latest_required_authorization_or_review_commit = 9d0fe34
```

## Boundary Confirmation

```text
external_artifact_inspection = NOT_RUN
data_fetching = NOT_RUN
data_downloading = NOT_RUN
dataset_creation = NOT_RUN
dataset_copy = NOT_RUN
dataset_restore = NOT_RUN
dataset_repair = NOT_RUN
dataset_regeneration = NOT_RUN
dataset_mutation_or_rewrite = NOT_RUN
sealed_dataset_validation_execution = NOT_RUN
validation_only_preflight_rerun = NOT_RUN
ppo_v2_training_execution = NOT_RUN
sealed_training_command_execution = NOT_RUN
model_learn = NOT_CALLED
model_fitting = NOT_RUN
model_artifacts = NOT_CREATED
quarantine_outputs = NOT_CREATED
paper_orders = NOT_SUBMITTED
live_orders = NOT_SUBMITTED
tagging = NOT_RUN
```

## Recommended Next Checkpoint

```text
next_checkpoint = v3.08 Exact Recipe Reconstruction Plan
recommended_next_branch = BRANCH_B_EXACT_RECIPE_RECONSTRUCTION_PLAN_UNLESS_USER_PROVIDES_EXACT_TRUSTED_ARTIFACT_LOCATION
```

The next checkpoint should design the exact recipe reconstruction plan.

It should not regenerate the dataset until a later separate authorization checkpoint approves execution.
