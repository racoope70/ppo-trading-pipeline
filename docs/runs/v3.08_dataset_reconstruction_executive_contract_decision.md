# v3.08 Dataset Reconstruction Executive Contract Decision

## Checkpoint

`v3.08 Dataset Reconstruction Executive Contract Decision`

## Decision status

```text
decision = ADOPT_SUPERSEDING_GOVERNED_ALPACA_ALIGNED_RECONSTRUCTION_DIRECTION
byte_identical_original_dataset_recovery = NOT_CLAIMED
code_implementation = NOT_AUTHORIZED
contract_resolution_record = NOT_CREATED_YET
```

This record is the final executive direction decision. It is documentation only. It is not the later contract-resolution record, an implementation authorization, a command manifest, an execution record, or an evidence artifact.

## Governing inputs

1. The controlling executive decision supplied for this checkpoint.
2. `docs/runs/v3.08_dataset_reconstruction_executive_contract_decision_draft_synthesis.md`, used as supporting context.

The controlling executive decision governs wherever the draft synthesis contains alternatives, unresolved recommendations, or less-final wording.

## Executive conclusion

Adopt the direction of a **superseding governed Alpaca-aligned reconstruction contract**.

This decision chooses the reconstruction direction only. It does not claim that the missing original dataset was recovered, recreated, reproduced, or proven byte-identical. Exact unresolved literals must be bound in the later contract-resolution record before any superseding implementation authorization can be reconsidered.

## Selected path

```text
superseding governed Alpaca-aligned reconstruction contract
```

## Rejected or unauthorized paths

```text
byte-identical original dataset recovery
code implementation authorization
```

Byte-identical recovery is not supported or claimed. Code implementation remains unauthorized.

## Nine-choice executive direction

| # | Choice | Executive decision | Limitation or reserved detail |
|---:|---|---|---|
| 1 | Source interface | Use `StockHistoricalDataClient` → `StockBarsRequest` → `get_stock_bars` with explicit `IEX`. This is the strongest deployment-aligned direction. | The original missing dataset feed is not historically proven. |
| 2 | Adjustment policy | Use explicit `RAW` to avoid omitted/default ambiguity. | This is a governance choice, not recovered historical proof. |
| 3 | Warm-up retrieval | Require at least 50 valid observed regular-session bars per symbol. | Do not bind `2022-12-01T00:00:00Z` here. Derive and bind the exact UTC retrieval start in contract resolution after the calendar, timestamp semantics, and session-edge rules are finalized. |
| 4 | Market calendar | Use `exchange_calendars` with `XNYS` and require one exact pinned version. | Bind the exact version in the contract-resolution record. |
| 5 | Timezone conversion | Use a UTC-first contract: normalize provider timestamps to timezone-aware UTC, evaluate XNYS session boundaries in UTC, and serialize final `Datetime` values in UTC. | Exact implementation literals remain unauthorized until contract resolution. |
| 6 | Bar timestamp semantics | Use Alpaca provider-defined `1H` bars with bar-open, left-edge timestamps. Do not silently reinterpret them as custom 09:30-aligned bars. Prefer excluding partial session-edge bars unless a separate custom aggregation regime is explicitly reviewed and approved. | Literalize the exact session-edge rule in the contract-resolution record. |
| 7 | Missing-bar handling | Create no synthetic OHLCV bars; do not forward fill or interpolate; report gaps; fail closed on contract-critical expected-slot gaps. | Literalize exact expected-slot derivation and failure thresholds in the contract-resolution record. |
| 8 | Parquet engine | Use explicit `pyarrow`, not pandas `auto`. | Freeze exact pandas and PyArrow versions and all writer literals in the contract-resolution record. |
| 9 | Dtype and serialization contract | Require a fully explicit physical schema and writer contract. | Some exact literals remain unresolved and must be bound in the contract-resolution record. |

## Unresolved literals reserved for contract resolution

The next record must bind every remaining exact literal, including:

- the exact UTC warm-up retrieval start;
- the exact pinned `exchange_calendars` version;
- the exact session-edge rule;
- expected-slot derivation and gap-failure thresholds;
- the exact pandas and PyArrow versions;
- `Symbol` representation;
- `Datetime` representation and timezone;
- numeric dtypes and column ordering;
- compression, dictionary encoding, and statistics;
- row-group size and timestamp coercion;
- null, NaN, and infinity handling;
- index serialization;
- Parquet logical/format version; and
- every writer option.

No environment-dependent default may substitute for an approved literal. This executive record does not introduce or approve additional technical values.

## Non-byte-identical limitation

The original missing dataset has not been recovered. This decision does not claim exact historical recovery, byte-identical recreation, reproduction of original bytes, or proof of historical dataset identity. The selected path is a new superseding governed reconstruction direction.

## Authorization effect

This record authorizes only:

- creation of this executive decision documentation record; and
- progression to the documentation checkpoint `v3.08 Dataset Reconstruction Contract Resolution`.

It does not authorize code implementation, source-code or test changes, dependency installation, Alpaca access, market-data fetching or downloading, any dataset creation/copying/restoration/repair/regeneration/mutation/rewriting/validation, validation-only preflight, training, model fitting or artifact creation, paper or live orders, deployment, or tagging.

```text
contract_resolution_record = NOT_CREATED_YET
superseding_implementation_authorization = NOT_GRANTED
```

## Next required checkpoint

```text
v3.08 Dataset Reconstruction Contract Resolution
```

That later contract-resolution record must bind all remaining exact literals before any superseding implementation authorization can be reconsidered.

## Locked boundaries

```text
code_implementation = NOT_AUTHORIZED
data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_AUTHORIZED
validation_only_preflight_rerun = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
deployment = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED
contract_resolution_record = NOT_CREATED_YET
superseding_implementation_authorization = NOT_GRANTED
```

## Prohibited-action confirmation

Only this documentation record was created for this checkpoint. No contract-resolution record, superseding authorization record, source-code change, test change, data file, command manifest, or evidence artifact was created. No dependency installation, Alpaca access, data operation, validation, training, model fitting, artifact creation, order submission, deployment, or tagging occurred.
