# v3.08 Dataset-Generation Re-Execution Authorization

## 1. Metadata

```text
checkpoint = v3.08 Dataset-Generation Re-Execution Authorization
record_scope = DATASET_GENERATION_REEXECUTION_AUTHORIZATION_ONLY
latest_completed_checkpoint = v3.08 Source-of-Truth Alignment After Dataset-Generation Execution Remediation
latest_completed_commit = 25d103475414abd69a5cf91862cc3322e93ab1ac
independent_audit_decision = PASS_TO_CREATE_REEXECUTION_AUTHORIZATION_RECORD
```

## 2. Freshness evidence

```text
HEAD = 25d103475414abd69a5cf91862cc3322e93ab1ac
origin_main = 25d103475414abd69a5cf91862cc3322e93ab1ac
HEAD_equals_origin_main = YES
working_tree_clean_before_record = YES
current_active_checkpoint = v3.08 Dataset-Generation Re-Execution Authorization
dataset_generation_reexecution_currently_authorized = NO
authorized_current_execution_scope = NONE
```

## 3. Independent audit evidence

```text
independent_audit_checkpoint = v3.08 Dataset-Generation Re-Execution Authorization Readiness Audit
independent_audit_evidence_source = TASK_SUPPLIED
audit_scope = GOVERNANCE_AND_REEXECUTION_AUTHORIZATION_READINESS_ONLY
audit_decision = PASS_TO_CREATE_REEXECUTION_AUTHORIZATION_RECORD
governance_documents_reviewed = PROJECT_CONTEXT.md; docs/workflows/milestone_review_reference_map.md; docs/workflows/future_validation_training_reference_map.md
future_validation_training_reference_map_treated_as_future_guidance_only = YES
```

The independent audit approved creating this documentation-only authorization record. It did not authorize dataset generation directly.

## 4. Prior remediation evidence

```text
remediation_record = docs/runs/v3.08_dataset_generation_execution_remediation.md
remediation_decision = PASS_DATASET_GENERATION_EXECUTION_REMEDIATION_WORKSPACE_VENV_SELECTED_AND_IMPORTS_VERIFIED
latest_runtime_remediation_commit = dbbc658fc04ad494ad86dbd6cf07137bed686f09
selected_runtime_interpreter = ../.venv/bin/python
selected_runtime_imports_pass = YES
dependencies_installed_during_remediation = NO
requirements_changed_during_remediation = NO
```

## 5. Raw input and output identity evidence

```text
raw_input_path = data/raw/ppo_v2/v3_08_alpaca_iex_hourly_raw_bars.parquet
raw_input_exists = YES
raw_input_sha256 = 1694d9e706666c0718e940f40e28a8170b246ce7e2a07a570a5b122fd3c04e30
raw_input_sha256_match = YES

output_path = data/processed/ppo_v2/v3_08_superseding_alpaca_aligned_no_submit_training_input.parquet
manifest_path = data/processed/ppo_v2/v3_08_superseding_alpaca_aligned_no_submit_training_input.manifest.json
checksum_path = data/processed/ppo_v2/v3_08_superseding_alpaca_aligned_no_submit_training_input.sha256

final_dataset_exists_before_reexecution = NO
manifest_exists_before_reexecution = NO
checksum_exists_before_reexecution = NO

old_missing_path = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet
old_path_usage = HISTORICAL_MISSING_ARTIFACT_REFERENCE_ONLY
old_path_as_output_target = PROHIBITED
```

## 6. Runtime readiness evidence

```text
selected_runtime_interpreter = ../.venv/bin/python
selected_runtime_python_version = Python 3.12.2
pandas_import = PASS
pandas_version = 3.0.2
pyarrow_import = PASS
pyarrow_version = 24.0.0
exchange_calendars_import = PASS
exchange_calendars_version = 4.13.2
runtime_ready_for_reexecution_authorization = YES
```

## 7. Authorization scope for next checkpoint

The next checkpoint only is authorized:

```text
authorized_next_checkpoint = v3.08 Dataset-Generation Re-Execution
authorized_next_checkpoint_scope = DATASET_GENERATION_REEXECUTION_ONLY
dataset_generation_reexecution_authorized_for_next_checkpoint = YES
```

The next checkpoint may only:

- use the selected runtime interpreter `../.venv/bin/python`;
- read the existing raw parquet input at `data/raw/ppo_v2/v3_08_alpaca_iex_hourly_raw_bars.parquet`;
- verify the raw input SHA-256 before processing;
- generate the governed superseding v3.08 final dataset at `data/processed/ppo_v2/v3_08_superseding_alpaca_aligned_no_submit_training_input.parquet`;
- generate the required manifest at `data/processed/ppo_v2/v3_08_superseding_alpaca_aligned_no_submit_training_input.manifest.json`;
- generate the required checksum at `data/processed/ppo_v2/v3_08_superseding_alpaca_aligned_no_submit_training_input.sha256`;
- write the execution evidence record `docs/runs/v3.08_dataset_generation_reexecution.md`;
- use a local temporary execution script only if needed, preferably outside the repository or clearly untracked; and
- preserve the old missing v3.07 path as historical context only.

## 8. Explicit non-authorization boundaries

```text
data_fetch_authorized = NO
api_calls_authorized = NO
source_code_changes_authorized = NO
test_changes_authorized = NO
requirements_changes_authorized = NO
workflow_changes_authorized = NO
dataset_validation_authorized = NO
validation_only_preflight_authorized = NO
training_authorized = NO
model_artifacts_authorized = NO
orders_authorized = NO
deployment_authorized = NO
tagging_authorized = NO
```

This authorization does not permit data fetching, Alpaca API calls, source/test/requirements/workflow changes, dataset validation, validation-only preflight, training, model artifacts, orders, deployment, or tagging.

## 9. Fail-closed conditions

The next checkpoint must fail closed if:

- HEAD does not match the required authorization-aligned commit;
- the working tree is not clean before execution;
- the raw input is missing;
- the raw input SHA-256 does not match;
- required runtime imports fail under `../.venv/bin/python`;
- the final dataset, manifest, or checksum already exist before execution;
- output would target the old missing v3.07 path;
- any source, test, requirements, or workflow change is required;
- dataset validation is required;
- validation-only preflight is required;
- training is required;
- model artifacts are required;
- orders are required;
- deployment is required; or
- tagging is required.

## 10. Decision

The independent audit passed, runtime remediation completed successfully, the raw input exists with the expected SHA-256, required outputs do not yet exist, and governance boundaries remain intact. Dataset-generation re-execution may therefore be authorized for the next checkpoint only.

```text
dataset_generation_reexecution_authorization_decision = PASS_DATASET_GENERATION_REEXECUTION_AUTHORIZATION_FOR_DATASET_GENERATION_REEXECUTION_ONLY
```

## 11. Next checkpoint

```text
next_checkpoint_if_pass = v3.08 Dataset-Generation Re-Execution
next_checkpoint_scope = DATASET_GENERATION_REEXECUTION_ONLY
dataset_validation_remains_separately_governed = YES
training_remains_separately_governed = YES
orders_deployment_tagging_remain_separately_governed = YES
```
