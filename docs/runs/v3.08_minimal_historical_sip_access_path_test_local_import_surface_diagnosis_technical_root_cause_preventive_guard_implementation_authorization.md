# v3.08 TypeError Diagnosis Preventive-Guard Implementation Authorization

## 1. Metadata

```text
record_type = LOCAL_IMPORT_SURFACE_TYPEERROR_DIAGNOSIS_PREVENTIVE_GUARD_IMPLEMENTATION_AUTHORIZATION
record_status = CORRECTED_DRAFT_PENDING_INDEPENDENT_REREVIEW_ALIGNMENT_COMMIT_PUSH_AND_GREEN_CI
baseline_HEAD = f98bce8bf3c78c2d591984118b00af72f605b61c
baseline_origin_main = f98bce8bf3c78c2d591984118b00af72f605b61c
baseline_worktree = CLEAN
baseline_staging_area = CLEAN
baseline_CI = Tests_512_PASS_USER_CONFIRMED
authorization_effective = NO
current_authorization_boundary = NO_EXECUTION_AUTHORIZED
authorized_current_execution_scope = NONE
supplement_role = REQUIRED_CONTROL_SUPPLEMENT_TO_EXISTING_TYPEERROR_DIAGNOSIS_AUTHORIZATION_ONLY
selected_pathway = SIP_ACCESS_PREREQUISITE_ASSESSMENT_PLANNING_ONLY
selected_pathway_scope = SIP_ENTITLEMENT_PERMISSION_SUBSCRIPTION_LICENSING_PERMITTED_USE_RESTRICTIONS_PLAN_TIER_AND_COST_PREREQUISITES_ONLY
raw_input_path = data/raw/ppo_v2/v3_08_alpaca_iex_hourly_raw_bars.parquet
raw_input_sha256 = 1694d9e706666c0718e940f40e28a8170b246ce7e2a07a570a5b122fd3c04e30
total_missing_expected_slots = 66
missing_expected_slot_tolerance = 0
```

This documentation-only draft supplies the concrete preflight and guard design missing from the existing technical-root-cause diagnosis chain. It performs no technical activity and independently authorizes nothing.

## 2. Basis and blocker

```text
existing_authorization = docs/runs/v3.08_minimal_historical_sip_access_path_test_local_import_surface_diagnosis_technical_root_cause_authorization.md
existing_authorization_sha256 = 9cfd5c9acb7b2c307de741a3e9f7026bdedd9df642a8127c501e4d928308132e
existing_review = docs/reviews/v3.08_minimal_historical_sip_access_path_test_local_import_surface_diagnosis_technical_root_cause_authorization_independent_review.md
existing_review_sha256 = 5b989fd66821bc5ed01ff93716ff87090bae6da3d49cd9c724971068a2ac28de
alignment_commit = 3f798df880dd2b5091502b5881ce1bd4e8024b07
post_CI_reconciliation_commit = f98bce8bf3c78c2d591984118b00af72f605b61c
post_CI_reconciliation_CI = Tests_512_PASS_USER_CONFIRMED
technical_TypeError_cause = UNKNOWN
guard_contribution_to_TypeError = UNKNOWN
pre_execution_blocker = EXACT_GUARD_IMPLEMENTATION_AND_SAME_CONTROL_PROCESS_PREFLIGHT_NOT_LITERALIZED
```

## 3. Exact proposed control-process preflight

After future effectiveness only, one controlling shell process may run exactly two read-only Git utility invocations before technical inspection:

```text
preflight_git_1 = git rev-parse HEAD origin/main
preflight_git_2 = git status --porcelain=v1 --untracked-files=all
preflight_git_utility_process_count_maximum = 2
```

The first command must return exactly two lowercase 40-hex lines in this order: repository `HEAD`, then `origin/main`. The second command must return zero bytes. Any extra line, whitespace, warning, error, untracked path, staged path, unstaged path, or other output stops before `exec`.

The shell must then use `exec` to replace itself with the single CPython process. The future aligned command must embed the accepted supplement identity and CI identity as fixed shell literals and pass exactly nine positional arguments after the Python program:

```text
argv_1 = accepted_preventive_guard_supplement_sha256
argv_2 = accepted_preventive_guard_supplement_chain_commit
argv_3 = accepted_preventive_guard_supplement_CI_workflow
argv_4 = accepted_preventive_guard_supplement_CI_run_id
argv_5 = accepted_diagnosis_authorization_sha256
argv_6 = repository_HEAD
argv_7 = repository_origin_main
argv_8 = worktree_clean
argv_9 = staging_clean
required_sys_argv_length = 10
```

The single Python program must contain fixed literal constants for arguments 1 through 5 and the two fixed record paths. It must reject the invocation unless:

```text
argv_1_format = exactly_64_lowercase_hex
argv_2_format = exactly_40_lowercase_hex
argv_3_required_literal = Tests
argv_4_format = nonzero_decimal_digits_only
argv_5_format = exactly_64_lowercase_hex
argv_6_format = exactly_40_lowercase_hex
argv_7_format = exactly_40_lowercase_hex
argv_8_required_literal = YES
argv_9_required_literal = YES
argv_1_equals_embedded_supplement_sha256 = YES
argv_2_equals_embedded_supplement_chain_commit = YES
argv_3_equals_embedded_CI_workflow = YES
argv_4_equals_embedded_CI_run_id = YES
argv_5_equals_embedded_diagnosis_authorization_sha256 = YES
argv_6_equals_argv_2 = YES
argv_7_equals_argv_2 = YES
```

Before guard installation, the Python process must hash only the two fixed record paths and require the calculated hashes to equal arguments 1 and 5. The accepted supplement-chain commit, CI workflow, and CI run ID must be literalized by the later reviewed source-of-truth alignment; placeholders, environment-derived values, command substitution, additional files, or runtime discovery are prohibited. No environment-variable key or value may carry preflight evidence.

```text
shell_exec_replacement_required = YES
diagnostic_Python_process_count_maximum = 1
second_Python_process_count_maximum = 0
post_exec_subprocess_count_maximum = 0
exact_positional_argument_schema_required = YES
```

This allowance resolves only repository-preflight process accounting. It authorizes no technical activity in a Git process and no second diagnosis, retry, fallback, replacement probe, or alternate import.

## 4. Exact Python bootstrap order

The single CPython process must:

1. confirm `CPython 3.12.2`;
2. confirm the positional preflight identities and clean state;
3. hash only the existing authorization record and match its accepted SHA-256;
4. query only `importlib.metadata.version("alpaca-py")` and require `0.43.4`;
5. define message-free local sentinels `EnvironmentValueAccessBlocked`, `ExternalConfigurationAccessBlocked`, `NetworkOrTransportAccessBlocked`, and `ProcessCreationBlocked`;
6. install the one composite guard bundle below;
7. verify it once inside a non-rendering boundary;
8. only then perform the existing four fixed import probes in the reviewed order; and
9. discard every temporary callable, exception, traceback, frame, code, source, and guard reference before the sole sanitized execution record.

No `str`, `repr`, exception logging, traceback formatting or printing, interpreter-default exception rendering, or object representation is permitted.

## 5. Exact composite guard bundle

All captured callables must be stored in fixed local tuples before the first assignment. Installation must use the exact orders below. A missing required surface, non-callable surface, failed capture, failed assignment, or identity mismatch stops before any target probe. A partial installation is not rolled back; the process terminates, so no persistent patch survives.

Define one additional message-free local sentinel: `GuardSurfaceOrArgumentAmbiguous`.

### Environment and credential-source surfaces

Capture and replace these exact surfaces in this order:

```text
os.getenv
os.getenvb_when_os.supports_bytes_environ_is_true
os._Environ.__getitem__
os._Environ.get
os._Environ.keys
os._Environ.items
os._Environ.values
os._Environ.copy
os._Environ.__iter__
os._Environ.__contains__
```

Every replacement must raise `EnvironmentValueAccessBlocked` before reading, testing, enumerating, copying, or returning any environment key or value. When `os.supports_bytes_environ` is true, `os.getenvb` is required; when false, it must be absent from the installed set and recorded only as `NOT_APPLICABLE`.

### Environment-file and external-configuration surfaces

Capture these exact callables in this order:

```text
builtins.open
io.open
io.open_code
os.open
pathlib.Path.open
```

Use one exact helper, `guard_lowercase_basename(value)`, with this algorithm:

1. Accept only `str`, `bytes`, or `os.PathLike`.
2. Call `os.fspath(value)` exactly once.
3. Require the result to be `str` or `bytes`.
4. Convert `bytes` only with `os.fsdecode`.
5. Reject an empty string, an embedded NUL, a decoding failure, or any other type by raising `GuardSurfaceOrArgumentAmbiguous`.
6. Compute only `os.path.basename(path_text).casefold()`.
7. Reject an empty basename.
8. Do not call `abspath`, `realpath`, `resolve`, `stat`, `exists`, `isfile`, `isdir`, `expanduser`, or any filesystem operation.
9. Return the transient lowercase basename and retain neither the input nor the basename after the wrapper decision.

Define:

```text
blocked_exact_basenames = .env,credentials,secrets,config,settings
blocked_prefixes = .env.
blocked_suffixes = .env,.ini,.cfg,.toml,.yaml,.yml
```

A basename is blocked when it equals an exact basename, starts with `.env.`, or ends with any blocked suffix. This closed rule covers all patterns listed in the initial draft without wildcard interpretation.

Use exactly these wrapper signatures:

```text
guarded_builtins_open(file, mode="r", buffering=-1, encoding=None, errors=None, newline=None, closefd=True, opener=None)
guarded_io_open(file, mode="r", buffering=-1, encoding=None, errors=None, newline=None, closefd=True, opener=None)
guarded_io_open_code(path)
guarded_os_open(path, flags, mode=0o777, *, dir_fd=None)
guarded_path_open(self, mode="r", buffering=-1, encoding=None, errors=None, newline=None)
```

For `builtins.open` and `io.open`, an integer file descriptor or any unsupported first argument raises `GuardSurfaceOrArgumentAmbiguous` before delegation. For every wrapper, a blocked basename raises `ExternalConfigurationAccessBlocked` before the captured callable. A nonblocked path delegates exactly once to the corresponding captured callable with the original argument values and keyword structure. Assignment order must equal capture order.

### Network and transport surfaces

Capture and replace these exact callables in this order:

```text
socket.socket
socket.create_connection
socket.getaddrinfo
socket.gethostbyname
socket.gethostbyname_ex
socket.gethostbyaddr
```

Every replacement raises `NetworkOrTransportAccessBlocked` before socket creation, DNS, connection, HTTP/provider/API transport, or market-data communication. No higher-level transport package may be imported merely to add another guard.

### Post-bootstrap process-creation surfaces

The following `subprocess` surfaces are required, captured, and replaced in this order:

```text
subprocess.Popen
subprocess.run
subprocess.call
subprocess.check_call
subprocess.check_output
```

The following closed `os` candidate list is availability-driven. For each name in the exact order below, `getattr(os, name, MISSING)` may be used only to determine presence. Every present surface must be callable, captured, replaced, and identity-verified; an absent surface is recorded only as `NOT_PRESENT`. No prefix scan, `dir`, broad inventory, or additional module import is permitted.

```text
system
popen
execl
execle
execlp
execlpe
execv
execve
execvp
execvpe
execveat
spawnl
spawnle
spawnlp
spawnlpe
spawnv
spawnve
spawnvp
spawnvpe
posix_spawn
posix_spawnp
fork
forkpty
vfork
startfile
```

Each installed replacement must accept `*args, **kwargs` and raise `ProcessCreationBlocked` before reaching its captured callable. Missing or unassignable required `subprocess` surfaces, or any present but non-callable or unassignable `os` candidate, stops before probing. This exact closed candidate set is the reviewed zero-process enforcement set for CPython 3.12.2; it may not be expanded during execution.

```text
guard_bundle_count = 1
guard_installation_transaction_count_maximum = 1
guard_verification_transaction_count_maximum = 1
persistent_monkeypatch_authorized = NO
guard_disable_bypass_weaken_or_replace_authorized = NO
unguarded_comparison_authorized = NO
complete_zero_process_candidate_set_required = YES
```

## 6. Exact one-time guard verification

Before the first `alpaca.*` probe, define the following verification-only in-memory state:

```text
verification_labels = ENVIRONMENT,EXTERNAL_CONFIGURATION,SOCKET,DNS,PROCESS
verification_active_label = NONE
wrapper_entry_count_per_label_initial = 0
sentinel_before_delegate_per_label_initial = NO
captured_callable_reached_during_verification_per_label_initial = 0
underlying_prohibited_operation_count_initial = 0
```

Every guard wrapper must first increment its matching wrapper-entry counter when the active verification label and fixed synthetic input match. Immediately before any captured callable could be reached while a verification label is active, the wrapper must increment both the matching captured-callable counter and the aggregate `underlying_prohibited_operation_count`, then raise `GuardSurfaceOrArgumentAmbiguous` without delegating. On the intended blocked branch, the wrapper must set `sentinel_before_delegate = YES` and raise the expected sentinel before any captured callable.

Run exactly these five checks, one at a time, inside one non-rendering boundary:

```text
ENVIRONMENT: os.getenv("__PPO_V308_GUARD_VERIFY_ONLY__") -> EnvironmentValueAccessBlocked
EXTERNAL_CONFIGURATION: builtins.open(".env.__PPO_V308_GUARD_VERIFY_ONLY__", "rb") -> ExternalConfigurationAccessBlocked
SOCKET: socket.socket() -> NetworkOrTransportAccessBlocked
DNS: socket.getaddrinfo("guard-verification.invalid", 443) -> NetworkOrTransportAccessBlocked
PROCESS: subprocess.run(()) -> ProcessCreationBlocked
```

For each check:

1. set only its active label;
2. invoke the fixed call exactly once;
3. catch without `str`, `repr`, logging, traceback formatting, traceback printing, or default rendering;
4. require `type(caught_exception) is expected_sentinel_class`;
5. delete the exception reference immediately;
6. require wrapper entry count `1`;
7. require `sentinel_before_delegate = YES`;
8. require captured-callable-reached count `0`;
9. require aggregate underlying prohibited-operation count `0`; and
10. reset the active label to `NONE`.

No environment lookup, file open, socket, DNS query, process, URL, request, response, provider state, or market-data value may reach an underlying operation. After all five checks, require:

```text
all_wrapper_entry_counts = 1
all_sentinel_before_delegate_flags = YES
all_captured_callable_reached_counts = 0
underlying_prohibited_operation_count_required = 0
guard_verification_result_required = PASS_ALL_FIXED_CHECKS
```

Copy only the sanitized aggregate result, the five verification booleans, and integer zero into the future record state. Delete the per-check dictionaries, active label, synthetic inputs, and verification exception references before the first target probe. Keep only the installed guards and their captured callables until all authorized probes stop or complete; discard those references before creating the sole persistent record.

A missing surface, failed assignment, absent wrapper entry, no exception, unexpected exception class, captured-callable entry, nonzero aggregate, prohibited output, or uncertain result stops before any target import.

## 7. Existing probe boundary preserved

```text
target_1 = StockHistoricalDataClient at alpaca.data.historical.stock
target_2 = StockBarsRequest at alpaca.data.requests
target_3 = TimeFrame at alpaca.data.timeframe
target_4 = Sort at alpaca.common.enums
diagnostic_import_probe_count_maximum = 4
diagnostic_import_probe_count_per_target_maximum = 1
retry_count_maximum = 0
fallback_count_maximum = 0
replacement_probe_count_maximum = 0
alternate_or_speculative_import_count_maximum = 0
```

A guard sentinel during a target import is a scope-or-evidence boundary violation, not evidence of the original `TypeError` cause. No unguarded comparison is permitted. For a non-sentinel failure, guard contribution may be retained only when exact authorized frame evidence enters a replacement surface; otherwise it remains `UNKNOWN`.

## 8. Fail-closed conditions

Stop before probing for any Git, CI, hash, runtime, version, cleanliness, `exec`, guard-installation, or verification mismatch. Stop during diagnosis for any existing authorization stop condition, any guard sentinel, any need for another process, or inability to keep the exact bundle installed.

Stopping authorizes no guard change, broader pattern, added patch surface, retry, second process, alternate import, unrelated inspection, remediation, or corrected execution.

## 9. Supplemental sanitized evidence fields

A future sole diagnosis record may add only:

```text
preventive_guard_supplement_record
preventive_guard_supplement_sha256
preventive_guard_supplement_chain_commit
preventive_guard_supplement_CI_workflow
preventive_guard_supplement_CI_run_id
exact_positional_argument_schema_confirmed
shell_exec_replacement_confirmed
preflight_git_utility_process_count
post_exec_subprocess_count
composite_guard_bundle_installed
composite_guard_bundle_verified
environment_guard_verified
external_configuration_guard_verified
socket_and_DNS_guard_verified
process_creation_guard_verified
file_wrapper_algorithm_confirmed
zero_process_candidate_set_confirmed
verification_counter_algorithm_confirmed
guard_sentinel_raised_during_probe
guard_sentinel_target_or_NONE
underlying_prohibited_operation_count
```

No raw path, environment key or value, exception message, traceback, callable/module/object representation, URL, request, response, provider output, or market-data value may be retained.

## 10. Current non-authorizations

```text
technical_diagnosis_authorized_now = NO
guard_installation_or_testing_authorized_now = NO
Python_execution_authorized_now = NO
import_probe_authorized_now = NO
package_source_or_dependency_inspection_authorized_now = NO
credential_environment_configuration_client_request_network_provider_API_or_market_data_activity_authorized_now = NO
package_dependency_source_test_requirements_workflow_configuration_or_data_change_authorized_now = NO
remediation_corrected_execution_validation_training_trading_deployment_release_or_tagging_authorized_now = NO
```

## 11. Effectiveness conditions

This supplement remains ineffective until independent review passes with `findings = NONE`; any findings are corrected and re-reviewed; the accepted supplement is aligned across `PROJECT_CONTEXT.md` and the milestone map; that alignment independently passes; the full supplement chain is committed and pushed; exact-chain CI is green; `HEAD` and `origin/main` match; the repository is clean; and the immediate reviewed same-control-process preflight succeeds.

Until then:

```text
authorization_effective = NO
current_authorization_boundary = NO_EXECUTION_AUTHORIZED
authorized_current_execution_scope = NONE
```

## 12. Draft decision

```text
decision = PASS_CORRECTED_PREVENTIVE_GUARD_IMPLEMENTATION_AUTHORIZATION_DRAFT_FOR_INDEPENDENT_REREVIEW_ONLY
technical_activity_performed = NO
repository_file_created = YES_ONE_DOCUMENTATION_ONLY_DRAFT
existing_repository_file_modified = NO
staging_performed = NO
commit_performed = NO
push_performed = NO
```
