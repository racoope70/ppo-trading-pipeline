# v3.07 Explicit Validation-Only Preflight Rerun Authorization Checkpoint

## Checkpoint Decision

checkpoint = v3.07 Explicit Validation-Only Preflight Rerun Authorization Checkpoint
decision = PASS_VALIDATION_ONLY_PREFLIGHT_RERUN_AUTHORIZATION
authorization_scope = ONE_VALIDATION_ONLY_PREFLIGHT_RERUN_ONLY
validation_only_preflight_rerun = AUTHORIZED_FOR_ONE_VALIDATION_ONLY_RERUN

## Current Boundary State Preserved

v3.07_status = BLOCKED
NO_SUBMIT = DEFAULT
preflight_readiness = NOT_PASSED
sealed_dataset_validation = NOT_PROVEN
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
training_command_execution = NOT_AUTHORIZED
model_learn = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED
legacy_ppo_retraining_decision = DO_NOT_RETRAIN_LEGACY_MODEL

## Prior Evidence

previous_validation_only_preflight_result = PARTIAL_FAIL
R1 = PASS
R2 = FAIL_SEALED_DATASET_NOT_FOUND
R3 = FAIL_DEPENDS_ON_R2
R4 = FAIL_DEPENDS_ON_R2
R5 = FAIL_DEPENDS_ON_R2
R6 = PASS
fail_closed_evidence_review = PASS_FAIL_CLOSED_EVIDENCE_REVIEW_ONLY
sealed_dataset_local_placement_evidence_review = PASS_DOCUMENTED_LOCAL_PLACEMENT_REQUIREMENT_ONLY
expected_sealed_dataset_path = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet

## Interpretation of Prior Evidence

The previous validation-only preflight result was accepted as valid fail-closed PARTIAL_FAIL evidence.

It was not a full preflight readiness pass.

R1 and R6 passed. R2 failed because the expected sealed local dataset was not found. R3, R4, and R5 failed because they depend on R2.

The sealed dataset local placement requirement was documented and reviewed, but sealed dataset validation remains NOT_PROVEN.

## Authorized Later Action

This checkpoint authorizes exactly one later validation-only preflight rerun.

The later rerun may only:

- run validation-only preflight checks
- read/inspect the local sealed dataset only as needed for validation
- create validation evidence outputs only
- capture runtime/dependency/git-state evidence
- capture checksum/inventory evidence
- fail closed if the sealed dataset is absent, invalid, or fails R1-R6 checks

The rerun must be no-submit.

The rerun must remain validation-only.

The rerun must fail closed if required evidence is missing, the sealed dataset is absent, the sealed dataset is invalid, or any required R1-R6 check fails.

## Non-Authorized Actions

This checkpoint does not authorize the later rerun to:

- run PPO v2 training
- execute the sealed training command
- run training command execution
- call model.learn
- fit models
- fetch data
- download data
- generate datasets
- backfill datasets
- mutate or rewrite datasets
- create model artifacts
- create quarantine model outputs
- submit paper orders
- submit live orders
- authorize controlled submit
- unblock PPO + Random Forest
- unblock PPO + XGBoost
- promote models
- claim production deployment readiness
- claim trading edge
- claim profitability

## Training Boundary

Even if the later validation-only preflight rerun passes, PPO v2 training remains NOT_AUTHORIZED until a separate later sealed training authorization checkpoint.

sealed_training_command_execution = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
training_command_execution = NOT_AUTHORIZED
model_learn = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED

## Final Decision

decision = PASS_VALIDATION_ONLY_PREFLIGHT_RERUN_AUTHORIZATION
authorization_scope = ONE_VALIDATION_ONLY_PREFLIGHT_RERUN_ONLY
validation_only_preflight_rerun = AUTHORIZED_FOR_ONE_VALIDATION_ONLY_RERUN

This record authorizes one validation-only preflight rerun only. It does not execute the rerun.
