# v3.08 Dataset Reconstruction Code Implementation Authorization

## Checkpoint

```text
checkpoint = v3.08 Dataset Reconstruction Code Implementation Authorization
scope = CODE_IMPLEMENTATION_AUTHORIZATION_CHECKPOINT_RECORD
decision = FAIL_CODE_IMPLEMENTATION_AUTHORIZATION_REQUIRES_CONTRACT_RESOLUTION
code_implementation = NOT_AUTHORIZED
independent_authorization_review_request = NOT_READY
```

This record does not authorize code implementation. The required pre-implementation
contract is incomplete, so an independent authorization review must not be requested
as though the implementation boundary has passed.

## Prerequisite Evidence

```text
required_commit = 300f0df105de8b92165dd7c2fbf7efc6abb1dbc2
required_ci = Tests #387 green
recorded_gate_decision = PASS_INDEPENDENT_GATE_REVIEW_FOR_CODE_IMPLEMENTATION_AUTHORIZATION_CHECKPOINT
working_tree = clean
HEAD = 300f0df105de8b92165dd7c2fbf7efc6abb1dbc2
origin/main = 300f0df105de8b92165dd7c2fbf7efc6abb1dbc2
execution_head_includes_required_review_commit = TRUE
```

The repeated independent gate permits this authorization checkpoint to be evaluated.
It does not itself authorize implementation or resolve the contract choices below.

## Authorization Decision Basis

The repository requires all nine pre-implementation choices to be resolved before
code implementation authorization. Repository-supported evidence binds portions of
the contract, but it does not support exact values for all nine choices. Unsupported
feed, endpoint, adjustment, timestamp, calendar, dtype, and serialization values are
not invented in this record.

Because one or more required choices remain unresolved:

```text
decision = FAIL_CODE_IMPLEMENTATION_AUTHORIZATION_REQUIRES_CONTRACT_RESOLUTION
code_implementation = NOT_AUTHORIZED
```

## Nine Required Pre-Implementation Decisions

### 1. Exact Alpaca feed/source interface

```text
selected_source_mode = API_HISTORICAL_BARS
selected_alpaca_feed = UNRESOLVED
selected_endpoint_or_source_interface = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  binds `alpaca_source_mode = API_HISTORICAL_BARS` but states that the feed and
  endpoint/source interface must be selected before implementation authorization.
- `docs/audits/v3.08_independent_dataset_reconstruction_gate_review_repeat_after_remediation.md`
  confirms that the exact feed and endpoint or approved local source interface remain
  unselected.

Required resolution: bind an exact repository-supported Alpaca feed and exact API
endpoint/source interface. Authorization fails until both are resolved.

### 2. Adjustment policy

```text
selected_adjustment_policy = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  states `adjustment_policy = TO_BE_SELECTED_BEFORE_IMPLEMENTATION_AUTHORIZATION`
  and explicitly declines to freeze an unsupported value.
- The repeated independent gate confirms that the adjustment policy remains
  unselected.

Required resolution: bind one exact repository-supported Alpaca adjustment policy,
including split/dividend semantics. Authorization fails until it is resolved.

### 3. Exact warm-up retrieval timestamp/counting method

```text
contract_start = 2023-01-03T14:30:00Z
minimum_warmup = at least 50 valid per-symbol regular-session bars before contract_start
exact_raw_retrieval_start = UNRESOLVED
warmup_counting_method = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_exact_feature_formula_and_raw_source_specification.md` requires at
  least 50 trading periods of warm-up before the contract start.
- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  refines this to at least 50 valid per-symbol regular-session bars and requires a
  later checkpoint to choose observed-bar, calendar-slot, or another approved
  counting method.
- The repeated independent gate confirms that the exact timestamp and counting
  method remain unresolved.

Required resolution: bind an exact UTC raw retrieval start and exact deterministic
counting method. Authorization fails until both are resolved.

### 4. Market calendar

```text
selected_market_calendar_source = UNRESOLVED
calendar_identifier = UNRESOLVED
early_close_handling = UNRESOLVED
holiday_handling = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  states `market_calendar_source = REQUIRED_BEFORE_CODE_IMPLEMENTATION_AUTHORIZATION`.
- The repeated independent gate confirms that the market calendar remains unresolved.

Required resolution: bind the exact source/library, calendar identifier, version
policy, and deterministic early-close and holiday behavior. Authorization fails
until these are resolved.

### 5. Timezone conversion order

```text
final_timezone = UTC
selected_timezone_conversion_order = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- The recovered dataset contract binds the final timezone to UTC.
- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  states `timezone_conversion_order = REQUIRED_BEFORE_CODE_IMPLEMENTATION_AUTHORIZATION`.
- The repeated independent gate confirms that conversion order remains unresolved.

Required resolution: bind the exact order for interpreting provider timestamps,
applying bar semantics, converting for calendar/session selection, filtering the
regular session, and producing final UTC timestamps. Authorization fails until the
order is resolved.

### 6. Bar timestamp semantics

```text
selected_bar_timestamp_semantics = UNRESOLVED
interval_edge_behavior = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  requires an explicit determination whether Alpaca 1h timestamps represent bar open
  or bar close.
- The repeated independent gate confirms that bar timestamp semantics remain
  unresolved.

Required resolution: bind open/close semantics and exact interval/session-edge
behavior for Alpaca 1h bars. Authorization fails until these are resolved.

### 7. Missing-bar handling

```text
synthetic_missing_bar_policy = no synthetic bars unless separately authorized
observed_early_close_holiday_policy = use observed Alpaca bars
missing_observed_bar_disposition = UNRESOLVED
missing_bar_evidence_and_row_count_policy = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_exact_feature_formula_and_raw_source_specification.md` binds the use
  of observed Alpaca bars for early closes and holidays and forbids synthesizing
  missing bars.
- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  states `missing_observed_bar_handling = REQUIRED_BEFORE_CODE_IMPLEMENTATION_AUTHORIZATION`.
- The repeated independent gate confirms that missing-bar handling remains partially
  unresolved.

Required resolution: retain the no-synthetic-bars rule and bind exact handling,
reporting, per-symbol row-count, and failure behavior for missing observed bars.
Authorization fails until the full policy is resolved.

### 8. Parquet engine

```text
selected_parquet_engine = UNRESOLVED
engine_version_policy = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  states `parquet_engine = REQUIRED_BEFORE_CODE_IMPLEMENTATION_AUTHORIZATION`.
- The repeated independent gate confirms that the engine remains unresolved.

Required resolution: bind a repository-supported Parquet engine and applicable
version policy. Authorization fails until they are resolved.

### 9. Complete dtype/serialization contract

```text
column_order = Symbol, Datetime, then configured feature columns in manifest order
datetime_dtype_and_timezone_serialization = UNRESOLVED
symbol_dtype = UNRESOLVED
numeric_feature_dtypes = UNRESOLVED
output_index_policy = UNRESOLVED
compression_policy = UNRESOLVED
null_nan_inf_serialization_policy = UNRESOLVED
deterministic_serialization_contract = UNRESOLVED
fully_bound_before_implementation = FALSE
unresolved = TRUE
```

Repository support:

- `docs/runs/v3.08_exact_feature_formula_and_raw_source_specification.md` and the
  documentation remediation bind the minimum column order.
- `docs/runs/v3.08_dataset_reconstruction_documentation_package_remediation.md`
  requires complete dtypes, timezone-aware datetime handling, symbol dtype, numeric
  dtypes, index policy, compression policy, and related serialization details before
  implementation authorization.
- The repeated independent gate confirms that the complete dtype/serialization
  contract remains unresolved.

Required resolution: retain the bound column order and bind every remaining dtype and
serialization field. Authorization fails until the complete contract is resolved.

## Permitted Future Implementation Scope

No implementation is permitted under this failed authorization decision.

Only after a later authorization record resolves all nine choices, passes the
authorization checkpoint, is committed with green CI, and passes independent
authorization review may implementation be limited to:

- a dataset reconstruction module/script
- configuration parsing for the approved reconstruction contract
- formula generation matching the reviewed specification
- fail-closed command and configuration checks
- evidence-generation code paths without executing them against market data
- no-training/no-submit/no-orders attestations
- reconstruction-code tests
- dry validation of command parameters without fetching data
- documentation for the new code path

## Explicit Non-Authorization

This record does not authorize:

- immediate code implementation
- market data fetching or downloading
- dataset generation or sealed dataset creation
- dataset copy, restore, repair, or regeneration
- dataset mutation or rewrite
- sealed dataset validation
- validation-only preflight rerun
- PPO v2 training or sealed training command execution
- model fitting or `model.learn`
- model artifact creation
- paper or live orders
- deployment
- tagging

## Actions Forbidden During Any Future Implementation Checkpoint

Unless a separate later checkpoint explicitly changes the applicable boundary, a
future code implementation checkpoint must forbid:

- market data fetching or downloading
- dataset generation
- writing to `data/processed/ppo_v2/v3_07_no_submit_training_input.parquet`
- dataset mutation or rewrite
- sealed dataset validation execution
- validation-only preflight rerun
- PPO v2 training
- `model.learn` or model fitting
- model artifact creation
- paper or live orders
- deployment
- tagging

## Required Fail-Closed Controls

A future authorized implementation must fail before side effects if:

- `run_id` does not equal the authorized repository-supported run ID
- output path does not equal the authorized output path
- provider, Alpaca feed, or source interface is missing or unapproved
- adjustment policy is missing or unapproved
- symbols differ from AAPL, AMD, MRK, PFE, UNH, XOM
- bar interval is not 1h
- timezone is not UTC
- temporal boundary convention is not the approved convention
- market calendar is missing or unapproved
- timestamp semantics are missing or unapproved
- raw retrieval start or counting method is missing or unapproved
- Parquet engine is missing or unapproved
- the dtype/serialization contract is incomplete or differs from authorization
- the final sealed output exists without explicit overwrite authorization
- code attempts market-data access during implementation
- code attempts dataset generation or sealed-path writing during implementation
- a training, model, or order module is invoked
- no-training, no-submit, or no-orders flags are absent or false
- the execution head is stale or lacks the latest required authorization/review commit
- an unexpected file or path would be created

Any future run ID, command, path, configuration, output, or evidence path must match a
repository source-of-truth record. Manually invented values are forbidden. If a run ID
is involved:

```text
authorized_command_run_id == source_required_run_id
```

## Required Future Tests

A future authorized implementation must test:

- command-contract validation
- output and evidence path validation
- provider, feed, and source-interface validation
- exact six-symbol universe validation
- temporal boundary convention validation
- embargo exclusion logic
- feature-formula configuration binding
- complete serialization-contract binding
- the no-fetch/no-generation boundary
- the no-training/no-order boundary
- fail-closed behavior for invalid or missing parameters
- that tests create no sealed dataset output unless separately authorized

Tests must not contact Alpaca, fetch or download data, generate the sealed dataset, run
preflight, invoke training, create model artifacts, or submit orders.

## Required Independent Review

This failed record is not ready to authorize implementation. After all unresolved
choices are supported and bound, a superseding authorization record must be created.
That authorization record must be committed and CI-green, then independently reviewed
with a passing decision before any code implementation begins.

The required sequence is:

1. resolve all nine pre-implementation choices from repository-supported evidence
2. create a passing authorization record
3. commit the authorization record and obtain green CI
4. independently review the authorization checkpoint
5. confirm execution-head freshness against the required independent-review commit
6. begin only the independently approved, bounded code implementation

## Remaining Locked States

```text
data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_AUTHORIZED
validation_only_preflight_rerun = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
deployment = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED
```

## Boundary Confirmation

```text
reconstruction_code_implementation = NOT_RUN
command_manifest_creation = NOT_RUN
evidence_artifact_creation = NOT_RUN
market_data_fetching = NOT_RUN
market_data_downloading = NOT_RUN
dataset_creation = NOT_RUN
dataset_copy = NOT_RUN
dataset_restore = NOT_RUN
dataset_repair = NOT_RUN
dataset_regeneration = NOT_RUN
dataset_mutation_or_rewrite = NOT_RUN
sealed_dataset_validation_execution = NOT_RUN
validation_only_preflight_rerun = NOT_RUN
ppo_v2_training_execution = NOT_RUN
sealed_training_command_execution = NOT_RUN
model_learn = NOT_CALLED
model_fitting = NOT_RUN
model_artifacts = NOT_CREATED
paper_orders = NOT_SUBMITTED
live_orders = NOT_SUBMITTED
deployment = NOT_RUN
tagging = NOT_RUN
```
