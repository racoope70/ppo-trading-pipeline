# v1.11 Post-Order Monitoring / Rebalance Verification

Date: 2026-06-01  
Status: Completed  
Mode: No-submit post-order monitoring cycle  
Environment: Local VS Code virtual environment  
Broker: Alpaca Paper Trading  

## Purpose

Verify that the redeployed Alpaca PPO paper-trading system correctly handles the existing AMD position created during the v1.10 controlled paper-order test.

This checkpoint follows:

```text
v1.10 Controlled Alpaca Paper-Order Redeployment Test
```

## Starting State

The system started from:

```text
git_tag = v1.10-controlled-alpaca-paper-order-redeployment-test
tests = 196 passed, 2 warnings
```

Broker state before v1.11:

```text
status = AccountStatus.ACTIVE
equity = 100353.8
cash = 100095.73
buying_power = 200449.53
AMD qty = 0.504922
AMD market_value = 258.070683
AMD avg_entry = 502.160031
AMD unrealized_pl = 4.519036
AMD unrealized_plpc = 0.01782
open_orders = none
```

## Fresh Dry Run

The v1.11 dry run used:

```text
manifest = config/paper_trading_six_ticker_manifest.json
artifacts_dir = models/alpaca_ppo_models_master
```

Dry-run result:

```text
predict_ok_count = 6
error_count = 0
orders_submitted = 0
Evaluation result = PASS
```

All six tickers produced valid model predictions.

## Rebalance Decision

The AMD position was already close to the model target:

```text
AMD target_weight = 0.0025738220661878
AMD actual_weight = 0.0025705048912501374
AMD target_notional = 258.29254174537
AMD actual_notional = 257.959651
AMD delta_notional = 0.33289074536997987
```

Because the difference was below the minimum notional threshold, the execution plan correctly chose hold:

```text
AMD side = hold
AMD should_order = False
AMD reason = below_min_notional
```

## Execution Plan

```text
rows = 6
orders_required = 0
gross_intended_notional = 0.0
buy_count = 0
sell_count = 0
```

## Risk Controls

```text
Risk result = PASS
risk_passed = True
```

## Guarded Paper-Order Runner

The paper-order runner stayed in no-submit mode:

```text
submit_orders = False
orders_required = 0
orders_submitted = 0
```

No orders were submitted.

## Pre-Trade Checklist

```text
Checklist result = PASS
dry_run_predict_ok = PASS
dry_run_error_count_zero = PASS
paper_order_summary_orders_submitted_zero = PASS
paper_order_summary_no_order_mode = PASS
audit_log_orders_submitted_zero = PASS
audit_log_no_order_mode = PASS
broker_open_orders_zero = PASS
broker_snapshot_errors_empty = PASS
```

## Broker State After v1.11

Broker state after the monitoring cycle:

```text
status = AccountStatus.ACTIVE
equity = 100353.77
cash = 100095.73
buying_power = 200449.5
AMD qty = 0.504922
AMD market_value = 258.035288
AMD avg_entry = 502.160031
AMD unrealized_pl = 4.483641
AMD unrealized_plpc = 0.01768
open_orders = none
```

## Interpretation

v1.11 confirms that the system correctly monitored the existing AMD paper position after v1.10.

The model target and actual AMD exposure were nearly aligned, so the system correctly avoided an unnecessary rebalance.

Validated:

- existing position awareness
- fresh model predictions
- no-submit execution path
- risk-control pass
- pre-trade checklist pass
- no unexpected open orders
- no overtrading after controlled submit

## Guardrails

Do not move to unattended trading yet.

Do not submit repeated orders without a fresh dry run, execution plan, risk-control pass, broker check, and manual review.

## Next Step

Recommended next checkpoint:

```text
v1.12 Short Monitored Alpaca Paper-Trading Session
```

This should run one or more supervised no-submit/optional-submit cycles over a short period, while continuing to verify broker state after each cycle.
