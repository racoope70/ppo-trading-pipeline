# v1.17 Post-Policy Monitored Paper-Trading Session

Date: 2026-06-04
Status: Completed
Mode: No-submit monitored paper-trading session
Environment: Local VS Code virtual environment
Broker: Alpaca Paper Trading

## Purpose

Run one supervised no-submit paper-trading cycle using the v1.16 Paper-Trading Session Policy / Operational Runbook as the formal operating guide.

This checkpoint follows:

```text
v1.16 Paper-Trading Session Policy / Operational Runbook
```

## Starting State

The system started from:

```text
git_tag = v1.16-paper-trading-session-operational-runbook
tests = 200 passed, 2 warnings
```

Broker state before v1.17:

```text
status = AccountStatus.ACTIVE
equity = 100363.84
cash = 100363.37
buying_power = 401454.42
AMD qty = 0.00092
AMD market_value = 0.468664
AMD avg_entry = 512.026
AMD unrealized_pl = -0.0024
AMD unrealized_plpc = -0.00509
open_orders = none
```

## Fresh Dry Run

The v1.17 dry run used:

```text
manifest = config/paper_trading_six_ticker_manifest.json
artifacts_dir = models/alpaca_ppo_models_master
```

Dry-run result:

```text
datetime_utc = 2026-06-04T14:24:17.901301+00:00
predict_ok_count = 6
error_count = 0
orders_submitted = 0
Evaluation result = PASS
```

All six tickers produced valid model predictions.

## Execution Plan

The execution plan required no orders:

```text
rows = 6
orders_required = 0
gross_intended_notional = 0.0
buy_count = 0
sell_count = 0
```

The remaining AMD residual was below the minimum notional threshold:

```text
AMD side = hold
AMD qty = 0.0
AMD actual_weight = 0.000005
AMD actual_notional = 0.469283
AMD delta_notional = -0.469283
AMD should_order = False
AMD reason = below_min_notional
```

## Risk Controls

```text
Risk result = PASS
order_rows = 0
total_order_notional = 0.0
single_order_notional_pct = 0.0
summary_orders_submitted = 0
```

## Guarded Paper-Order Runner

The paper-order runner stayed in no-submit mode:

```text
submit_orders = False
orders_required = 0
orders_submitted = 0
risk_passed = True
```

No orders were submitted.

## Pre-Trade Checklist

```text
Checklist result = PASS
dry_run_predict_ok = PASS
dry_run_error_count_zero = PASS
execution_plan_review_required = PASS
paper_order_summary_orders_submitted_zero = PASS
paper_order_summary_no_order_mode = PASS
audit_log_orders_submitted_zero = PASS
audit_log_no_order_mode = PASS
broker_equity_near_expected = PASS
broker_cash_available = PASS
broker_open_orders_zero = PASS
broker_snapshot_errors_empty = PASS
```

## Broker State After v1.17

Broker state after the monitored cycle:

```text
status = AccountStatus.ACTIVE
equity = 100363.84
cash = 100363.37
buying_power = 401454.42
AMD qty = 0.00092
AMD market_value = 0.470756
AMD avg_entry = 512.026
AMD unrealized_pl = -0.000308
AMD unrealized_plpc = -0.00065
open_orders = none
```

## Interpretation

v1.17 confirms that the v1.16 operational runbook can be followed successfully for a supervised no-submit paper-trading session.

Validated:

```text
fresh broker-connected dry run
model predictions for all six symbols
execution-plan generation
risk-control pass
no-submit paper-order loop
pre-trade checklist pass
broker-state verification
residual AMD position handled as below minimum notional
no accidental submit
no unexpected open orders
```

## Guardrails

```text
Do not move to unattended trading.
Do not submit from stale plans.
Do not force cleanup orders for tiny residual positions.
```

Future submit tests should continue to require:

```text
fresh dry run
evaluation pass
risk-control pass
pre-submit checklist pass
manual review
single selected order
broker verification after submit
documentation
```

## Next Step

Recommended next checkpoint:

```text
v1.18 Stale-Plan Prevention / Max-Plan-Age Enforcement
```

Purpose: add or formalize protection so stale execution plans cannot be submitted after the market data or latest bar becomes too old.
