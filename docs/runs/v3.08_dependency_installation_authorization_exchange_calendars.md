# v3.08 Dependency Installation Authorization for exchange-calendars==4.13.2

## Checkpoint metadata

```text
checkpoint = v3.08 Dependency Installation Authorization for exchange-calendars==4.13.2
record_scope = DOCUMENTATION_ONLY_DEPENDENCY_INSTALLATION_AUTHORIZATION
latest_completed_checkpoint = v3.08 Requirements/Dependency File Update for exchange-calendars==4.13.2
latest_completed_commit = c7e05c2717ca0a53c5aab002b94abfe582ca6103
latest_completed_change = requirements.txt contains exchange-calendars==4.13.2
latest_completed_ci = NOT_INDEPENDENTLY_VERIFIED_IN_THIS_CHECKPOINT

dependency_installation_authorization_decision = PASS_DEPENDENCY_INSTALLATION_AUTHORIZATION_FOR_EXACT_EXCHANGE_CALENDARS_PIN_ONLY
```

This record is documentation only. It authorizes progression to a later, separately executed local dependency-installation checkpoint under the exact limits below. It does not install dependencies, perform XNYS runtime verification, implement code, or authorize data activity.

## Freshness check results

The required freshness commands were run before this record was created:

```text
git fetch origin
git status --short
git rev-parse HEAD
git rev-parse origin/main
git merge-base --is-ancestor c7e05c2717ca0a53c5aab002b94abfe582ca6103 HEAD
echo $?
```

Observed results:

```text
working_tree = CLEAN
HEAD = c7e05c2717ca0a53c5aab002b94abfe582ca6103
origin_main = c7e05c2717ca0a53c5aab002b94abfe582ca6103
required_completed_commit_is_ancestor = TRUE
merge_base_exit_code = 0
```

## Records reviewed

- `PROJECT_CONTEXT.md`
- `docs/workflows/milestone_review_reference_map.md`
- `docs/runs/v3.08_dataset_reconstruction_dependency_requirements_authorization.md`
- `docs/runs/v3.08_requirements_dependency_change_authorization.md`
- `docs/audits/v3.08_independent_dataset_reconstruction_implementation_plan_review_repeat_after_remediation.md`
- `docs/runs/v3.08_dataset_reconstruction_implementation_plan.md`
- `docs/runs/v3.08_dataset_reconstruction_contract_resolution.md`
- `docs/runs/v3.08_dataset_reconstruction_exchange_calendar_version_selection.md`
- `docs/runs/v3.08_dataset_reconstruction_contract_literal_binding_remediation.md`
- `docs/runs/v3.08_dataset_reconstruction_conditional_technical_literal_verification.md`
- `requirements.txt`

## Governing reconstruction classification

```text
reconstruction_classification = SUPERSEDING_GOVERNED_ALPACA_ALIGNED_RECONSTRUCTION
original_dataset_recovered = FALSE
byte_identical_original_dataset_recovery = NOT_CLAIMED
byte_identical_recovery_claim = PROHIBITED
historical_equivalence_claim = NOT_PROVEN
new_dataset_identity_required = TRUE
```

This authorization does not alter the reconstruction classification or make any original-dataset recovery, byte-identical recovery, historical-equivalence, validation, runtime-behavior, or implementation-readiness claim.

## Installed and runtime status before authorization

```text
exchange_calendars_installed_under_this_governed_workflow = NO
dependency_installation_performed_by_this_checkpoint = NO
XNYS_local_runtime_verification = NOT_PERFORMED
calendar_runtime_verified = NO
calendar_version_status = GOVERNANCE_SELECTED_DEPENDENCY_PIN_PENDING_FUTURE_INSTALL_AND_RUNTIME_VERIFICATION
```

Installation has not been performed under this governed workflow. XNYS local runtime verification has not been performed, and this authorization does not prove calendar behavior.

## Exact dependency installation boundary

`requirements.txt` now contains exactly `exchange-calendars==4.13.2`. The exact pin was added by the completed requirements/dependency file update; this checkpoint does not edit the dependency file.

```text
requirements_txt_contains_exact_pin = YES
exact_requirements_pin = exchange-calendars==4.13.2
direct_dependency_installation_candidate = exchange-calendars==4.13.2
requirements_source_for_future_install = requirements.txt
broad_dependency_upgrades_authorized = NO
unrelated_dependency_changes_authorized = NO
pandas_change_authorized = NO
pyarrow_change_authorized = NO
alpaca_py_change_authorized = NO
python_version_change_authorized = NO
```

The only direct dependency installation candidate is `exchange-calendars==4.13.2`. The future installation checkpoint may only make that exact pinned dependency available locally. It must not authorize broad dependency upgrades, unrelated package changes, or direct changes to pandas, pyarrow, alpaca-py, or Python.

Any transitive dependency effects caused by installing the exact pin must be captured and reviewed during the later installation execution checkpoint. They must not be treated as implicit authority for broad or unrelated dependency drift.

## Reason installation is needed

The final reconstruction contract selects `exchange-calendars==4.13.2` as the governed dependency for the pinned XNYS calendar. The exact pin is now recorded in `requirements.txt`, but the dependency is not yet available under this governed local workflow.

A separate installation execution checkpoint is therefore required before a later, separately authorized XNYS runtime-verification checkpoint can evaluate calendar behavior. Installation alone will not establish that XNYS loading, schedules, holidays, early closes, timestamps, expected slots, or determinism satisfy the governing contract.

## Authorization decision

```text
dependency_installation_authorization_decision = PASS_DEPENDENCY_INSTALLATION_AUTHORIZATION_FOR_EXACT_EXCHANGE_CALENDARS_PIN_ONLY
```

The evidence supports PASS because the exact governed pin is present in `requirements.txt`, the preceding requirements/dependency change was authorized and completed, the working tree is fresh, and the future action can be constrained to exact local installation with explicit fail-closed review of dependency effects.

## Exact future authority granted by this PASS

This PASS authorizes only a later, separately executed checkpoint that may:

- install dependencies only to make `exchange-calendars==4.13.2` available locally;
- use `requirements.txt` as the dependency source;
- record the exact installation command;
- record the Python version;
- record the environment or interpreter path;
- record the installed `exchange-calendars` version;
- record relevant transitive dependency versions after installation;
- capture installation output and dependency-drift evidence;
- fail closed if `exchange-calendars==4.13.2` is not installed exactly; and
- fail closed if installation creates broad or unrelated dependency drift that cannot be narrowly reviewed.

The future execution checkpoint must repeat its own freshness check and report the exact resulting environment state. This PASS does not execute installation and does not authorize a different direct dependency version or dependency source.

## Explicit prohibited actions

This PASS does not authorize:

- dependency installation during this authorization checkpoint;
- XNYS runtime verification;
- editing `requirements.txt` or another dependency file;
- broad or unrelated dependency upgrades;
- source-code or test changes;
- implementation authorization or implementation;
- market-data fetching, downloading, or Alpaca API calls;
- dataset or artifact creation;
- validation-only preflight, validation, training, or model fitting;
- paper or live orders;
- deployment; or
- tagging.

## Non-authorization boundaries

```text
code_implementation = NOT_AUTHORIZED
source_code_changes = NOT_AUTHORIZED
test_changes = NOT_AUTHORIZED
requirements_change = NOT_PERFORMED
dependency_file_edit = NOT_PERFORMED
dependency_installation = NOT_PERFORMED
XNYS_local_runtime_verification = NOT_PERFORMED
data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
dataset_validation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_PROVEN
validation_only_preflight_rerun = NOT_AUTHORIZED
training = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
deployment = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED
```

## Next checkpoints

Because this authorization passes, the next checkpoint is:

```text
next_checkpoint_if_pass = v3.08 Dependency Installation Execution for exchange-calendars==4.13.2
```

That future checkpoint may perform the exact local installation only if separately approved.

If this authorization had failed, the only next checkpoint would have been:

```text
next_checkpoint_if_fail = v3.08 Dependency Installation Authorization Remediation for exchange-calendars==4.13.2
```

## Relationship to later XNYS runtime verification

After installation, XNYS runtime verification must still be separately authorized, executed, and reviewed before implementation authorization can be reconsidered. This record does not combine installation authority with runtime-verification authority.

Implementation authorization remains blocked until exact dependency installation is completed and accepted, the separately governed XNYS runtime verification is completed and accepted, and a later explicit implementation-authorization checkpoint passes.

## Source-of-truth observation

`PROJECT_CONTEXT.md` and `docs/workflows/milestone_review_reference_map.md` still describe the broader combined dependency-installation/XNYS-verification step.

That does not block this narrow installation authorization record. Any later source-of-truth alignment must be separately approved and must preserve the refined sequence:

```text
requirements/dependency file update
dependency installation authorization
dependency installation execution
XNYS runtime verification
independent/runtime verification review
implementation authorization consideration
```

## File and action confirmation

```text
files_created = docs/runs/v3.08_dependency_installation_authorization_exchange_calendars.md
other_files_edited = NO
requirements_changed = NO
dependency_file_edit_performed = NO
dependencies_installed = NO
XNYS_runtime_verification_run = NO
source_code_changed = NO
tests_changed = NO
market_data_accessed = NO
Alpaca_API_called = NO
datasets_created = NO
artifacts_created = NO
validation_run = NO
training_run = NO
orders_submitted = NO
deployment_performed = NO
tag_created = NO
```
