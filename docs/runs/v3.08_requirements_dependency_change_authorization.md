# v3.08 Requirements/Dependency Change Authorization

## Checkpoint metadata

```text
checkpoint = v3.08 Requirements/Dependency Change Authorization
record_scope = DOCUMENTATION_ONLY_REQUIREMENTS_DEPENDENCY_CHANGE_AUTHORIZATION
latest_completed_checkpoint = v3.08 Dataset Reconstruction Dependency/Requirements Authorization
latest_completed_commit = 3a178e56457553251382ce49d9989262ee16ffb3
latest_completed_ci = NOT_INDEPENDENTLY_VERIFIED_IN_THIS_CHECKPOINT
latest_completed_record = docs/runs/v3.08_dataset_reconstruction_dependency_requirements_authorization.md
latest_completed_decision = PASS_DEPENDENCY_REQUIREMENTS_AUTHORIZATION_FOR_SEPARATE_REQUIREMENTS_CHANGE_AND_RUNTIME_VERIFICATION_CHECKPOINT_ONLY

requirements_dependency_change_authorization_decision = PASS_REQUIREMENTS_DEPENDENCY_CHANGE_AUTHORIZATION_FOR_EXACT_EXCHANGE_CALENDARS_PIN_ONLY
```

This record is documentation only. It authorizes progression to a later, separately executed dependency-file update checkpoint under the exact limits below. It does not edit a dependency file, install dependencies, perform runtime verification, implement code, or authorize data activity.

## Freshness check results

The required freshness commands were run before this record was created:

```text
git fetch origin
git status --short
git rev-parse HEAD
git rev-parse origin/main
git merge-base --is-ancestor 3a178e56457553251382ce49d9989262ee16ffb3 HEAD
echo $?
```

Observed results:

```text
working_tree = CLEAN
HEAD = 3a178e56457553251382ce49d9989262ee16ffb3
origin_main = 3a178e56457553251382ce49d9989262ee16ffb3
required_authorization_commit_is_ancestor = TRUE
merge_base_exit_code = 0
```

## Records reviewed

- `PROJECT_CONTEXT.md`
- `docs/workflows/milestone_review_reference_map.md`
- `docs/runs/v3.08_dataset_reconstruction_dependency_requirements_authorization.md`
- `docs/audits/v3.08_independent_dataset_reconstruction_implementation_plan_review_repeat_after_remediation.md`
- `docs/runs/v3.08_dataset_reconstruction_implementation_plan.md`
- `docs/runs/v3.08_dataset_reconstruction_contract_resolution.md`
- `docs/runs/v3.08_dataset_reconstruction_exchange_calendar_version_selection.md`
- `docs/runs/v3.08_dataset_reconstruction_contract_literal_binding_remediation.md`
- `docs/runs/v3.08_dataset_reconstruction_conditional_technical_literal_verification.md`
- `requirements.txt`

## Governing reconstruction classification

```text
reconstruction_classification = SUPERSEDING_GOVERNED_ALPACA_ALIGNED_RECONSTRUCTION
original_dataset_recovered = FALSE
byte_identical_original_dataset_recovery = NOT_CLAIMED
byte_identical_recovery_claim = PROHIBITED
historical_equivalence_claim = NOT_PROVEN
new_dataset_identity_required = TRUE
```

This authorization does not alter the reconstruction classification or make any recovery, equivalence, validation, or readiness claim.

## Current runtime status

```text
exchange_calendars_installed = NO
XNYS_local_runtime_verification = NOT_PERFORMED
calendar_runtime_verified = NO
calendar_version_status = GOVERNANCE_SELECTED_DEPENDENCY_PIN_PENDING_FUTURE_INSTALL_AND_RUNTIME_VERIFICATION
```

No dependency installation or XNYS runtime verification is performed by this checkpoint.

## Dependency-file and exact-pin boundary

Repository inspection identified `requirements.txt` as the only dependency manifest in scope. It currently does not contain `exchange-calendars`.

```text
dependency_file_under_future_edit_consideration = requirements.txt
requirements_txt_currently_contains_dependency = NO
exact_dependency_pin_under_authorization = exchange-calendars==4.13.2
broad_dependency_upgrades_authorized = NO
unrelated_dependency_changes_authorized = NO
other_dependency_files_identified = NONE

pandas_change_authorized = NO
pyarrow_change_authorized = NO
alpaca_py_change_authorized = NO
python_version_change_authorized = NO
```

The only direct dependency candidate is `exchange-calendars==4.13.2`. No broad upgrade, unrelated dependency change, or modification to pandas, pyarrow, alpaca-py, or the Python version is authorized.

## Reason the dependency-file edit is needed

The final reconstruction contract selects exactly `exchange-calendars==4.13.2`, while the current `requirements.txt` does not contain `exchange-calendars`.

Recording the exact pin in the dependency manifest is required before later installation and XNYS runtime verification can proceed reproducibly. This authorization does not perform that edit.

## Authorization decision

```text
requirements_dependency_change_authorization_decision = PASS_REQUIREMENTS_DEPENDENCY_CHANGE_AUTHORIZATION_FOR_EXACT_EXCHANGE_CALENDARS_PIN_ONLY
```

The completed dependency/requirements authorization permits progression toward separately governed dependency work. The exact contract pin is settled, the target dependency file is identified, and the proposed edit can be constrained to one line without authorizing installation or runtime activity.

## Exact future authority granted by this PASS

This PASS authorizes only a later, separately executed checkpoint that may:

- edit `requirements.txt` only to add or confirm exactly `exchange-calendars==4.13.2`;
- avoid reformatting or changing unrelated requirement lines;
- avoid broad or unrelated dependency upgrades;
- display and review the exact dependency-file diff before commit;
- preserve dependency-installation and XNYS runtime-verification boundaries; and
- fail closed if the resulting diff contains anything beyond the authorized exact pin.

The later checkpoint must repeat its own freshness verification. This record does not authorize editing any other dependency file or changing any other dependency entry.

## Execution boundary

```text
requirements_change_performed_by_this_checkpoint = NO
dependency_file_edit_performed_by_this_checkpoint = NO
dependency_installation_authorized = NO
dependency_installation_performed = NO
XNYS_runtime_verification_performed = NO
implementation_authorized = NO
data_activity_authorized = NO
```

## Explicit prohibited actions

This checkpoint does not authorize:

- editing `requirements.txt` or any other dependency file during this checkpoint;
- installing or upgrading dependencies;
- resolving or accepting unrelated transitive dependency changes;
- performing XNYS runtime verification;
- editing source code or tests;
- implementation authorization or implementation;
- market-data access, Alpaca API calls, or dataset activity;
- validation-only preflight, validation, training, or model artifacts;
- paper or live orders;
- deployment or tagging.

## Non-authorization boundaries

```text
code_implementation = NOT_AUTHORIZED
source_code_changes = NOT_AUTHORIZED
test_changes = NOT_AUTHORIZED
dependency_installation = NOT_AUTHORIZED
XNYS_local_runtime_verification = NOT_PERFORMED

data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
dataset_validation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_PROVEN

validation_only_preflight_rerun = NOT_AUTHORIZED
training = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED

paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
deployment = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED

requirements_change = NOT_PERFORMED
dependency_file_edit = NOT_PERFORMED
```

## Next checkpoints

Because this authorization passes, the next checkpoint is:

```text
next_checkpoint_if_pass = v3.08 Requirements/Dependency File Update for exchange-calendars==4.13.2
```

If this authorization had failed, the only next checkpoint would have been:

```text
next_checkpoint_if_fail = v3.08 Requirements/Dependency Change Authorization Remediation
```

This record does not create or execute either later checkpoint.

## Relationship to later runtime verification

The governing sequence remains:

```text
requirements/dependency change authorization
requirements/dependency file update
separately authorized dependency installation
XNYS runtime verification
implementation authorization consideration
```

After the dependency-file update and installation are completed, XNYS runtime verification must still be performed and accepted before implementation authorization can be reconsidered. A separate explicit implementation-authorization checkpoint must then pass before source or test implementation.

## Source-of-truth observation

`PROJECT_CONTEXT.md` and `docs/workflows/milestone_review_reference_map.md` still describe the broader combined dependency installation and XNYS runtime-verification step and have not yet incorporated this more granular authorization/update split.

That does not block this narrow authorization record. Any later source-of-truth alignment must be separately approved and must preserve installation and XNYS runtime verification as prerequisites to implementation authorization.

## File and action confirmation

```text
files_created_by_this_checkpoint = docs/runs/v3.08_requirements_dependency_change_authorization.md
files_edited_elsewhere = NO
requirements_changed = NO
dependency_file_edit_performed = NO
dependencies_installed = NO
XNYS_runtime_verification_run = NO
source_code_changed = NO
tests_changed = NO
market_data_accessed = NO
Alpaca_API_called = NO
datasets_created = NO
artifacts_created = NO
validation_run = NO
training_run = NO
orders_submitted = NO
deployment_performed = NO
tag_created = NO
```
