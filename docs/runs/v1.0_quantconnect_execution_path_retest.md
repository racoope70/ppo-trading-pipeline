# v1.0 QuantConnect Execution-Path Retest

## Purpose

Validate that the v0.8 selected paper-trading models can pass through a QuantConnect-style signal path and produce broker-simulation results consistent with the local paper-trading execution plan.

This is a local compatibility retest. It does not import `AlgorithmImports`, does not run a LEAN backtest, and does not submit orders.

## Inputs

Paper dry-run directory:

```text
reports/paper_trading_dry_runs/latest
```

Manifest:

```text
config/paper_trading_six_ticker_manifest.json
```

## Selected Model Signals

```text
AAPL: ppo_AAPL_window2 | signal=SELL | target_weight=0.0
AMD: ppo_AMD_window2 | signal=HOLD | target_weight=0.0
MRK: ppo_MRK_window1 | signal=SELL | target_weight=0.0
PFE: ppo_PFE_window3 | signal=HOLD | target_weight=0.0
UNH: ppo_UNH_window1 | signal=HOLD | target_weight=0.0
XOM: ppo_XOM_window2 | signal=SELL | target_weight=0.0
```

## Retest Summary

```json
{
  "rows": 6,
  "payload_models": 6,
  "manifest_prefixes_passed": true,
  "prefix_mismatches": [],
  "qc_orders_required": 0,
  "qc_gross_intended_notional": 0.0,
  "comparison_passed": true,
  "comparison_failures": [],
  "retest_passed": true
}
```

## Comparison Result

```text
PASS
```

## Local Outputs

```text
reports/model_selection/v1_0_quantconnect_execution_retest/quantconnect_signal_payload.json
reports/model_selection/v1_0_quantconnect_execution_retest/quantconnect_simulated_execution_plan.csv
reports/model_selection/v1_0_quantconnect_execution_retest/quantconnect_paper_execution_comparison.csv
reports/model_selection/v1_0_quantconnect_execution_retest/quantconnect_execution_retest_summary.json
```

## Interpretation

This validates the execution-path compatibility layer:

```text
v0.8 manifest selections
paper dry-run target outputs
QuantConnect-style signal payload
local broker-simulation comparison
paper execution-plan parity check
```

A passing result means the selected v0.8 candidates can be represented as QuantConnect-style external signals and produce the same local execution intent behavior as the paper-trading execution plan.

## Next Step

If this passes, the next checkpoint can be either:

```text
v1.1 controlled Alpaca paper-order test
```

or a true QuantConnect/LEAN IDE backtest using the generated signal payload.
