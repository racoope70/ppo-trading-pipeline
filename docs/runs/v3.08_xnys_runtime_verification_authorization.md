# v3.08 XNYS Runtime Verification Authorization

## Checkpoint metadata

```text
checkpoint = v3.08 XNYS Runtime Verification Authorization
record_scope = DOCUMENTATION_ONLY_XNYS_RUNTIME_VERIFICATION_AUTHORIZATION
latest_completed_checkpoint = v3.08 Dependency Installation Execution for exchange-calendars==4.13.2
latest_completed_commit = b1c116ecbaefd0213cfb1638bb920a8bf49f6e4a
latest_completed_result = PASS_EXACT_EXCHANGE_CALENDARS_INSTALLATION_COMPLETED
latest_completed_ci = NOT_INDEPENDENTLY_VERIFIED_IN_THIS_CHECKPOINT

xnys_runtime_verification_authorization_decision = PASS_XNYS_RUNTIME_VERIFICATION_AUTHORIZATION_FOR_INSTALLED_EXCHANGE_CALENDARS_PIN_ONLY
```

This record is documentation only. It authorizes progression to a later, separately executed XNYS runtime-verification checkpoint under the exact limits below. It does not perform runtime verification, call `get_calendar("XNYS")`, construct or inspect schedules, implement code, or authorize data activity.

## Freshness check results

The required freshness commands were run before this record was created:

```text
git fetch origin
git status --short
git rev-parse HEAD
git rev-parse origin/main
git merge-base --is-ancestor b1c116ecbaefd0213cfb1638bb920a8bf49f6e4a HEAD
echo $?
```

Observed results:

```text
working_tree = CLEAN
HEAD = b1c116ecbaefd0213cfb1638bb920a8bf49f6e4a
origin_main = b1c116ecbaefd0213cfb1638bb920a8bf49f6e4a
required_completed_commit_is_ancestor = TRUE
merge_base_exit_code = 0
```

## Records reviewed

- `PROJECT_CONTEXT.md`
- `docs/workflows/milestone_review_reference_map.md`
- `docs/runs/v3.08_dataset_reconstruction_dependency_requirements_authorization.md`
- `docs/runs/v3.08_requirements_dependency_change_authorization.md`
- `docs/runs/v3.08_dependency_installation_authorization_exchange_calendars.md`
- `docs/runs/v3.08_dependency_installation_execution_exchange_calendars.md`
- `docs/audits/v3.08_independent_dataset_reconstruction_implementation_plan_review_repeat_after_remediation.md`
- `docs/runs/v3.08_dataset_reconstruction_implementation_plan.md`
- `docs/runs/v3.08_dataset_reconstruction_contract_resolution.md`
- `docs/runs/v3.08_dataset_reconstruction_exchange_calendar_version_selection.md`
- `docs/runs/v3.08_dataset_reconstruction_contract_literal_binding_remediation.md`
- `docs/runs/v3.08_dataset_reconstruction_conditional_technical_literal_verification.md`
- `requirements.txt`

## Governing reconstruction classification

```text
reconstruction_classification = SUPERSEDING_GOVERNED_ALPACA_ALIGNED_RECONSTRUCTION
original_dataset_recovered = FALSE
byte_identical_original_dataset_recovery = NOT_CLAIMED
byte_identical_recovery_claim = PROHIBITED
historical_equivalence_claim = NOT_PROVEN
new_dataset_identity_required = TRUE
```

This authorization does not alter the reconstruction classification or make any original-dataset recovery, byte-identical recovery, historical-equivalence, validation, runtime-verification, or implementation-readiness claim.

## Installed dependency and runtime status

The committed installation execution record confirms that the exact dependency is available in the existing project virtual environment and that its dependency consistency check passed:

```text
exchange_calendars_installed_version = 4.13.2
dependency_installation_execution_result = PASS_EXACT_EXCHANGE_CALENDARS_INSTALLATION_COMPLETED
prior_installation_pip_check_result = PASS
XNYS_local_runtime_verification = NOT_PERFORMED
calendar_runtime_verified = NO
```

Installation proves package availability only. It does not prove XNYS calendar availability, schedule coverage, timestamp behavior, holiday handling, early-close handling, governed slot counts, or determinism. XNYS runtime verification has not occurred, and this authorization checkpoint does not perform it.

## Governing runtime-verification boundary

The final reconstruction contract requires a separately governed runtime verification of the installed `exchange-calendars==4.13.2` package before implementation authorization can be reconsidered.

The future execution checkpoint may verify only:

1. The installed `exchange-calendars` version is exactly `4.13.2`.
2. `get_calendar("XNYS")` succeeds.
3. The governed reconstruction date range is covered by the XNYS calendar.
4. Session opens and closes are timezone-aware UTC values.
5. Representative holidays and other non-sessions are excluded correctly.
6. Representative early-close sessions match the governed expectations.
7. Whole-interval containment produces the governed full-session and early-close hourly slot counts.
8. Repeated construction of the same schedule is deterministic.
9. Relevant direct and transitive dependency versions are recorded.

Schedule construction must be limited to the governed verification range and representative checks. This authorization does not permit source implementation, tests, market-data access, dataset activity, application validation, or training.

## Authorization decision

```text
xnys_runtime_verification_authorization_decision = PASS_XNYS_RUNTIME_VERIFICATION_AUTHORIZATION_FOR_INSTALLED_EXCHANGE_CALENDARS_PIN_ONLY
```

The evidence supports PASS because the exact governed dependency version is installed, the prior installation execution passed, `pip check` passed according to the committed evidence, runtime verification remains explicitly unperformed, and the future checks can be constrained to the final contract's calendar requirements.

## Exact future authority granted by this PASS

This PASS authorizes only a later, separately executed checkpoint that may:

- use the existing project virtual environment;
- import `exchange_calendars` only as required for XNYS runtime verification;
- verify that the installed `exchange-calendars` version is exactly `4.13.2`;
- call `get_calendar("XNYS")`;
- construct XNYS schedules only for the governed range and representative checks;
- verify calendar availability;
- verify governed reconstruction-range coverage;
- verify UTC-aware session opens and closes;
- verify representative holidays and non-sessions;
- verify representative early closes;
- verify governed whole-interval full-session and early-close hourly slot counts;
- verify deterministic schedule construction;
- record relevant direct and transitive dependency versions;
- capture exact commands, outputs, assertions, expected values, and failures; and
- fail closed if any required runtime behavior is not verified.

The future execution checkpoint must repeat its own freshness verification. This record does not execute that checkpoint and does not authorize dependency or environment changes if a prerequisite is missing.

## Required future execution evidence

The later execution record must identify the Python interpreter and environment, installed package versions, governed verification range, representative dates, expected outcomes, exact commands, raw outputs, and assertion results sufficient for independent review.

At minimum, it must record:

```text
python_version
python_executable
exchange_calendars_installed_version
relevant_transitive_dependency_versions
XNYS_calendar_load_result
governed_range_schedule_coverage_result
UTC_aware_opens_result
UTC_aware_closes_result
representative_holiday_and_non_session_result
representative_early_close_result
full_session_whole_interval_slot_count_result
early_close_whole_interval_slot_count_result
schedule_determinism_result
overall_runtime_verification_result
```

## Required fail-closed conditions

The later execution checkpoint must fail if:

- the installed `exchange-calendars` version is not exactly `4.13.2`;
- `get_calendar("XNYS")` fails;
- the governed reconstruction range is not covered;
- opens or closes are not timezone-aware UTC;
- representative holidays or non-sessions are handled incorrectly;
- representative early-close sessions differ from the governed expectation;
- whole-interval containment produces incorrect slot counts;
- repeated schedule construction is not deterministic;
- required dependency or environment evidence cannot be recorded; or
- any required assertion is ambiguous or unproven.

A failure must not trigger dependency installation, package changes, source or test edits, data activity, or other remediation within the runtime-verification execution checkpoint. It must be reported and routed to a separately governed remediation decision.

## Explicit prohibited actions

This PASS does not authorize:

- source-code changes;
- test changes;
- dependency installation, removal, or upgrades;
- requirements or dependency-file changes;
- market-data fetching or downloading;
- Alpaca API calls;
- dataset or artifact creation;
- validation-only preflight or dataset validation;
- training or model fitting;
- paper or live orders;
- deployment; or
- tagging.

## Non-authorization boundaries

```text
code_implementation = NOT_AUTHORIZED
source_code_changes = NOT_AUTHORIZED
test_changes = NOT_AUTHORIZED
requirements_change = NOT_PERFORMED
dependency_file_edit = NOT_PERFORMED
dependency_installation = NOT_PERFORMED
data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
dataset_validation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_PROVEN
validation_only_preflight_rerun = NOT_AUTHORIZED
training = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
deployment = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED

XNYS_local_runtime_verification = NOT_PERFORMED
calendar_runtime_verified = NO
XNYS_runtime_verification_run = NO
XNYS_get_calendar_called = NO
XNYS_schedule_constructed = NO
```

## Next checkpoints

Because this authorization passes, the next checkpoint is:

```text
next_checkpoint_if_pass = v3.08 XNYS Runtime Verification Execution
```

That future checkpoint may perform the exact authorized runtime checks only if separately approved.

If this authorization had failed, the only next checkpoint would have been:

```text
next_checkpoint_if_fail = v3.08 XNYS Runtime Verification Authorization Remediation
```

## Relationship to independent/runtime verification review

Runtime-verification execution will produce evidence but will not independently accept its own conclusions. After execution, a separate independent/runtime verification review must assess the environment identity, installed versions, calendar availability, governed-range coverage, UTC awareness, representative holidays and non-sessions, early closes, whole-interval slot counts, determinism, and preservation of prohibited-action boundaries.

## Relationship to implementation authorization

Implementation authorization remains blocked until all of the following occur:

1. XNYS runtime-verification execution is completed.
2. The independent/runtime verification review passes.
3. A later explicit implementation-authorization checkpoint passes.

Even a successful runtime-verification execution does not authorize source or test implementation.

## Source-of-truth observation

`PROJECT_CONTEXT.md` and `docs/workflows/milestone_review_reference_map.md` may still describe the broader combined dependency-installation/XNYS-verification step and retain older status language.

That does not block this narrow XNYS runtime-verification authorization record. Any later source-of-truth alignment must be separately approved and must preserve the refined sequence:

```text
dependency installation execution
XNYS runtime verification authorization
XNYS runtime verification execution
independent/runtime verification review
implementation authorization consideration
```

## File and action confirmation

```text
files_created = docs/runs/v3.08_xnys_runtime_verification_authorization.md
other_files_edited = NO
requirements_changed = NO
dependency_file_edit_performed = NO
dependencies_installed = NO
XNYS_runtime_verification_run = NO
XNYS_get_calendar_called = NO
XNYS_schedule_constructed = NO
source_code_changed = NO
tests_changed = NO
market_data_accessed = NO
Alpaca_API_called = NO
datasets_created = NO
artifacts_created = NO
validation_run = NO
training_run = NO
orders_submitted = NO
deployment_performed = NO
tag_created = NO
```
