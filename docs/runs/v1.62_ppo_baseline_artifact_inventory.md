# v1.62 PPO Baseline Artifact Inventory

Date: 2026-06-17
Status: Completed
Mode: Legacy PPO baseline artifact inventory

## Purpose

Inventory the legacy PPO baseline artifacts after v1.61 defined the model-quality audit scope.

This checkpoint prepares evidence for the v1.63 model-quality audit report.

This checkpoint does not make a final model-quality pass/fail decision.

## Starting State

```text
starting_sealed_checkpoint = v1.61-ppo-baseline-model-quality-audit-scope
starting_commit = eeddf96 Define v1.61 PPO audit scope
tests = 227 passed, 2 warnings
```

## Background

v1.60 reclassified the PPO model as a legacy baseline / infrastructure validation fixture.

v1.61 defined the model-quality audit scope.

v1.62 inventories the actual artifact coverage and available metadata.

Final model-quality interpretation is deferred to v1.63.

## Current Classification

```text
PPO model = legacy baseline / infrastructure validation fixture
PPO trading edge = unproven
controlled paper submit = blocked
paper order submission = not authorized
live orders = not authorized
PPO + Random Forest deployment = blocked
PPO + XGBoost deployment = blocked
retraining = not yet
NO-SUBMIT = default
```

## Inventory Files

Reference:

```text
docs/audits/v1.62_ppo_baseline_artifact_inventory.csv
docs/audits/v1.62_ppo_baseline_artifact_inventory_summary.md
```

## Inventory Fields

```text
symbol
artifact_root
selected_artifact_prefix
model_path_exists
vecnormalize_path_exists
features_path_exists
probability_config_path_exists
model_info_path_exists
selected_window
final_portfolio
buy_hold
ppo_minus_buy_hold
ppo_vs_buy_hold_pct
sharpe
drawdown_if_available
source_repo_or_artifact_source
notes
```

## Inventory Summary

Generated inventory summary:

```text
manifest_path = config/paper_trading_six_ticker_manifest.json
symbols = AAPL, AMD, MRK, PFE, UNH, XOM
artifact_dirs_found = models/alpaca_ppo_models_master, trained_models
inventory_rows = 120
complete_artifact_rows = 18
incomplete_or_missing_rows = 102
```

Symbol coverage:

```text
AAPL rows = 20
AAPL complete_artifact_sets = 3

AMD rows = 20
AMD complete_artifact_sets = 3

MRK rows = 20
MRK complete_artifact_sets = 3

PFE rows = 20
PFE complete_artifact_sets = 3

UNH rows = 20
UNH complete_artifact_sets = 3

XOM rows = 20
XOM complete_artifact_sets = 3
```

## Inventory Interpretation

The generated inventory found 120 artifact rows across six symbols.

Each symbol has 20 inventoried rows and 3 complete artifact sets.

Across the full inventory, 18 rows are complete artifact sets and 102 rows are incomplete or missing.

The generated summary states that artifact completeness supports infrastructure validation only.

Artifact completeness does not prove trading edge.

Final model-quality interpretation is deferred to v1.63.

Controlled submit remains blocked.

PPO + RF and PPO + XGBoost remain blocked.

Retraining is not authorized by this inventory.

Evidence missing for v1.63:

```text
complete artifact coverage for 102 incomplete_or_missing_rows
model_info metadata for incomplete rows where model_info_path_exists = false
benchmark fields for rows where final_portfolio, buy_hold, ppo_minus_buy_hold, ppo_vs_buy_hold_pct, sharpe, or drawdown_if_available are blank
```

## What v1.62 Can Prove

```text
Artifact files exist or do not exist.
Artifact sets are complete or incomplete.
model_info metadata is available or missing.
Benchmark fields are available or missing.
The legacy PPO baseline can be prepared for v1.63 audit.
```

## What v1.62 Cannot Prove

```text
It cannot prove trading edge.
It cannot approve controlled submit.
It cannot approve paper order submission.
It cannot approve live trading.
It cannot approve PPO + RF deployment.
It cannot approve PPO + XGBoost deployment.
It cannot justify retraining by itself.
It cannot use feature importance as trading-edge evidence.
```

## v1.62 Decision

```text
Artifact inventory completed.
Proceed to v1.63 PPO Baseline Model Quality Audit Report.
No retraining authorized.
No paper orders authorized.
No live orders authorized.
Controlled submit remains blocked.
PPO + RF remains blocked.
PPO + XGBoost remains blocked.
```

## Next Milestone

```text
v1.63 PPO Baseline Model Quality Audit Report
```

## Current Operating Posture

```text
NO-SUBMIT unless a separate controlled-submit checkpoint explicitly authorizes otherwise.
```
