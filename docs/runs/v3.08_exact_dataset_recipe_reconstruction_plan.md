# v3.08 Exact Dataset Recipe Reconstruction Plan

## Checkpoint Decision

```text
checkpoint = v3.08 Exact Dataset Recipe Reconstruction Plan
decision = PASS_RECIPE_RECONSTRUCTION_PLAN_ONLY
scope = DOCUMENTATION_ONLY_RECIPE_DESIGN
dataset_generation = NOT_AUTHORIZED
dataset_fetching = NOT_AUTHORIZED
dataset_downloading = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_AUTHORIZED
validation_only_preflight_rerun = NOT_AUTHORIZED
training = NOT_AUTHORIZED
```

## Decision Source

```text
v3.08_exact_dataset_decision_commit = 55573dc
v3.08_exact_dataset_decision_ci = Tests #379 green
recommended_next_branch = BRANCH_B_EXACT_RECIPE_RECONSTRUCTION_PLAN_UNLESS_USER_PROVIDES_EXACT_TRUSTED_ARTIFACT_LOCATION
```

## Scope Clarification

This checkpoint is not a dataset build.

This checkpoint is not a restore.

This checkpoint is not a validation.

This checkpoint is not a training authorization.

This checkpoint only defines the exact recipe requirements that must be satisfied before any future dataset reconstruction can be authorized.

## Known Dataset State

```text
missing_dataset_path = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet

PATH_MISMATCH = NOT_SUPPORTED
DATASET_INTENTIONALLY_GITIGNORED = SUPPORTED
PACKAGE_PREPARATION_INCOMPLETE = SUPPORTED_AT_EXECUTION_READINESS_BOUNDARY
EXTERNAL_ARTIFACT_REQUIRED = STRONGLY_IMPLIED_OR_PLAUSIBLE
REGENERATE_FROM_TRACKED_RECIPE = NOT_YET_SUPPORTED
DATASET_CREATED_BUT_UNTRACKED = PLAUSIBLE_BUT_NOT_PROVEN
DATASET_NEVER_CREATED = UNDETERMINED
EXACT_DATASET_ORIGIN_AND_CREATION_STATUS = UNDETERMINED
```

## Recovered Dataset Contract

```text
provider = Alpaca historical bars
bar_interval = 1h
hours_policy = regular session only
timezone = UTC
expected_output_path = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet
```

## Ticker Universe

```text
ticker_universe =
- AAPL
- AMD
- MRK
- PFE
- UNH
- XOM
```

## Minimum Expected Schema Metadata

```text
minimum_expected_schema_metadata =
- Symbol
- Datetime
```

## Configured Feature Columns

```text
configured_feature_columns =
- Open
- High
- Low
- Close
- Volume
- return_1h
- log_return_1h
- sma_10
- sma_20
- sma_50
- ema_12
- ema_26
- rsi_14
- macd
- macd_signal
- macd_hist
- atr_14
- realized_volatility_20
- volume_zscore_20
- close_to_sma20
- close_to_sma50
- hour_sin
- hour_cos
- day_of_week_sin
- day_of_week_cos
```

## Temporal Contract

```text
temporal_contract =
train: 2023-01-03T14:30:00Z to 2024-06-14T20:00:00Z
train_eval_embargo: 2024-06-17T13:30:00Z to 2024-06-21T20:00:00Z
evaluation: 2024-06-24T13:30:00Z to 2024-12-13T21:00:00Z
eval_holdout_embargo: 2024-12-16T14:30:00Z to 2024-12-20T21:00:00Z
holdout: 2024-12-23T14:30:00Z to 2025-06-30T20:00:00Z
```

## Required Future Recipe Section 1: Raw Data Source Specification

A later documentation/design checkpoint must define the exact raw source specification before any dataset generation or reconstruction authorization.

Required fields:

```text
provider = Alpaca
account/source mode
exact bar timeframe = 1h
exact API endpoint or local raw source, if applicable
exact ticker universe
exact UTC start/end retrieval windows
regular-session filtering rule
timezone normalization rule
split-adjustment/dividend-adjustment policy, if applicable
missing raw bar handling policy
duplicate raw bar handling policy
source snapshot or retrieval timestamp evidence requirement
```

This checkpoint does not authorize Alpaca API calls, data fetching, data downloading, raw-data inspection, external artifact inspection, or dataset creation.

## Required Future Recipe Section 2: Feature Engineering Specification

A later documentation/design checkpoint must define exact formulas for every configured feature column.

Required formulas:

```text
return_1h
log_return_1h
sma_10
sma_20
sma_50
ema_12
ema_26
rsi_14
macd
macd_signal
macd_hist
atr_14
realized_volatility_20
volume_zscore_20
close_to_sma20
close_to_sma50
hour_sin
hour_cos
day_of_week_sin
day_of_week_cos
```

For each formula, the future recipe must define:

```text
input columns
rolling window
min_periods
smoothing method
standard deviation convention/ddof
NaN/warm-up behavior
infinity handling
per-symbol grouping
timezone/order of operations
deterministic sorting
data type expectations
```

The future recipe must bind each formula to a source-controlled implementation before execution authorization.

## Required Future Recipe Section 3: Temporal Split and Embargo Specification

A later documentation/design checkpoint must define the exact temporal split and embargo behavior.

Required fields:

```text
train window
train/eval embargo window
evaluation window
eval/holdout embargo window
holdout window
inclusive/exclusive boundary convention
how rows inside embargo periods are excluded
final-only holdout rule
no holdout use for fitting, normalization, tuning, feature selection, early stopping, model selection, or manual iteration
```

The future recipe must make the row assignment rule deterministic and auditable.

## Required Future Recipe Section 4: Preprocessing and Leakage-Control Specification

A later documentation/design checkpoint must define the preprocessing and leakage-control rules.

Required rules:

```text
Symbol and Datetime as metadata only
forbidden feature-input columns:
- Target
- Return
- Datetime
- Symbol
train-only preprocessing fit
no holdout preprocessing fit
scaler/normalizer artifact rules, if any
feature selection restrictions
leakage-column rejection
deterministic row ordering
```

No future dataset reconstruction should be authorized unless the leakage-control plan is explicit and testable.

## Required Future Recipe Section 5: Expected Output and Evidence Specification

A later documentation/design checkpoint must define the exact expected outputs and evidence required from any future authorized reconstruction.

Required fields:

```text
output path
parquet engine
schema/order of columns
expected row-count methodology
per-symbol row-count methodology
split row-count methodology
checksum/SHA-256 requirement
size_bytes requirement
validation_inventory requirement
dataset manifest requirement
data contract report requirement
command log requirement
runtime/dependency/git-state capture
no-training and no-order evidence
```

The future evidence package must be sufficient to support later sealed dataset validation, but this checkpoint does not authorize that validation.

## Required Future Recipe Section 6: Governance and Authorization Requirements

Before any actual dataset generation or reconstruction, a separate authorization checkpoint must define:

```text
exact command
exact source inputs
exact output path
expected evidence files
allowed artifacts
forbidden actions
rollback/cleanup policy
execution-head freshness guardrail
command-contract guardrail
no-training boundary
no-order boundary
```

Any future execution checkpoint must remain fail-closed if command, path, source input, evidence output, or run identity diverges from the approved contract.

## Unresolved Recipe Gaps

The following gaps remain unresolved:

```text
no positive existing dataset checksum
no expected file size
no expected total row count
no per-symbol expected row counts
no exact raw Alpaca source snapshot
no exact generation command
no exact feature formula implementation binding
no proof original v3.07 dataset ever existed
no byte-identical regeneration guarantee
no trusted external artifact currently identified
```

Because byte-identical restoration is not currently provable, future reconstruction should be treated as a new governed reconstruction of the v3.07 contract unless a trusted artifact is later found.

## Explicit Non-Authorization

```text
dataset_generation = NOT_AUTHORIZED
dataset_fetching = NOT_AUTHORIZED
dataset_downloading = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
sealed_dataset_restore = NOT_AUTHORIZED
sealed_dataset_copy = NOT_AUTHORIZED
sealed_dataset_repair = NOT_AUTHORIZED
sealed_dataset_regeneration = NOT_AUTHORIZED
sealed_dataset_mutation_or_rewrite = NOT_AUTHORIZED
sealed_dataset_validation = NOT_AUTHORIZED
sealed_dataset_validation_execution = NOT_AUTHORIZED
validation_only_preflight_rerun = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED
```

## Preserved Authorization State

```text
preflight_readiness = NOT_PASSED
sealed_dataset_validation = NOT_PROVEN

sealed_dataset_recovery = NOT_AUTHORIZED
sealed_dataset_restore = NOT_AUTHORIZED
sealed_dataset_copy = NOT_AUTHORIZED
sealed_dataset_repair = NOT_AUTHORIZED
sealed_dataset_regeneration = NOT_AUTHORIZED
sealed_dataset_mutation_or_rewrite = NOT_AUTHORIZED
sealed_dataset_validation_execution = NOT_AUTHORIZED

validation_only_preflight_rerun = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED

model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED
```

## Command-Contract Guardrail

Verify that any command/run_id/path/config/output/evidence path in the checkpoint matches source code, config, command manifest, artifact manifest, or source-of-truth record.

Do not accept manually invented values.

If run_id is involved, confirm:

```text
authorized_command_run_id == source_required_run_id
```

No run_id execution is authorized in this checkpoint.

## Execution-Head Freshness Guardrail

Before any authorized execution or commit workflow:

```bash
git fetch origin
git status --short
git rev-parse HEAD
git rev-parse origin/main
git merge-base --is-ancestor <latest_required_authorization_or_review_commit> HEAD
```

Commit workflow may proceed only if:

```text
execution_head_includes_required_review_commit = TRUE
```

For this checkpoint, the required decision commit is:

```text
latest_required_authorization_or_review_commit = 55573dc
```

## Boundary Confirmation

```text
external_artifact_inspection = NOT_RUN
data_fetching = NOT_RUN
data_downloading = NOT_RUN
dataset_creation = NOT_RUN
dataset_copy = NOT_RUN
dataset_restore = NOT_RUN
dataset_repair = NOT_RUN
dataset_regeneration = NOT_RUN
dataset_mutation_or_rewrite = NOT_RUN
sealed_dataset_validation_execution = NOT_RUN
validation_only_preflight_rerun = NOT_RUN
ppo_v2_training_execution = NOT_RUN
sealed_training_command_execution = NOT_RUN
model_learn = NOT_CALLED
model_fitting = NOT_RUN
model_artifacts = NOT_CREATED
quarantine_outputs = NOT_CREATED
paper_orders = NOT_SUBMITTED
live_orders = NOT_SUBMITTED
tagging = NOT_RUN
```

## Recommended Next Checkpoint

```text
next_checkpoint = v3.08 Exact Feature Formula and Raw Source Specification
```

Purpose of next checkpoint:

```text
Define exact feature formulas and raw Alpaca source boundaries in a documentation-only specification before any generation authorization.
```
