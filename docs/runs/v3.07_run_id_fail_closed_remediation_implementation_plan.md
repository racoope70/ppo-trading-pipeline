# v3.07 Run-ID / Fail-Closed Remediation Implementation Plan

## Checkpoint Decision

```text
checkpoint = v3.07 Run-ID / Fail-Closed Remediation Implementation Plan
decision = PASS_RUN_ID_FAIL_CLOSED_REMEDIATION_IMPLEMENTATION_PLAN
latest_completed_checkpoint = v3.07 Run-ID / Fail-Closed Remediation Authorization Review
latest_completed_decision = PASS_RUN_ID_FAIL_CLOSED_REMEDIATION_AUTHORIZATION_REVIEW
latest_completed_commit = 46f1776
latest_completed_ci = Tests #365 green
remediation_scope = CLI_RUN_ID_HANDLING_AND_FAIL_CLOSED_EVIDENCE_BEHAVIOR_ONLY
```

## Diagnosed Root Cause Preserved

```text
root_cause = run-id mismatch
attempted_run_id = v3_07_validation_only_preflight_rerun_001
required_run_id = v3_07_validation_only_preflight_r1_r6_001
source_constant = V3_07_VALIDATION_RUN_ID
source_constant_value = v3_07_validation_only_preflight_r1_r6_001
command_exit_code = 2
evidence_writer_not_reached = TRUE
R1_R6_evidence_files = NOT_EMITTED
missing_dataset_check_not_reached_due_to_request_validation_failure = TRUE
```

## Purpose

This checkpoint creates a documentation-only implementation plan for the narrow run-id / fail-closed remediation.

This checkpoint does not implement the remediation.

This checkpoint does not edit source code, tests, artifacts, datasets, model files, or prior records.

## Proposed Later File Scope

The later remediation checkpoint may propose or perform changes only if separately approved, and only within this narrow scope:

```text
src/ppo_v2_validation_only_preflight_execution.py
tests/test_ppo_v2_validation_only_preflight_execution.py
docs/runs/v3.07_run_id_fail_closed_remediation_implementation_record.md
```

No other files are authorized by this implementation-plan checkpoint.

## Guardrail Principle

```text
Never authorize a command with a manually invented run_id.
First inspect the source/config/command manifest, then copy or derive the exact required value.
```

Future validation-only preflight command authorization must use the source-required run id unless a later approved remediation safely changes how run ids are derived, validated, or recorded.

## Proposed Run-ID Remediation Plan

The later remediation checkpoint should consider:

```text
1. Aligning any validation-only preflight command/template/manifest with the source-required run id.
2. Ensuring authorized_command_run_id is copied or derived from source/config/manifest instead of invented manually.
3. Making the required run id discoverable before command authorization.
4. Clarifying whether future reruns should use the existing source-required run id or safely support an explicit rerun id.
5. Preventing recurrence of attempted_run_id = v3_07_validation_only_preflight_rerun_001 when the source requires v3_07_validation_only_preflight_r1_r6_001.
```

## Proposed Static Guardrail Test

The later remediation checkpoint should add or improve a static guardrail test confirming:

```text
authorized_command_run_id == source_required_run_id
authorized_command_run_id must match V3_07_VALIDATION_RUN_ID
V3_07_VALIDATION_RUN_ID = v3_07_validation_only_preflight_r1_r6_001
```

The test should compare any authorized command/template/manifest used for validation-only preflight against the source-required constant.

The test must not rerun validation-only preflight, train models, fetch data, generate datasets, create artifacts, or submit orders.

## Proposed Fail-Closed Evidence Behavior Remediation

The later remediation checkpoint should consider improving invalid run_id / invalid request handling so it emits a clear fail-closed rejection record or evidence-compatible reject output instead of exiting before any useful evidence file is written.

The rejection output should clearly identify:

```text
request_validation_failure = TRUE
invalid_run_id = TRUE when applicable
attempted_run_id = <provided value>
required_run_id = v3_07_validation_only_preflight_r1_r6_001
R1_R6_preflight_executed = FALSE
training_performed = FALSE
sealed_training_command_executed = FALSE
```

The reject output must preserve that it is not a full R1-R6 preflight result unless R1-R6 actually run.

The remediation must not convert a rejected request into preflight readiness.

## Proposed Safe Later Tests

The later remediation checkpoint should propose safe tests only, such as:

```text
invalid run_id produces fail-closed rejection/evidence-compatible output
required run id is discoverable from source constant
command/template run id matches source-required run id
no-submit boundary remains enforced
training boundary remains enforced
data boundary remains enforced
artifact boundary remains enforced
order boundary remains enforced
targeted pytest for the relevant test file
full pytest only after targeted tests pass
```

Targeted pytest should run only after the later implementation checkpoint is separately approved.

Full pytest should run only after targeted tests pass and only within the separately approved implementation checkpoint.

## Documentation Plan

The later implementation record should document:

```text
root cause = run-id mismatch
attempted run id vs required run id
why evidence files were not emitted
how the remediation prevents recurrence
why no additional rerun is authorized
why no PPO v2 training or sealed training command execution is authorized
why preflight_readiness remains NOT_PASSED
why sealed_dataset_validation remains NOT_PROVEN
```

## Explicit Non-Authorization

This checkpoint does not authorize:

```text
validation-only preflight rerun
additional validation-only preflight rerun
PPO v2 training
sealed training command execution
training command execution
model.learn
model fitting
data fetching
data downloading
dataset generation
dataset backfill
dataset mutation or rewrite
sealed dataset validation
model artifact creation
quarantine model output creation
paper orders
live orders
controlled submit
PPO + Random Forest
PPO + XGBoost
model promotion
production deployment
deployment-readiness claims
trading-edge claims
profitability claims
```

## Preserved Hard Boundaries

```text
validation_only_preflight_rerun = NOT_AUTHORIZED
additional_validation_only_preflight_rerun = NOT_AUTHORIZED
preflight_readiness = NOT_PASSED
sealed_dataset_validation = NOT_PROVEN
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
training_command_execution = NOT_AUTHORIZED
model_learn = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED
data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
dataset_backfill = NOT_AUTHORIZED
dataset_mutation_or_rewrite = NOT_AUTHORIZED
sealed_dataset_validation_execution = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
quarantine_model_output_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
controlled_submit = BLOCKED
ppo_rf = BLOCKED
ppo_xgboost = BLOCKED
model_promotion = NOT_AUTHORIZED
production_deployment = NOT_AUTHORIZED
deployment_readiness_claims = NOT_AUTHORIZED
trading_edge_claims = NOT_AUTHORIZED
profitability_claims = NOT_AUTHORIZED
```

## Final Conclusion

```text
decision = PASS_RUN_ID_FAIL_CLOSED_REMEDIATION_IMPLEMENTATION_PLAN
implementation_performed = FALSE
source_code_edited = FALSE
tests_edited = FALSE
validation_only_preflight_rerun = NOT_AUTHORIZED
additional_validation_only_preflight_rerun = NOT_AUTHORIZED
preflight_readiness = NOT_PASSED
sealed_dataset_validation = NOT_PROVEN
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
```
