# v1.21 Controlled Submit Using Full v1.18 + v1.19 Safety Stack

Date: 2026-06-05
Status: Completed
Mode: Controlled single-order Alpaca paper submit
Environment: Local VS Code virtual environment
Broker: Alpaca Paper Trading

## Purpose

Run one controlled Alpaca paper-order submit using the full safety stack introduced in:

```text
v1.18 Stale-Plan Prevention / Max-Plan-Age Enforcement
v1.19 Submit-Mode Preflight / Explicit Run-Dir Confirmation
v1.20 Submit-Mode Preflight Validation / No-Submit Regression Session
```

The objective was to confirm that a real paper order can be submitted only after:

```text
fresh dry run
execution-plan review
single-order filter
risk-control pass
pre-submit checklist pass
max-plan-age enforcement
explicit run-dir confirmation
manual approval
broker verification after submit
```

## Starting State

```text
git_tag = v1.20-submit-mode-preflight-no-submit-regression
tests = 210 passed, 2 warnings
```

## Fresh No-Submit Decision Chain

The fresh dry run used:

```text
manifest = config/paper_trading_six_ticker_manifest.json
artifacts_dir = models/alpaca_ppo_models_master
```

Dry-run result:

```text
datetime_utc = 2026-06-05T17:10:10.317995+00:00
predict_ok_count = 6
error_count = 0
orders_submitted = 0
Evaluation result = PASS
```

## Initial Execution Plan

The fresh execution plan produced three proposed buy orders:

```text
orders_required = 3
gross_intended_notional = 888.135258517782
buy_count = 3
sell_count = 0
```

Proposed orders:

```text
AAPL buy 0.823888   delta_notional = 255.35158560532082
AMD  buy 1.054577   delta_notional = 498.899129541157
UNH  buy 0.333295   delta_notional = 133.88454337130423
```

Because `orders_required = 3`, the system was not submitted from:

```text
reports/paper_trading_dry_runs/latest
```

## Single-Order Filter

The v1.13 single-order filter was applied to select only the smallest proposed order:

```text
selected_symbol = UNH
selected_side = buy
selected_qty = 0.333295
selected_delta_notional = 133.88454337130423
original_orders_required = 3
disabled_order_count = 2
```

Filtered run directory:

```text
reports/paper_trading_dry_runs/v1_21_single_order_UNH_buy
```

Disabled orders:

```text
AAPL buy 0.823888
AMD buy 1.054577
```

Filtered execution plan:

```text
orders_required = 1
orders_submitted = 0
gross_intended_notional = 133.88454337130423
buy_count = 1
sell_count = 0
```

## Filtered Risk Controls

Risk controls were run against the filtered directory with:

```text
--max-plan-age-minutes 90
```

Risk result:

```text
Risk result = PASS
order_rows = 1
total_order_notional = 133.88454337130423
single_order_notional_pct = 0.0013339923694729
plan_not_stale = PASS
age_minutes = 18.02
limit = 90.0
summary_orders_submitted = 0
```

## Filtered No-Submit Paper Loop

The guarded paper-order runner was first run in no-submit mode:

```text
run_dir = reports/paper_trading_dry_runs/v1_21_single_order_UNH_buy
submit_orders = False
orders_required = 1
orders_submitted = 0
risk_passed = True
```

No orders were submitted in this validation step.

## Filtered Pre-Submit Checklist

The pre-submit checklist was run with broker checks and max-plan-age enforcement:

```text
--check-broker
--expected-equity 100000
--equity-tolerance-pct 0.05
--allow-open-positions
--max-plan-age-minutes 90
```

Checklist result before submit:

```text
Checklist result = PASS
execution_plan_review_required = orders_required=1
execution_plan_not_stale = PASS
dry_run_error_count_zero = PASS
paper_order_summary_orders_submitted_zero = PASS
paper_order_summary_no_order_mode = PASS
paper_order_risk_passed = PASS
audit_log_risk_passed = PASS
audit_log_orders_submitted_zero = PASS
audit_log_no_order_mode = PASS
broker_open_orders_zero = PASS
broker_snapshot_errors_empty = PASS
```

## Controlled Submit

The controlled submit used the full v1.18 + v1.19 safety stack:

```bash
python -m src.paper_trading.paper_trade_loop \
  --run-dir reports/paper_trading_dry_runs/v1_21_single_order_UNH_buy \
  --submit-orders \
  --max-plan-age-minutes 90 \
  --confirm-run-dir reports/paper_trading_dry_runs/v1_21_single_order_UNH_buy
```

Submitted order:

```text
symbol = UNH
side = buy
qty = 0.333295
order_submitted = True
order_id = f266bae6-e003-44ea-9cf5-be463c818601
risk_passed = True
execution_note = order_submitted
```

Paper-order summary after submit:

```text
rows = 6
orders_required = 1
orders_submitted = 1
submit_orders = True
risk_passed = True
```

## Broker Verification After Submit

Broker state after submit:

```text
status = AccountStatus.ACTIVE
equity = 100363.84
cash = 100229.39
buying_power = 401186.46
```

Positions:

```text
AMD qty = 0.00092
AMD market_value = 0.436098
AMD avg_entry = 512.026
AMD unrealized_pl = -0.034966
AMD unrealized_plpc = -0.07423

UNH qty = 0.333295
UNH market_value = 134.014587
UNH avg_entry = 401.988
UNH unrealized_pl = 0.033997
UNH unrealized_plpc = 0.00025
```

Open orders:

```text
none
```

## Post-Submit Checklist Interpretation

The post-submit checklist reported FAIL, but only because no-submit checks are expected to fail after an intentional submit.

Expected post-submit failures:

```text
paper_order_summary_orders_submitted_zero = FAIL
paper_order_summary_no_order_mode = FAIL
audit_log_orders_submitted_zero = FAIL
audit_log_no_order_mode = FAIL
```

Important checks passed:

```text
execution_plan_not_stale = PASS
dry_run_error_count_zero = PASS
paper_order_risk_passed = PASS
audit_log_risk_passed = PASS
broker_equity_near_expected = PASS
broker_cash_available = PASS
broker_open_orders_zero = PASS
broker_snapshot_errors_empty = PASS
```

## Interpretation

v1.21 confirms that the system can execute exactly one controlled Alpaca paper order using the full safety stack.

Validated:

```text
fresh no-submit decision chain
multi-order plan detection
single-order filter
filtered risk-control pass
filtered pre-submit checklist pass
max-plan-age enforcement
explicit run-dir confirmation
controlled submit of exactly one order
broker verification after submit
open-orders check after submit
```

This checkpoint does not approve unattended trading.

## Guardrails

Do not submit another order immediately.
Do not submit from latest when multiple orders are proposed.

Do not submit without:

```text
fresh dry run
risk controls
pre-submit checklist
--max-plan-age-minutes 90
--confirm-run-dir <exact reviewed run dir>
manual review
broker verification
```

## Next Step

Recommended next checkpoint:

```text
v1.22 Post-Full-Safety-Stack Submit Monitoring
```

Purpose: run a fresh no-submit monitoring cycle after the v1.21 UNH buy and verify that the new UNH position and tiny AMD residual are handled safely without unnecessary overtrading.
