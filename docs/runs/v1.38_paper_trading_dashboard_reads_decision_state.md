# v1.38 Paper-Trading Dashboard Reads Decision State

Date: 2026-06-10  
Status: Completed  
Mode: Reporting / no-submit dashboard integration  

## Purpose

Update the paper-trading dashboard/reporting layer so it reads the decision-state classification generated by the v1.35-v1.37 reporting workflow.

This checkpoint makes the decision state visible in dashboard form.

No broker connection is required.

No orders were submitted.

## Starting State

```text
git_tag = v1.37-paper-trading-run-summary-includes-decision-state
tests = 222 passed, 2 warnings
```

## Files Added

```text
src/paper_trading/build_decision_dashboard_with_state.py
tests/test_build_decision_dashboard_with_state.py
docs/runs/paper_trading_decision_dashboard_with_state.md
```

## Dashboard Input Artifacts

The dashboard reads:

```text
paper_trading_run_summary.json
decision_state_report.json
```

The preferred source is:

```text
paper_trading_run_summary.json
```

If that file is unavailable, the dashboard falls back to:

```text
decision_state_report.json
```

## Dashboard Output

The dashboard writes:

```text
docs/runs/paper_trading_decision_dashboard_with_state.md
```

The dashboard includes:

```text
run_dir
state
decision
orders_required
buy_count
sell_count
submit_allowed
orders_submitted
risk_passed
reason
```

## CLI Usage

```bash
python -m src.paper_trading.build_decision_dashboard_with_state \
  --run-dir reports/paper_trading_dry_runs/latest
```

## Latest Dashboard Result

The latest run displayed:

```text
state = NO_CANDIDATE_HOLD
decision = NO_SUBMIT
orders_required = 0
submit_allowed = False
```

## Safety Design

The dashboard utility:

```text
does not connect to Alpaca
does not submit orders
does not alter broker state
does not change execution plans
does not approve trades
```

It only reads local run artifacts and writes a local dashboard markdown file.

## Test Coverage

New tests validate that the dashboard:

```text
reads decision state from paper_trading_run_summary.json
falls back to decision_state_report.json
renders state and decision in markdown
writes the markdown dashboard file
```

Validation commands:

```bash
python -m pytest tests/test_build_decision_dashboard_with_state.py
python -m pytest tests/test_build_run_summary_with_decision_state.py
python -m pytest tests/test_pipeline_decision_state_hook.py
python -m pytest tests/test_classify_decision_state.py
python -m pytest
```

## Interpretation

v1.38 closes the reporting loop by surfacing the decision state in the paper-trading dashboard.
The current dashboard makes the no-submit state visible without requiring manual inspection of JSON files.

## Guardrails

The dashboard is reporting-only.

A future controlled submit still requires:

```text
fresh dry run
evaluation pass
risk controls pass
checklist pass
single-order reviewed directory
manual approval
--max-plan-age-minutes 90
--confirm-run-dir <exact reviewed run dir>
broker verification after submit
documentation
```

## Next Step

Recommended next checkpoint:

```text
v1.39 Paper-Trading Reporting Chain Smoke Test / No-Submit
```

Purpose: run the complete no-submit reporting chain end-to-end:

```text
classification hook
run summary with decision state
dashboard with decision state
tests
documentation
```
