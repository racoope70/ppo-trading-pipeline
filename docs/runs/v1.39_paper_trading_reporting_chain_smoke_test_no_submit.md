# v1.39 Paper-Trading Reporting Chain Smoke Test / No-Submit

Date: 2026-06-10  
Status: Completed  
Mode: Reporting / no-submit smoke test  

## Purpose

Run the complete paper-trading reporting chain end-to-end on the latest local run directory.

This checkpoint validates that the reporting utilities can produce and reconcile:

```text
decision_state_report.json
paper_trading_run_summary.json
paper_trading_decision_dashboard_with_state.md
reporting_chain_smoke_test_report.json
```

No broker connection is required.
No orders were submitted.

## Starting State

```text
git_tag = v1.38-paper-trading-dashboard-reads-decision-state
tests = 226 passed, 2 warnings
```

## Files Added

```text
src/paper_trading/reporting_chain_smoke_test.py
tests/test_reporting_chain_smoke_test.py
```

## Smoke-Test Chain

The smoke test runs:

```text
1. post-checklist decision-state hook
2. paper-trading run summary with decision state
3. paper-trading dashboard with decision state
4. consistency checks across artifacts
```

## CLI Usage

```bash
python -m src.paper_trading.reporting_chain_smoke_test \
  --run-dir reports/paper_trading_dry_runs/latest \
  --prior-symbol AMD \
  --prior-side buy
```

## Latest Smoke-Test Result

The latest smoke test produced:

```text
passed = true
state = NO_CANDIDATE_HOLD
decision = NO_SUBMIT
orders_required = 0
submit_allowed = False
```

## Artifacts Verified

```text
decision_state_report.json = present
paper_trading_run_summary.json = present
paper_trading_decision_dashboard_with_state.md = present
reporting_chain_smoke_test_report.json = present
```

## Safety Design

The smoke-test utility:

```text
does not connect to Alpaca
does not submit orders
does not alter broker state
does not change execution plans
does not approve trades
```

It only reads and writes local reporting artifacts.

## Test Coverage

New tests validate that the smoke test:

```text
writes all expected reporting artifacts
keeps submit_allowed = False
keeps decision = NO_SUBMIT
renders the state and decision into the dashboard
writes a passed smoke-test report
```

Validation commands:

```bash
python -m pytest tests/test_reporting_chain_smoke_test.py
python -m pytest tests/test_build_decision_dashboard_with_state.py
python -m pytest tests/test_build_run_summary_with_decision_state.py
python -m pytest tests/test_pipeline_decision_state_hook.py
python -m pytest tests/test_classify_decision_state.py
python -m pytest
```

## Interpretation

v1.39 validates the reporting chain as an end-to-end no-submit workflow.
The reporting layer now consistently carries the decision state from classifier to run summary to dashboard.

## Guardrails

The smoke test does not authorize trading.
A future controlled submit still requires:

```text
fresh dry run
evaluation pass
risk controls pass
checklist pass
single-order reviewed directory
manual approval
--max-plan-age-minutes 90
--confirm-run-dir <exact reviewed run dir>
broker verification after submit
documentation
```

## Next Step

Recommended next checkpoint:

```text
v1.40 Paper-Trading Operational Reporting Runbook
```

Purpose: document the standard operating procedure for running the reporting chain after each paper-trading dry run.
