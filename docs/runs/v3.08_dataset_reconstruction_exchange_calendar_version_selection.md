# v3.08 Dataset Reconstruction Exchange Calendar Version Selection

## Metadata

```text
checkpoint = v3.08 Dataset Reconstruction Exchange Calendar Version Selection
record_scope = DOCUMENTATION_ONLY_VERSION_SELECTION_RECORD
selected_dependency = exchange-calendars
selected_version = 4.13.2
selected_pin = exchange-calendars==4.13.2
selection_status = GOVERNANCE_SELECTED_DEPENDENCY_PIN_PENDING_FUTURE_INSTALL_AND_RUNTIME_VERIFICATION
exchange_calendars_installed = NO
XNYS_local_runtime_verification = NOT_PERFORMED
dependency_installation_authorized = NO
requirements_change_authorized = NO
code_implementation_authorized = NO
```

## Controlling Decision

The project continues with the executive-selected superseding governed Alpaca-aligned reconstruction direction.

The selected canonical calendar library remains `exchange_calendars` with calendar identifier `XNYS`.

This record selects the exact future version pin:

```text
exchange-calendars==4.13.2
```

This is a governance-selected dependency pin, not a locally installed or runtime-verified dependency.

## Evidence

Planning evidence supporting this governance selection is:

- Official PyPI metadata identifies `exchange_calendars` 4.13.2 as an exact release.
- Release date = March 10, 2026.
- Requires-Python = `>=3.10,<4`.
- Local Python = 3.12.2, therefore compatible by declared metadata.
- The PyPI install instruction supports the exact pin `exchange_calendars==4.13.2`.
- Project examples show `get_calendar("XNYS")`.
- `XNYS` is identified as the New York Stock Exchange calendar.
- Schedule examples show UTC open/close timestamps.
- Session examples show holiday/session exclusion behavior.
- Interval examples show left-closed interval behavior.

This planning evidence does not constitute local installation or runtime verification.

## Selected Contract Term

```text
calendar_library = exchange_calendars
calendar_identifier = XNYS
calendar_version = 4.13.2
calendar_pin = exchange-calendars==4.13.2
calendar_version_status = GOVERNANCE_SELECTED_DEPENDENCY_PIN_PENDING_FUTURE_INSTALL_AND_RUNTIME_VERIFICATION
calendar_runtime_verified = NO
calendar_installation_authorized = NO
```

## Future Required Verification

After dependency installation is separately authorized, a future read-only verification checkpoint must confirm:

- The installed version is exactly 4.13.2.
- `exchange_calendars.get_calendar("XNYS")` succeeds.
- The XNYS schedule covers the full governed reconstruction range.
- Schedule open and close timestamps are timezone-aware UTC.
- Representative holidays are excluded.
- Representative early-close sessions have shortened closes.
- Whole-interval containment logic can be applied deterministically.
- Repeated schedule construction is deterministic.
- Transitive dependency versions are recorded.

## Effect on Literal-Binding Remediation

This record resolves the version-selection blocker at the governance-selection level.

It does not resolve runtime verification.

The future literal-binding remediation record may proceed only if it preserves:

```text
calendar_version_status = GOVERNANCE_SELECTED_DEPENDENCY_PIN_PENDING_FUTURE_INSTALL_AND_RUNTIME_VERIFICATION
```

and does not claim:

```text
calendar_runtime_verified = YES
```

## Explicit Limitations

- `exchange-calendars` is not currently installed in the repo virtual environment.
- XNYS was not locally runtime-verified.
- This does not authorize installing `exchange-calendars`.
- This does not authorize editing `requirements.txt`.
- This does not authorize code implementation.
- This does not authorize data fetching or dataset generation.
- This does not prove historical calendar identity for the missing original dataset.

## Next Checkpoint

```text
next_checkpoint = v3.08 Dataset Reconstruction Contract Literal Binding Remediation
purpose = Record the approved literals, including this calendar version selection, while preserving remaining non-authorization boundaries.
```

## Explicit Non-Authorization Boundaries

```text
code_implementation = NOT_AUTHORIZED
source_code_changes = NOT_AUTHORIZED
test_changes = NOT_AUTHORIZED
dependency_installation = NOT_AUTHORIZED
requirements_change = NOT_AUTHORIZED

data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
dataset_copy = NOT_AUTHORIZED
dataset_restore = NOT_AUTHORIZED
dataset_repair = NOT_AUTHORIZED
dataset_regeneration = NOT_AUTHORIZED
dataset_mutation = NOT_AUTHORIZED
dataset_rewrite = NOT_AUTHORIZED
dataset_validation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_PROVEN

validation_only_preflight_rerun = NOT_AUTHORIZED

training = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED
model_learn = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED

paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
deployment = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED

literal_binding_remediation_record = NOT_CREATED
contract_resolution_record = NOT_CREATED
```

## File/Action Confirmation

```text
files_created_by_this_checkpoint = docs/runs/v3.08_dataset_reconstruction_exchange_calendar_version_selection.md
files_edited_elsewhere = NO
dependencies_installed = NO
requirements_changed = NO
source_code_changed = NO
tests_changed = NO
market_data_accessed = NO
datasets_created = NO
validation_run = NO
training_run = NO
orders_submitted = NO
tag_created = NO
```
