# v3.08 Dependency Installation Execution for exchange-calendars==4.13.2

## Checkpoint metadata

```text
checkpoint = v3.08 Dependency Installation Execution for exchange-calendars==4.13.2
record_scope = LOCAL_DEPENDENCY_INSTALLATION_EXECUTION_EVIDENCE
latest_completed_checkpoint = v3.08 Dependency Installation Authorization for exchange-calendars==4.13.2
latest_completed_commit = 18eb59f9f41e9212efeaa485f3604b3bd222e04a
latest_completed_decision = PASS_DEPENDENCY_INSTALLATION_AUTHORIZATION_FOR_EXACT_EXCHANGE_CALENDARS_PIN_ONLY
latest_completed_ci = NOT_INDEPENDENTLY_VERIFIED_IN_THIS_CHECKPOINT

dependency_installation_execution_result = PASS_EXACT_EXCHANGE_CALENDARS_INSTALLATION_COMPLETED
```

This record captures the separately authorized local installation of exactly `exchange-calendars==4.13.2` into the existing project virtual environment. It does not authorize or perform XNYS runtime verification, source or test implementation, data activity, validation, training, orders, deployment, or tagging.

## Freshness check results

The required freshness commands were run before installation:

```text
git fetch origin
git status --short
git rev-parse HEAD
git rev-parse origin/main
git merge-base --is-ancestor 18eb59f9f41e9212efeaa485f3604b3bd222e04a HEAD
echo $?
```

Observed results:

```text
working_tree = CLEAN
HEAD = 18eb59f9f41e9212efeaa485f3604b3bd222e04a
origin_main = 18eb59f9f41e9212efeaa485f3604b3bd222e04a
required_authorization_commit_is_ancestor = TRUE
merge_base_exit_code = 0
```

## Governing reconstruction classification

```text
reconstruction_classification = SUPERSEDING_GOVERNED_ALPACA_ALIGNED_RECONSTRUCTION
original_dataset_recovered = FALSE
byte_identical_original_dataset_recovery = NOT_CLAIMED
byte_identical_recovery_claim = PROHIBITED
historical_equivalence_claim = NOT_PROVEN
new_dataset_identity_required = TRUE
```

This installation does not alter the reconstruction classification or make any recovery, historical-equivalence, validation, calendar-behavior, or implementation-readiness claim.

## Exact requirements pin verification

The dependency file was inspected before installation. Exactly one exchange-calendars entry was present, and it matched the authorized pin with no conflicting entry.

```text
requirements_txt_contains_exact_pin = YES
exact_requirements_pin = exchange-calendars==4.13.2
matching_requirements_lines = 1
conflicting_exchange_calendars_entries = NONE
requirements_changed = NO
dependency_file_edit_performed = NO
```

## Project virtual environment

Only the required existing project virtual-environment interpreter was used.

```text
python_version = Python 3.12.2
python_executable = /Users/richardcooper/Desktop/Day Trading/Deployable Code Setup - VS Code/quantitative-trading-system-main/.venv/bin/python
pip_version = pip 26.1
pip_location = /Users/richardcooper/Desktop/Day Trading/Deployable Code Setup - VS Code/quantitative-trading-system-main/.venv/lib/python3.12/site-packages/pip
global_python_used = NO
global_pip_used = NO
```

Before installation, `pip show exchange-calendars` reported that the package was not installed. A pre-installation environment snapshot was captured at `/tmp/v3_08_pip_freeze_before_exchange_calendars.txt`.

## Installation command and output

The exact successful installation command was:

```text
installation_command = "/Users/richardcooper/Desktop/Day Trading/Deployable Code Setup - VS Code/quantitative-trading-system-main/.venv/bin/python" -m pip install "exchange-calendars==4.13.2"
```

The initial sandboxed invocation of the same exact command could not resolve PyPI and made no installation. The network-enabled retry used the same interpreter and exact pin and completed successfully.

Successful pip output identified:

```text
Successfully installed exchange-calendars-4.13.2 korean_lunar_calendar-0.4.0 pyluach-2.3.0 toolz-1.1.0 tzdata-2026.3
```

Pip reported that the existing `numpy==1.26.4`, `pandas==3.0.2`, `python-dateutil==2.9.0.post0`, and `six==1.17.0` already satisfied their requirements. Pip was not upgraded.

## Installed-version evidence

`pip show exchange-calendars` and `importlib.metadata.version` both confirmed the exact installed version:

```text
exchange_calendars_installed_version = 4.13.2
exchange_calendars_install_location = /Users/richardcooper/Desktop/Day Trading/Deployable Code Setup - VS Code/quantitative-trading-system-main/.venv/lib/python3.12/site-packages

exchange-calendars==4.13.2
pandas==3.0.2
numpy==1.26.4
pyluach==2.3.0
toolz==1.1.0
tzdata==2026.3
korean-lunar-calendar==0.4.0
python-dateutil==2.9.0.post0
```

The metadata version assertion for `exchange-calendars==4.13.2` passed. No XNYS calendar object was loaded or inspected as part of version confirmation.

## Pip consistency check

The project virtual environment reported:

```text
pip_check_output = No broken requirements found.
pip_check_result = PASS
```

## Dependency drift summary

A post-installation environment snapshot was captured at `/tmp/v3_08_pip_freeze_after_exchange_calendars.txt` and compared with the pre-installation snapshot.

```text
dependency_drift_summary = FIVE_PACKAGES_ADDED_ZERO_REMOVED_ZERO_CHANGED

ADDED_PACKAGES
+ exchange_calendars==4.13.2
+ korean_lunar_calendar==0.4.0
+ pyluach==2.3.0
+ toolz==1.1.0
+ tzdata==2026.3

REMOVED_PACKAGES
NONE

CHANGED_PACKAGES
NONE
```

The observed drift is limited to the exact direct dependency and four dependencies required by it. No existing package version changed, and no existing package was removed.

## Execution result

```text
dependency_installation_execution_result = PASS_EXACT_EXCHANGE_CALENDARS_INSTALLATION_COMPLETED
```

The exact pin is installed in the required project virtual environment, its metadata version assertion passed, and `pip check` passed. This result confirms installation only; it does not prove XNYS calendar behavior.

## XNYS runtime boundary

```text
XNYS_runtime_verification_run = NO
XNYS_get_calendar_called = NO
XNYS_schedule_constructed = NO
XNYS_opens_closes_inspected = NO
XNYS_holidays_inspected = NO
XNYS_early_closes_inspected = NO
XNYS_slot_counts_inspected = NO
XNYS_determinism_checked = NO
XNYS_local_runtime_verification = NOT_PERFORMED
```

No call to `get_calendar("XNYS")` occurred. No XNYS schedules, opens, closes, holidays, early closes, slot counts, or determinism were constructed or inspected. XNYS runtime verification remains a separately governed future checkpoint.

## Boundary confirmations

```text
requirements_changed = NO
dependency_file_edit_performed = NO
source_code_changed = NO
tests_changed = NO
XNYS_runtime_verification_run = NO
XNYS_get_calendar_called = NO
XNYS_schedule_constructed = NO
market_data_accessed = NO
Alpaca_API_called = NO
datasets_created = NO
artifacts_created = NO
validation_run = NO
training_run = NO
orders_submitted = NO
deployment_performed = NO
tag_created = NO
```

## Non-authorization boundaries still preserved

```text
code_implementation = NOT_AUTHORIZED
source_code_changes = NOT_AUTHORIZED
test_changes = NOT_AUTHORIZED
XNYS_local_runtime_verification = NOT_PERFORMED
data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
dataset_validation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_PROVEN
validation_only_preflight_rerun = NOT_AUTHORIZED
training = NOT_AUTHORIZED
model_fitting = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
deployment = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED
```

## Next checkpoints

Because installation passed, the next checkpoint is:

```text
next_checkpoint_if_pass = v3.08 XNYS Runtime Verification Authorization
```

If installation had failed, the next checkpoint would have been:

```text
next_checkpoint_if_fail = v3.08 Dependency Installation Execution Remediation for exchange-calendars==4.13.2
```

This record does not create or execute either later checkpoint.

## File and action confirmation

```text
files_created = docs/runs/v3.08_dependency_installation_execution_exchange_calendars.md
other_repository_files_edited = NO
requirements_changed = NO
dependency_file_edit_performed = NO
dependencies_installed = exchange-calendars==4.13.2 AND REQUIRED_TRANSITIVE_DEPENDENCIES
XNYS_runtime_verification_run = NO
source_code_changed = NO
tests_changed = NO
market_data_accessed = NO
Alpaca_API_called = NO
datasets_created = NO
artifacts_created = NO
validation_run = NO
training_run = NO
orders_submitted = NO
deployment_performed = NO
tag_created = NO
```
