# v1.10 Controlled Alpaca Paper-Order Redeployment Test

Date: 2026-06-01  
Status: Completed  
Mode: Controlled Alpaca paper-order submit test  
Environment: Local VS Code virtual environment  
Broker: Alpaca Paper Trading  

## Purpose

Run one controlled, manually reviewed Alpaca paper-order submission using the v1.9 Alpaca-trained PPO candidate manifest.

This checkpoint follows:

```text
v1.9 Alpaca PPO Paper-Trading Redeployment
```

## Starting Validation

The system started from:

```text
git_tag = v1.9-alpaca-ppo-paper-trading-redeployment
tests = 196 passed, 2 warnings
```

## Manifest

```text
manifest = config/paper_trading_six_ticker_manifest.json
artifacts_dir = models/alpaca_ppo_models_master
```

Selected models:

```json
{
  "AAPL": "ppo_AAPL_window19",
  "AMD": "ppo_AMD_window14",
  "MRK": "ppo_MRK_window20",
  "PFE": "ppo_PFE_window11",
  "UNH": "ppo_UNH_window20",
  "XOM": "ppo_XOM_window15"
}
```

## Fresh No-Submit Decision Chain

Dry-run evaluation passed:

```text
predict_ok_count = 6
error_count = 0
orders_submitted = 0
Evaluation result = PASS
```

Execution plan:

```text
orders_required = 1
gross_intended_notional = 196.57996187106502
buy_count = 1
sell_count = 0
```

Reviewed order:

```text
symbol = AMD
side = buy
qty = 0.384321
price = 511.50
target_weight = 0.0025738220661878
actual_weight = 0.0006149611247040732
delta_notional = 196.57996187106502
```

Risk controls passed:

```text
Risk result = PASS
risk_passed = True
```

## Controlled Submit

The guarded paper-order runner was run with:

```text
submit_orders = True
```

Submitted order:

```text
symbol = AMD
side = buy
qty = 0.384321
order_submitted = True
order_id = ec23c777-179b-4b11-af2a-e286a4c05a55
risk_passed = True
execution_note = order_submitted
```

Paper-order summary:

```text
rows = 6
orders_required = 1
orders_submitted = 1
submit_orders = True
```

## Broker Verification After Submit

Broker account remained active:

```text
status = AccountStatus.ACTIVE
equity = 100354.15
cash = 100095.73
buying_power = 200449.88
```

Position after submit:

```text
AMD qty = 0.504922
market_value = 258.41908
avg_entry = 502.160031
unrealized_pl = 4.867433
unrealized_plpc = 0.0192
```

Open orders:

```text
none
```

## Post-Submit Checklist Interpretation

The post-submit checklist reported FAIL because it includes no-submit safety checks.

Expected post-submit failures:

```text
paper_order_summary_orders_submitted_zero = FAIL
paper_order_summary_no_order_mode = FAIL
audit_log_orders_submitted_zero = FAIL
audit_log_no_order_mode = FAIL
```

These failures are expected because v1.10 intentionally submitted one paper order.

Important checks passed:

```text
dry_run_predict_ok = PASS
dry_run_error_count_zero = PASS
paper_order_risk_passed = PASS
audit_log_risk_passed = PASS
broker_equity_near_expected = PASS
broker_cash_available = PASS
broker_open_orders_zero = PASS
broker_snapshot_errors_empty = PASS
```

## Interpretation

v1.10 confirms that the v1.9 Alpaca PPO redeployment path can submit one small, reviewed paper order through the guarded paper-order runner.

Validated:

- fresh dry-run predictions
- execution-plan creation
- risk-control validation
- manual order review
- guarded submit-orders path
- Alpaca paper endpoint order submission
- broker state verification
- audit logging
- no unexpected open orders

This does not approve unattended trading.

## Guardrails

Do not run unattended trading.

Do not submit repeated orders without a fresh dry-run, execution plan, risk-control pass, broker check, and manual review.

## Next Step

Recommended next checkpoint:

```text
v1.11 Post-Order Monitoring / Rebalance Verification
```

This should monitor the AMD position after the controlled paper order and verify that future dry-runs handle the existing position correctly.
