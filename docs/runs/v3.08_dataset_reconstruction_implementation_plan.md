# v3.08 Dataset Reconstruction Implementation Plan

## Checkpoint

checkpoint = v3.08 Dataset Reconstruction Implementation Plan
scope = DOCUMENTATION_ONLY_IMPLEMENTATION_PLANNING
decision = PASS_IMPLEMENTATION_PLAN_FOR_FUTURE_AUTHORIZATION_REVIEW_ONLY

## Source Chain

recipe_reconstruction_plan_commit = bd02da5
recipe_reconstruction_plan_ci = Tests #380 green

feature_formula_raw_source_specification_commit = 09ea505
feature_formula_raw_source_specification_ci = Tests #381 green

feature_formula_raw_source_specification_review_commit = 240c098
feature_formula_raw_source_specification_review_ci = Tests #382 green

review_decision = PASS_SPECIFICATION_REVIEW_FOR_RECIPE_PLANNING_ONLY

## Explicit Non-Authorization

This checkpoint does not authorize:

- data fetching
- data downloading
- dataset generation
- sealed dataset creation
- sealed dataset validation
- validation-only preflight rerun
- PPO v2 training
- sealed training command execution
- model artifact creation
- paper/live orders
- tagging

This checkpoint does not implement reconstruction code.

This checkpoint does not create command manifests.

This checkpoint does not create evidence artifact files.

This checkpoint does not create, copy, restore, repair, regenerate, mutate, rewrite, validate, or create the sealed dataset.

## Target Missing Dataset

target_missing_dataset = data/processed/ppo_v2/v3_07_no_submit_training_input.parquet

## Recovered Dataset Contract

provider = Alpaca historical bars
bar_interval = 1h
hours_policy = regular session only
timezone = UTC

symbols =
- AAPL
- AMD
- MRK
- PFE
- UNH
- XOM

metadata columns =
- Symbol
- Datetime

configured feature columns =
- Open
- High
- Low
- Close
- Volume
- return_1h
- log_return_1h
- sma_10
- sma_20
- sma_50
- ema_12
- ema_26
- rsi_14
- macd
- macd_signal
- macd_hist
- atr_14
- realized_volatility_20
- volume_zscore_20
- close_to_sma20
- close_to_sma50
- hour_sin
- hour_cos
- day_of_week_sin
- day_of_week_cos

temporal windows =
train: 2023-01-03T14:30:00Z to 2024-06-14T20:00:00Z
train_eval_embargo: 2024-06-17T13:30:00Z to 2024-06-21T20:00:00Z
evaluation: 2024-06-24T13:30:00Z to 2024-12-13T21:00:00Z
eval_holdout_embargo: 2024-12-16T14:30:00Z to 2024-12-20T21:00:00Z
holdout: 2024-12-23T14:30:00Z to 2025-06-30T20:00:00Z

## 1. Proposed Future Module / Script Design

future_module_candidate = src/ppo_v2_dataset_reconstruction.py
future_command_manifest_candidate = artifacts/ppo_v2/dataset_reconstruction/v3_08_reconstruction_command_manifest.txt
future_output_root_candidate = artifacts/ppo_v2/dataset_reconstruction/v3_08_dataset_reconstruction

These files, scripts, command manifests, and output roots are proposed design candidates only.

They are not created in this checkpoint.

future_module_status = PROPOSED_NOT_CREATED
future_command_manifest_status = PROPOSED_NOT_CREATED
future_output_root_status = PROPOSED_NOT_CREATED

## 2. Future Command Design

future_command_status = PROPOSED_NOT_AUTHORIZED

A later authorization checkpoint should define an exact command shape with explicit parameters for:

- run-id
- source provider
- symbols
- bar interval
- session policy
- timezone
- raw retrieval start
- contract start
- contract end
- output path
- output evidence root
- no-training
- no-submit

The proposed command must be treated as non-executable until separately authorized.

No run_id execution is authorized in this checkpoint.

No command manifest is created in this checkpoint.

## 3. Future Raw Data Handling Design

A later authorized implementation should handle:

- Alpaca historical bars
- 1h timeframe
- six-symbol universe
- UTC normalization
- regular-session filtering
- holidays and early closes
- no synthetic missing-bar creation unless separately authorized
- duplicate Symbol/Datetime rejection
- OHLCV numeric validation
- OHLCV nonnegative validation
- raw retrieval span including warm-up before 2023-01-03T14:30:00Z

exact_raw_retrieval_start = STILL_REQUIRES_FINAL_WARMUP_DECISION_BEFORE_GENERATION_AUTHORIZATION

No Alpaca API calls are authorized in this checkpoint.

No data fetching or downloading is authorized in this checkpoint.

## 4. Future Feature Generation Design

Future implementation must follow the reviewed specification from:

feature_formula_raw_source_specification_commit = 09ea505
feature_formula_raw_source_specification_review_commit = 240c098

Preserved proposed defaults:

- grouped by Symbol
- sorted by Symbol, Datetime
- rolling min_periods = window size
- EMA adjust = False
- RSI method = Wilder smoothing
- ATR method = Wilder smoothing
- rolling std ddof = 0
- NaN warm-up rows dropped after feature generation but before split assignment
- infinity values replaced with NaN and handled by drop policy
- zero std in volume_zscore_20 outputs NaN and is handled by drop policy
- output column order = Symbol, Datetime, then configured feature columns in manifest order

This checkpoint does not bind or implement source code.

## 5. Future Temporal Split and Embargo Design

Future implementation must:

- assign rows only after feature generation and warm-up/drop handling
- exclude embargo rows
- preserve train, evaluation, and holdout windows exactly
- prevent holdout use for fitting, normalization, tuning, feature selection, early stopping, manual iteration, or model selection
- produce row counts by symbol and split

Rows inside train/eval embargo and eval/holdout embargo periods must be excluded from training, evaluation, and holdout splits.

## 6. Future Evidence Design

future_evidence_files =
- reconstruction_request.json
- raw_source_manifest.json
- feature_formula_manifest.json
- temporal_split_manifest.json
- dataset_identity.json
- dataset_schema.json
- dataset_row_counts.json
- dataset_missingness_report.json
- dataset_validation_precheck.json
- reconstruction_inventory.json
- reconstruction_checksums.sha256
- reconstruction_git_state.json
- reconstruction_environment.json
- reconstruction_no_training_no_order_attestation.json
- reconstruction_command_log.txt

Future evidence must include:

- output path
- sha256 checksum
- size bytes
- total row count
- row count per symbol
- row count per split
- min/max Datetime per symbol
- schema and column order
- dtypes
- missing/inf count after final cleaning
- duplicate count
- raw source parameters
- git HEAD
- origin/main
- execution-head freshness result
- command manifest
- dependency/environment capture
- no-training attestation
- no-order attestation

No evidence artifact files are created in this checkpoint.

## 7. Future Fail-Closed Behavior

Future implementation must fail closed if:

- command run_id does not match authorized run_id
- output path does not match authorized output path
- source provider does not match Alpaca
- symbols differ from approved six-symbol universe
- timeframe is not 1h
- timezone is not UTC
- session policy is not regular_session_only
- source retrieval start is missing or unapproved
- feature formula spec version is missing
- output dataset already exists and overwrite is not explicitly authorized
- unexpected files would be created
- git HEAD does not include latest required authorization/review commit
- no-training or no-submit/no-order flags are absent
- any training/model/order module is invoked

## 8. Future Authorization and Audit Requirements

Before any code implementation or data generation, a future independent audit or gate review should review this implementation plan and confirm it is sufficient for controlled authorization.

Recommended next checkpoint:

v3.08 Dataset Reconstruction Implementation Plan Review

Purpose:

Review the documentation-only implementation plan before any future code implementation or dataset generation authorization.

Later steps may require separate checkpoints:

- Dataset Reconstruction Code Implementation Authorization
- Dataset Reconstruction Code Review
- Dataset Generation Authorization
- Dataset Generation Execution
- Dataset Reconstruction Evidence Review
- Sealed Dataset Validation Authorization
- Validation-Only Preflight Rerun Authorization

## Preserved Authorization State

data_fetching = NOT_AUTHORIZED
data_downloading = NOT_AUTHORIZED
dataset_generation = NOT_AUTHORIZED
sealed_dataset_creation = NOT_AUTHORIZED
sealed_dataset_validation = NOT_AUTHORIZED
validation_only_preflight_rerun = NOT_AUTHORIZED
ppo_v2_training_execution = NOT_AUTHORIZED
sealed_training_command_execution = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
tagging = NOT_AUTHORIZED

preflight_readiness = NOT_PASSED
sealed_dataset_validation = NOT_PROVEN
sealed_dataset_recovery = NOT_AUTHORIZED
sealed_dataset_restore = NOT_AUTHORIZED
sealed_dataset_copy = NOT_AUTHORIZED
sealed_dataset_repair = NOT_AUTHORIZED
sealed_dataset_regeneration = NOT_AUTHORIZED
sealed_dataset_mutation_or_rewrite = NOT_AUTHORIZED
sealed_dataset_validation_execution = NOT_AUTHORIZED

## Command-Contract Guardrail

Verify that any command/run_id/path/config/output/evidence path in the checkpoint matches source code, config, command manifest, artifact manifest, or source-of-truth record.

Do not accept manually invented values.

If run_id is involved, confirm:

authorized_command_run_id == source_required_run_id

No run_id execution is authorized in this checkpoint.

## Execution-Head Freshness Guardrail

Before any authorized execution or commit workflow:

- git fetch origin
- git status --short
- git rev-parse HEAD
- git rev-parse origin/main
- git merge-base --is-ancestor 240c098 HEAD

Proceed only if:

execution_head_includes_required_review_commit = TRUE

## Boundary Confirmation

external_artifact_inspection = NOT_RUN
data_fetching = NOT_RUN
data_downloading = NOT_RUN
reconstruction_code_implementation = NOT_RUN
command_manifest_creation = NOT_RUN
evidence_artifact_creation = NOT_RUN
dataset_creation = NOT_RUN
dataset_copy = NOT_RUN
dataset_restore = NOT_RUN
dataset_repair = NOT_RUN
dataset_regeneration = NOT_RUN
dataset_mutation_or_rewrite = NOT_RUN
sealed_dataset_validation_execution = NOT_RUN
validation_only_preflight_rerun = NOT_RUN
ppo_v2_training_execution = NOT_RUN
sealed_training_command_execution = NOT_RUN
model_learn = NOT_CALLED
model_fitting = NOT_RUN
model_artifacts = NOT_CREATED
paper_orders = NOT_SUBMITTED
live_orders = NOT_SUBMITTED
tagging = NOT_RUN

## Recommended Next Checkpoint

next_checkpoint = v3.08 Dataset Reconstruction Implementation Plan Review

Purpose:

Review the documentation-only implementation plan before any future code implementation or dataset generation authorization.
