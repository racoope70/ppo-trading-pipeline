# v2.03 PPO v2 Controlled Training Execution Authorization Plan

Date: 2026-06-20
Status: Completed
Mode: Authorization planning only

## Purpose

Define the approval criteria required before any future one-time controlled PPO v2 training execution may be considered.

v2.03 is not an execution checkpoint.

v2.03 does not run PPO training.
v2.03 does not fetch data.
v2.03 does not generate datasets.
v2.03 does not create model artifacts.
v2.03 does not authorize model promotion.
v2.03 does not authorize paper orders.
v2.03 does not authorize live orders.
v2.03 does not authorize controlled submit.
v2.03 does not unblock PPO + Random Forest.
v2.03 does not unblock PPO + XGBoost.

## Starting State

```txt
sealed_checkpoint = v2.02-ppo-v2-controlled-training-execution-wrapper-scaffold-review
sealed_checkpoint_commit = e28c1ba
review_scope = CONTROLLED_TRAINING_EXECUTION_AUTHORIZATION_PLANNING_ONLY
NO_SUBMIT = DEFAULT
```

## Authorization Plan Scope

This checkpoint may define the controls required for a later controlled execution authorization review.

It does not authorize execution.

The future authorization review must still be separate from the future execution checkpoint.

## Required Approval Gates For Future Execution Authorization

```txt
sealed source-of-truth context exists
PROJECT_CONTEXT.md points to the correct active checkpoint
git state is clean
main matches origin/main
all source-of-truth docs exist
v2.01 wrapper scaffold exists
v2.02 wrapper scaffold review is sealed
full tests pass
targeted wrapper tests pass
precise executable-call scan has no unsafe executable calls
static guardrail-token scan shows expected prohibited-token constants only
no broker/order path is reachable
no paper/live order flag is enabled
no controlled-submit flag is enabled
no model promotion flag is enabled
no PPO + RF continuation is enabled
no PPO + XGBoost continuation is enabled
NO-SUBMIT remains default
```

## Required Execution Package For Future One-Time Training

```txt
single explicit command
sealed configuration snapshot
training input manifest
temporal boundary manifest
ticker universe manifest
data source manifest
normalization policy manifest
embargo policy manifest
holdout protection statement
runtime log path
stdout capture path
stderr capture path
quarantine root
artifact inventory path
checksum manifest path
metrics output path
post-training audit package path
failure flag policy
non-promotion statement
non-trading statement
non-hybrid statement
```

## Required PPO v2 Controls

```txt
Alpaca historical loader only
train-only normalization
locked evaluation statistics
explicit embargo
untouched holdout
leakage controls
holdout audit
PPO-only baseline before hybrid discussion
candidate stability review before paper observation
fresh no-submit paper observation before controlled-submit review
NO-SUBMIT default
```

## Fail-Closed Conditions

Authorization must fail closed if any condition below occurs:

```txt
dirty git state
missing source-of-truth document
missing wrapper scaffold
missing wrapper scaffold review
tests fail
unsafe executable call appears in wrapper
training command includes submit/order/live flags
training command lacks --no-submit
training command bypasses quarantine
data source is not explicitly controlled
normalization policy is not train-only
embargo policy is missing
holdout is touched
artifact output is not quarantined
promotion path is present
paper order path is present
live order path is present
controlled submit path is present
PPO + RF is enabled
PPO + XGBoost is enabled
```

## Future Checkpoint Sequence

The next checkpoint should review this authorization plan.

The review checkpoint should not execute training.

Only after the authorization plan is reviewed may a later checkpoint consider a one-time controlled training authorization decision.

## Explicit Non-Authorizations

```txt
PPO training = NOT_AUTHORIZED
data fetching = NOT_AUTHORIZED
dataset generation = NOT_AUTHORIZED
model artifact creation = NOT_AUTHORIZED
model promotion = NOT_AUTHORIZED
paper orders = NOT_AUTHORIZED
live orders = NOT_AUTHORIZED
controlled submit = BLOCKED
PPO + RF = BLOCKED
PPO + XGBoost = BLOCKED
hybrid continuation = BLOCKED
NO_SUBMIT = DEFAULT
```

## Decision

```txt
v2_03_decision = AUTHORIZATION_PLAN_DEFINED
training_execution = NOT_PERFORMED
data_fetching = NOT_PERFORMED
dataset_generation = NOT_PERFORMED
model_artifact_creation = NOT_PERFORMED
model_promotion = NOT_AUTHORIZED
paper_orders = NOT_AUTHORIZED
live_orders = NOT_AUTHORIZED
controlled_submit = BLOCKED
ppo_rf = BLOCKED
ppo_xgboost = BLOCKED
NO_SUBMIT = DEFAULT
```

## Next Checkpoint

```txt
v2.04 PPO v2 Controlled Training Execution Authorization Plan Review
```
