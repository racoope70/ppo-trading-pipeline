# v1.78 PPO v2 Data Preparation Interface Integration Boundary Plan

Date: 2026-06-18  
Status: Completed  
Mode: Planning checkpoint only

## Purpose

Define the controlled integration boundary for how the v1.76 PPO v2 data-preparation interface may eventually connect to the next orchestration layer.

v1.78 is a planning checkpoint only.

v1.78 does not create source code.

v1.78 does not create tests.

v1.78 does not fetch data.

v1.78 does not generate datasets.

v1.78 does not create a training script.

v1.78 does not run retraining.

v1.78 does not create model artifacts.

v1.78 does not authorize paper orders.

v1.78 does not authorize live orders.

v1.78 does not authorize controlled submit.

v1.78 does not unblock PPO + Random Forest.

v1.78 does not unblock PPO + XGBoost.

## Starting State

```txt
starting_checkpoint = v1.77-ppo-v2-data-preparation-interface-scaffold-review-next-boundary-decision
starting_commit = b4dcf13 Add v1.77 PPO v2 data preparation interface scaffold review
starting_tests = 311 passed, 2 warnings
```

## Prior Boundary Inputs

v1.76 created the controlled in-memory data-preparation interface scaffold.

v1.77 reviewed the scaffold and approved it for the next planning checkpoint only.

The existing approved interface is:

```txt
src/ppo_v2_data_preparation_interface.py
tests/test_ppo_v2_data_preparation_interface.py
```

The existing approved data-contract layer is:

```txt
src/ppo_v2_data_contract.py
tests/test_ppo_v2_data_contract.py
```

## Integration Boundary Definition

The future integration layer may connect the existing v1.76 interface to a controlled orchestration boundary.

The future integration layer may only coordinate already-loaded in-memory inputs.

The future integration layer may not fetch data.

The future integration layer may not write datasets.

The future integration layer may not train PPO.

The future integration layer may not create artifacts.

The future integration layer may not submit orders.

The future integration layer may not activate hybrid models.

## Planned Future Inputs

A later checkpoint may define an integration request object with the following conceptual inputs:

```txt
raw_df = already-loaded in-memory dataframe
split_boundary_spec = PPOV2SplitBoundarySpec
observation_columns = approved observation columns
holdout_uses = final-validation-only holdout usage list
preprocessing_fit_split = train_df only
approved_symbols = canonical PPO v2 universe
required_raw_columns = PPO v2 raw-data contract columns
run_identifier = metadata only
execution_mode = dry_run / validation_only only
```

These planned inputs are not implemented in v1.78.

## Planned Future Outputs

A later checkpoint may define integration outputs that preserve the existing v1.76 fail-closed behavior:

```txt
data_preparation_result
integration_errors
integration_metadata
boundary_decision
non_authorization_evidence
```

These planned outputs are not implemented in v1.78.

## Required Future Integration Rules

Any future integration scaffold must satisfy these rules:

```txt
must call the existing v1.76 data-preparation interface
must preserve fail-closed validation behavior
must preserve train/eval/holdout separation
must preserve holdout final-validation-only policy
must preserve train-only preprocessing-fit policy
must preserve forbidden observation-column exclusion
must preserve approved six-ticker PPO v2 universe
must preserve non-execution metadata
must return errors instead of continuing after validation failure
must remain deterministic for identical in-memory inputs
```

## Explicit Future Non-Authorizations

The future integration layer must not include:

```txt
Alpaca client construction
broker API calls
external data download
requests-based data fetch
read_csv ingestion
read_parquet ingestion
generated dataset creation
CSV dataset writing
parquet dataset writing
PPO model loading
PPO model training
sklearn fitting
joblib artifact writing
torch artifact writing
pickle artifact writing
paper-order submission
live-order submission
controlled-submit activation
PPO + RF activation
PPO + XGBoost activation
```

## Integration Acceptance Criteria for a Later Checkpoint

A future implementation checkpoint may be accepted only if it proves:

```txt
integration accepts in-memory data only
integration calls the v1.76 data-preparation interface
integration passes through validation errors
integration returns empty split outputs on fail-closed cases
integration preserves non-authorization metadata
integration includes source boundary scans
integration includes targeted tests
integration does not alter existing v1.73/v1.76 behavior
full test suite passes
GitHub Actions passes
```

## Planned Future Test Categories

A later implementation checkpoint may add tests for:

```txt
successful integration with valid in-memory data
fail-closed propagation of raw-data contract errors
fail-closed propagation of observation-column errors
fail-closed propagation of split-boundary errors
fail-closed propagation of holdout-policy errors
fail-closed propagation of preprocessing-boundary errors
metadata preservation
non-authorization preservation
source boundary scan
deterministic repeated calls
```

v1.78 does not create these tests.

## Risk Controls

The main risk is accidentally turning a planning/integration boundary into an execution boundary.

The control is to keep future integration limited to already-loaded in-memory inputs and to block every external side effect.

The future integration layer must be treated as orchestration glue only.

## Decision

v1.78 authorizes planning for a future controlled PPO v2 data-preparation integration scaffold.

v1.78 does not authorize implementation.

v1.78 does not authorize execution.

## Next Checkpoint

v1.79 PPO v2 Data Preparation Interface Integration Scaffold and Tests
