# v1.87 PPO v2 Controlled Training Execution Authorization Plan

Date: 2026-06-20  
Status: Completed  
Mode: Planning checkpoint only

## Purpose

Define the authorization plan for a future controlled PPO v2 training execution checkpoint.

v1.87 does not execute PPO training.

v1.87 does not create a training script.

v1.87 does not fetch market data.

v1.87 does not generate datasets.

v1.87 does not create model artifacts.

v1.87 does not authorize paper orders.

v1.87 does not authorize live orders.

v1.87 does not authorize controlled submit.

v1.87 does not unblock PPO + Random Forest.

v1.87 does not unblock PPO + XGBoost.

This checkpoint defines the minimum evidence required before a later checkpoint may request controlled PPO v2 training execution.

## Starting State

```txt
starting_checkpoint = v1.86-ppo-v2-training-configuration-review-execution-readiness-boundary
starting_commit = 4a19f76 Add v1.86 PPO v2 training configuration review
starting_tests = 376 passed, 2 warnings
```

## Required Source-of-Truth Review

Before any future controlled PPO v2 training execution can be considered, review:

```txt
PROJECT_CONTEXT.md
docs/workflows/milestone_review_reference_map.md
docs/standards/v1.64_ppo_promotion_standard_acceptance_criteria.md
docs/decisions/v1.65_legacy_ppo_final_audit_decision.md
docs/designs/v1.66_ppo_v2_retraining_design.md
docs/decisions/v1.67_ppo_v2_retraining_authorization_review.md
docs/specifications/v1.72_ppo_v2_controlled_retraining_data_contract_and_split_specification.md
docs/reviews/v1.74_ppo_v2_data_contract_validation_review_next_implementation_boundary.md
docs/reviews/v1.77_ppo_v2_data_preparation_interface_scaffold_review_next_boundary_decision.md
docs/reviews/v1.80_ppo_v2_data_preparation_integration_scaffold_review_next_boundary_decision.md
docs/reviews/v1.83_ppo_v2_training_input_handoff_interface_review_section_closeout.md
docs/reviews/v1.86_ppo_v2_training_configuration_review_execution_readiness_boundary.md
```

PROJECT_CONTEXT.md remains the controlling source of truth.

docs/workflows/milestone_review_reference_map.md is a navigation map only and does not authorize work.

## Completed PPO v2 Pre-Execution Chain

The following chain is complete for non-executing governance, validation, handoff, and configuration scope:

```txt
v1.66 = PPO v2 retraining design
v1.67 = PPO v2 retraining authorization review
v1.72 = PPO v2 data contract and split specification
v1.73 = PPO v2 data contract validation tests
v1.74 = PPO v2 data contract validation review
v1.75 = PPO v2 data preparation interface boundary plan
v1.76 = PPO v2 data preparation interface scaffold and tests
v1.77 = PPO v2 data preparation interface review
v1.78 = PPO v2 data preparation integration boundary plan
v1.79 = PPO v2 data preparation integration scaffold and tests
v1.80 = PPO v2 data preparation integration review
v1.81 = PPO v2 training input handoff boundary plan
v1.82 = PPO v2 training input handoff scaffold and tests
v1.83 = PPO v2 training input handoff review and section closeout
v1.84 = PPO v2 training configuration boundary plan
v1.85 = PPO v2 training configuration scaffold and tests
v1.86 = PPO v2 training configuration review and execution-readiness boundary
```

## Authorization Boundary

v1.87 authorizes planning only.

v1.87 does not authorize:

```txt
data_fetching
generated_dataset_creation
training_script_creation
ppo_training_execution
model_artifact_creation
model_promotion
paper_order_submission
live_order_submission
controlled_submit
ppo_rf_continuation
ppo_xgboost_continuation
```

## Future Controlled Training Execution Request Requirements

A future checkpoint may request controlled PPO v2 training execution only if all required evidence is present.

Required evidence:

```txt
clean git working tree
latest sealed checkpoint is v1.87 or later
PROJECT_CONTEXT.md reviewed
milestone_review_reference_map.md reviewed
legacy PPO remains classified as INFRASTRUCTURE_FIXTURE_ONLY
legacy PPO is not retrained
PPO + RF remains blocked
PPO + XGBoost remains blocked
NO-SUBMIT remains default
data contract tests pass
data preparation interface tests pass
data preparation integration tests pass
training input handoff tests pass
training configuration tests pass
full test suite passes
training configuration boundary scan has no execution hooks
future execution plan identifies exact input data source
future execution plan identifies exact output artifact quarantine location
future execution plan identifies exact training command
future execution plan includes no broker clients
future execution plan includes no paper order path
future execution plan includes no live order path
future execution plan includes no controlled submit path
future execution plan defines post-training audit requirements
future execution plan defines promotion rejection criteria
```

## Future Controlled Training Execution Scope

A future controlled training execution checkpoint may only run training if it remains isolated from deployment and broker execution.

Allowed future scope, if separately authorized later:

```txt
load pre-approved local training input
build PPO v2 training environment
run PPO v2 training with validated configuration
write artifacts only to a quarantined research/artifact directory
record training metadata
record training logs
record seed and configuration
record package/runtime context
create post-training model-quality audit input package
```

Forbidden future scope unless a later checkpoint explicitly authorizes otherwise:

```txt
broker_client_import
paper_order_submission
live_order_submission
controlled_submit
deployment_repository_update
production_artifact_promotion
hybrid_gate_unblocking
PPO_RF_training
PPO_XGBoost_training
live_market_data_fetching
unreviewed_dataset_generation
```

## Required Future Training Command Policy

The future training execution command must be explicit, reviewable, and fail-closed.

A later checkpoint must define the command before execution.

The command must include:

```txt
configuration path or configuration object source
training input source
run identifier
seed
output directory
execution mode
artifact quarantine directory
log directory
```

The command must not include:

```txt
submit_orders
paper_trade
live_trade
controlled_submit
broker_client
deployment_push
artifact_promotion
```

## Required Future Artifact Policy

Any future model artifacts must remain quarantined until model-quality review is complete.

A future training execution checkpoint must define:

```txt
artifact_root
run_id
model_file_path
normalization_file_path
training_metadata_path
training_log_path
configuration_snapshot_path
data_contract_snapshot_path
post_training_audit_package_path
```

No trained artifact may be used for paper trading, live trading, controlled submit, RF, or XGBoost until later promotion checkpoints explicitly authorize it.

## Required Future Post-Training Audit

A future training execution checkpoint must not be considered complete until a separate post-training audit is planned.

The post-training audit must evaluate:

```txt
training completion status
configuration consistency
artifact completeness
data leakage controls
holdout preservation
evaluation results
benchmark comparison
drawdown behavior
Sharpe behavior
turnover behavior
trade count behavior
stability across symbols
failure modes
promotion eligibility
rejection criteria
```

## Minimum Promotion Boundary After Future Training

Even if future PPO v2 training completes successfully, the model remains blocked from paper/live trading until later checkpoints establish:

```txt
offline_model_quality_decision = PASS
trading_edge_decision = PASS_FOR_LIMITED_OBSERVATION_ONLY
paper_order_authorization = explicitly reviewed
controlled_submit_decision = explicitly reviewed
hybrid_gate_status = explicitly reviewed
```

## Planned Future Files

The next checkpoint may create a controlled training execution scaffold or dry-run plan.

Potential future files:

```txt
docs/plans/v1.88_ppo_v2_controlled_training_execution_scaffold_plan.md
docs/runs/v1.88_ppo_v2_controlled_training_execution_scaffold_plan.md
```

A later checkpoint must decide whether to create actual training source code.

## v1.87 Decision

```txt
controlled_training_execution_authorization_plan = COMPLETE
actual_training_execution = NOT_AUTHORIZED
training_script_creation = NOT_AUTHORIZED
data_fetching = NOT_AUTHORIZED
generated_dataset_creation = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
paper_order_authorization = NOT_AUTHORIZED
live_order_authorization = NOT_AUTHORIZED
controlled_submit_decision = BLOCKED
ppo_rf_deployment_decision = BLOCKED
ppo_xgboost_deployment_decision = BLOCKED
next_checkpoint = v1.88 PPO v2 Controlled Training Execution Scaffold / Dry-Run Plan
```

## Final Decision

v1.87 creates the controlled PPO v2 training execution authorization plan.

The project may move to a future controlled training execution scaffold or dry-run planning checkpoint.

Actual PPO v2 training remains blocked until a later checkpoint explicitly authorizes it.
