# v1.94 PPO v2 Controlled Training Execution Authorization Plan

Date: 2026-06-20  
Status: Completed  
Mode: Authorization-planning checkpoint only

## Purpose

Define the authorization plan that must be satisfied before any future controlled PPO v2 training execution can be considered.

v1.94 is an authorization-planning checkpoint only.

v1.94 does not authorize PPO training execution.

v1.94 does not fetch data.

v1.94 does not generate datasets.

v1.94 does not create a training script.

v1.94 does not create model artifacts.

v1.94 does not authorize model promotion.

v1.94 does not authorize paper orders.

v1.94 does not authorize live orders.

v1.94 does not authorize controlled submit.

v1.94 does not unblock PPO + Random Forest.

v1.94 does not unblock PPO + XGBoost.

## Starting State

```txt
starting_checkpoint = v1.93-ppo-v2-controlled-training-execution-scaffold-review-execution-boundary-decision
starting_commit = 79bb8e7 Add v1.93 PPO v2 controlled training execution scaffold review
starting_tests = 448 passed, 2 warnings
```

## Required Source-of-Truth Review

Before any future controlled PPO v2 training execution authorization review, the following must be reviewed:

```txt
PROJECT_CONTEXT.md
docs/workflows/milestone_review_reference_map.md
docs/reviews/v1.93_ppo_v2_controlled_training_execution_scaffold_review_execution_boundary_decision.md
docs/runs/v1.93_ppo_v2_controlled_training_execution_scaffold_review_execution_boundary_decision.md
docs/runs/v1.92_ppo_v2_controlled_training_execution_implementation_scaffold_and_tests.md
src/ppo_v2_controlled_training_execution.py
tests/test_ppo_v2_controlled_training_execution.py
src/ppo_v2_controlled_training_execution_dry_run.py
tests/test_ppo_v2_controlled_training_execution_dry_run.py
src/ppo_v2_training_configuration.py
tests/test_ppo_v2_training_configuration.py
src/ppo_v2_training_input_handoff.py
tests/test_ppo_v2_training_input_handoff.py
```

PROJECT_CONTEXT.md remains the controlling source of truth.

docs/workflows/milestone_review_reference_map.md is a navigation map only and does not authorize work.

## v1.94 Authorization Boundary

v1.94 authorizes planning only.

v1.94 does not authorize:

```txt
source_code_creation
test_code_creation
data_fetching
generated_dataset_creation
training_script_creation
ppo_training_execution
model_artifact_creation
model_promotion
paper_order_submission
live_order_submission
controlled_submit
ppo_rf_continuation
ppo_xgboost_continuation
```

## Future Authorization Objective

A later checkpoint may perform a controlled PPO v2 training execution authorization review.

That future review may decide whether the project is ready to create a controlled execution checkpoint.

The future authorization review must remain separate from actual PPO training execution.

A future authorization review may authorize only the next execution checkpoint design, not model promotion or trading.

## Required Preconditions Before Future Authorization Review

Before a future checkpoint can authorize controlled PPO v2 training execution, all of the following must be true:

```txt
PROJECT_CONTEXT.md was reviewed
milestone_review_reference_map.md was reviewed
v1.93 scaffold review was reviewed
v1.92 controlled execution scaffold is sealed
v1.92 controlled execution scaffold tests are green
training input handoff boundary is sealed and green
training configuration boundary is sealed and green
controlled execution dry-run boundary is sealed and green
controlled execution scaffold boundary is sealed and green
latest full test suite is green
production boundary scan shows no broker/order/deployment hooks in non-executing scaffolds
all non-authorization flags remain disabled
controlled submit remains blocked
paper orders remain blocked
live orders remain blocked
PPO + RF remains blocked
PPO + XGBoost remains blocked
```

## Required Data and Leakage Controls

Before future training execution can be considered, the future authorization review must explicitly confirm:

```txt
historical data source is controlled
Alpaca historical data loader is the controlled historical data source
train/evaluation/holdout split is documented
embargo gap is enforced between train and evaluation periods
embargo compliance is tested or reviewed
final holdout remains untouched until final validation
no final holdout information is used for model selection
feature columns exclude target, return, datetime, and symbol leakage columns
training input handoff uses validated in-memory split objects
training input source is explicitly identified
data-contract boundary decision is PASS
data-preparation boundary decision is PASS
data-preparation integration boundary decision is PASS
training-input handoff boundary decision is PASS
```

## Required Normalization Controls

Before future training execution can be considered, the future authorization review must explicitly confirm:

```txt
VecNormalize or equivalent normalization statistics are fit on train data only
train-only normalization controls are preserved
evaluation uses locked train-only normalization statistics
holdout uses locked train-only normalization statistics only at final validation
normalization artifacts are quarantined if created in a later execution checkpoint
normalization artifacts are not promoted automatically
```

## Required Training Configuration Controls

Before future training execution can be considered, the future authorization review must explicitly confirm:

```txt
training configuration boundary decision is PASS
PPO-only baseline is the only authorized algorithm family
policy type is explicitly defined
total timesteps are explicitly defined
learning rate is explicitly defined
n_steps is explicitly defined
batch size is explicitly defined
gamma is explicitly defined
gae_lambda is explicitly defined
clip_range is explicitly defined
entropy coefficient is explicitly defined
value-function coefficient is explicitly defined
max gradient norm is explicitly defined
seed is explicitly defined
device preference is explicitly defined
environment identifier is explicitly defined
reward contract is explicitly defined
risk contract is explicitly defined
artifact policy remains quarantine-only or disabled
paper order flags are disabled
live order flags are disabled
controlled submit flags are disabled
hybrid continuation flags are disabled
```

## Required Execution Controls

Before future training execution can be considered, the future authorization review must define:

```txt
single controlled execution command
explicit execution mode
explicit runtime environment capture
explicit random seed
explicit timeout
explicit log quarantine root
explicit artifact quarantine root
explicit configuration snapshot
explicit data-contract snapshot
explicit training-input manifest
explicit model output quarantine path
explicit metrics output quarantine path
explicit training log quarantine path
fail-closed behavior
interruption behavior
no automatic promotion behavior
no automatic deployment behavior
no automatic paper trading behavior
no automatic live trading behavior
```

## Required Quarantine Policy

Any future training output must remain quarantined.

Required quarantine policy:

```txt
model artifact quarantine required
normalization artifact quarantine required if produced
training logs quarantine required
metrics output quarantine required
configuration snapshot quarantine required
data-contract snapshot quarantine required
training-input manifest quarantine required
checksums required for future artifacts
artifact inventory required
post-training audit required
promotion checkpoint required
paper-trading checkpoint required
controlled-submit checkpoint required
hybrid-gate checkpoint required
```

## Required Post-Training Audit Package

If a later checkpoint eventually executes PPO v2 training, the output must not be promoted until a post-training audit package is created and reviewed.

The future post-training audit package must include:

```txt
training command
training configuration snapshot
data-contract snapshot
training input manifest
random seed
runtime environment
training duration
training logs
metrics summary
artifact inventory
model checksum
normalization checksum if applicable
failure warnings
leakage-control confirmation
embargo-control confirmation
holdout untouched confirmation
non-promotion statement
paper-trading non-authorization statement
controlled-submit non-authorization statement
hybrid-continuation non-authorization statement
next audit decision
```

## Required Model Quality Gates After Training

A future trained PPO v2 candidate must not be promoted unless later audit checkpoints show:

```txt
PPO-only baseline performance package is complete
train/eval metrics are documented
final untouched holdout validation is complete
candidate selection occurs only after holdout validation and audit review
historical validation evidence is reviewed
candidate stability review is complete
fresh no-submit paper observation evidence is complete
no-submit paper observation review is complete
controlled submit review is separately approved
```

These are future gates only.

v1.94 does not validate model quality.

## Hybrid Gate Restrictions

PPO + Random Forest and PPO + XGBoost remain blocked.

Hybrid gate work cannot resume until:

```txt
PPO v2 controlled training execution is completed under a later authorized checkpoint
post-training audit is completed
PPO-only baseline performance package is completed
PPO-only model quality is accepted
final untouched holdout validation is accepted
candidate stability review is accepted
fresh no-submit paper observation evidence is accepted
hybrid continuation is separately authorized
```

## Future Authorization Review Decision Options

A future authorization review checkpoint may choose one of:

```txt
REJECT_CONTROLLED_TRAINING_EXECUTION
CONTINUE_PLANNING
AUTHORIZE_CONTROLLED_TRAINING_EXECUTION_CHECKPOINT_DESIGN_ONLY
AUTHORIZE_ONE_TIME_CONTROLLED_TRAINING_EXECUTION_CHECKPOINT
```

Only the final option would permit a later checkpoint to run controlled PPO training.

Even that option would not authorize promotion, paper trading, live trading, controlled submit, or hybrid continuation.

## Future Non-Authorization Defaults

All future checkpoints remain governed by these defaults unless explicitly changed by a sealed later checkpoint:

```txt
data_fetching = NOT_AUTHORIZED
generated_dataset_creation = NOT_AUTHORIZED
ppo_training_execution = NOT_AUTHORIZED
model_artifact_creation = NOT_AUTHORIZED
model_promotion = NOT_AUTHORIZED
paper_order_authorization = NOT_AUTHORIZED
live_order_authorization = NOT_AUTHORIZED
controlled_submit_decision = BLOCKED
ppo_rf_deployment_decision = BLOCKED
ppo_xgboost_deployment_decision = BLOCKED
NO-SUBMIT remains default
```

## Required v1.94 Validation Evidence

The v1.94 checkpoint must prove:

```txt
targeted data-contract tests pass
targeted data-preparation interface tests pass
targeted data-preparation integration tests pass
targeted training-input handoff tests pass
targeted training-configuration tests pass
targeted controlled-training dry-run tests pass
targeted controlled-training execution tests pass
full test suite passes
controlled execution source boundary scan has no execution hooks
```

## Final Decision

v1.94 defines the controlled PPO v2 training execution authorization plan.

No data fetching, generated dataset creation, training script creation, PPO training execution, model artifact creation, model promotion, order submission, controlled submit, or hybrid deployment is authorized.

The project may move to a future controlled PPO v2 training execution authorization review checkpoint.

## Next Checkpoint

```txt
v1.95 PPO v2 Controlled Training Execution Authorization Review
```
